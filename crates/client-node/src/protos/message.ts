// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               v7.36.1
// source: message.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "firefly";

export enum CallSignalType {
  /** CALL_REQUEST - Call invitation (contains SDP Offer) */
  CALL_REQUEST = 0,
  /** CALL_ANSWER - Call accepted (contains SDP Answer) */
  CALL_ANSWER = 1,
  /** CALL_REJECT - Call declined by receiver */
  CALL_REJECT = 2,
  /** CALL_CANCEL - Call canceled by caller before answer */
  CALL_CANCEL = 3,
  /** CALL_HANGUP - Call ended by either party during or after session */
  CALL_HANGUP = 4,
  /** CALL_DISMISS - Server-sent notification to dismiss call on other devices */
  CALL_DISMISS = 5,
  /** CALL_ICECANDIDATE - Dynamic ICE candidates signaling */
  CALL_ICECANDIDATE = 6,
  UNRECOGNIZED = -1,
}

export function callSignalTypeFromJSON(object: any): CallSignalType {
  switch (object) {
    case 0:
    case "CALL_REQUEST":
      return CallSignalType.CALL_REQUEST;
    case 1:
    case "CALL_ANSWER":
      return CallSignalType.CALL_ANSWER;
    case 2:
    case "CALL_REJECT":
      return CallSignalType.CALL_REJECT;
    case 3:
    case "CALL_CANCEL":
      return CallSignalType.CALL_CANCEL;
    case 4:
    case "CALL_HANGUP":
      return CallSignalType.CALL_HANGUP;
    case 5:
    case "CALL_DISMISS":
      return CallSignalType.CALL_DISMISS;
    case 6:
    case "CALL_ICECANDIDATE":
      return CallSignalType.CALL_ICECANDIDATE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CallSignalType.UNRECOGNIZED;
  }
}

export function callSignalTypeToJSON(object: CallSignalType): string {
  switch (object) {
    case CallSignalType.CALL_REQUEST:
      return "CALL_REQUEST";
    case CallSignalType.CALL_ANSWER:
      return "CALL_ANSWER";
    case CallSignalType.CALL_REJECT:
      return "CALL_REJECT";
    case CallSignalType.CALL_CANCEL:
      return "CALL_CANCEL";
    case CallSignalType.CALL_HANGUP:
      return "CALL_HANGUP";
    case CallSignalType.CALL_DISMISS:
      return "CALL_DISMISS";
    case CallSignalType.CALL_ICECANDIDATE:
      return "CALL_ICECANDIDATE";
    case CallSignalType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum CallMessageType {
  none = 0,
  request = 1,
  reject = 2,
  end = 3,
  /** ended - for saving call messages */
  ended = 4,
  rejected = 5,
  /** candidate - webrtc messages */
  candidate = 10,
  answer = 11,
  offer = 12,
  UNRECOGNIZED = -1,
}

export function callMessageTypeFromJSON(object: any): CallMessageType {
  switch (object) {
    case 0:
    case "none":
      return CallMessageType.none;
    case 1:
    case "request":
      return CallMessageType.request;
    case 2:
    case "reject":
      return CallMessageType.reject;
    case 3:
    case "end":
      return CallMessageType.end;
    case 4:
    case "ended":
      return CallMessageType.ended;
    case 5:
    case "rejected":
      return CallMessageType.rejected;
    case 10:
    case "candidate":
      return CallMessageType.candidate;
    case 11:
    case "answer":
      return CallMessageType.answer;
    case 12:
    case "offer":
      return CallMessageType.offer;
    case -1:
    case "UNRECOGNIZED":
    default:
      return CallMessageType.UNRECOGNIZED;
  }
}

export function callMessageTypeToJSON(object: CallMessageType): string {
  switch (object) {
    case CallMessageType.none:
      return "none";
    case CallMessageType.request:
      return "request";
    case CallMessageType.reject:
      return "reject";
    case CallMessageType.end:
      return "end";
    case CallMessageType.ended:
      return "ended";
    case CallMessageType.rejected:
      return "rejected";
    case CallMessageType.candidate:
      return "candidate";
    case CallMessageType.answer:
      return "answer";
    case CallMessageType.offer:
      return "offer";
    case CallMessageType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum MeetingSessionStatus {
  MEETING_STATUS_ACTIVE = 0,
  MEETING_STATUS_ENDED = 1,
  UNRECOGNIZED = -1,
}

export function meetingSessionStatusFromJSON(object: any): MeetingSessionStatus {
  switch (object) {
    case 0:
    case "MEETING_STATUS_ACTIVE":
      return MeetingSessionStatus.MEETING_STATUS_ACTIVE;
    case 1:
    case "MEETING_STATUS_ENDED":
      return MeetingSessionStatus.MEETING_STATUS_ENDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MeetingSessionStatus.UNRECOGNIZED;
  }
}

export function meetingSessionStatusToJSON(object: MeetingSessionStatus): string {
  switch (object) {
    case MeetingSessionStatus.MEETING_STATUS_ACTIVE:
      return "MEETING_STATUS_ACTIVE";
    case MeetingSessionStatus.MEETING_STATUS_ENDED:
      return "MEETING_STATUS_ENDED";
    case MeetingSessionStatus.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum MeetingSignalType {
  /** MEETING_SIGNAL_STARTED - New meeting created in a channel */
  MEETING_SIGNAL_STARTED = 0,
  /** MEETING_SIGNAL_JOINED - A user joined */
  MEETING_SIGNAL_JOINED = 1,
  /** MEETING_SIGNAL_LEFT - A user left */
  MEETING_SIGNAL_LEFT = 2,
  /** MEETING_SIGNAL_ENDED - Meeting ended (by creator or last participant) */
  MEETING_SIGNAL_ENDED = 3,
  UNRECOGNIZED = -1,
}

export function meetingSignalTypeFromJSON(object: any): MeetingSignalType {
  switch (object) {
    case 0:
    case "MEETING_SIGNAL_STARTED":
      return MeetingSignalType.MEETING_SIGNAL_STARTED;
    case 1:
    case "MEETING_SIGNAL_JOINED":
      return MeetingSignalType.MEETING_SIGNAL_JOINED;
    case 2:
    case "MEETING_SIGNAL_LEFT":
      return MeetingSignalType.MEETING_SIGNAL_LEFT;
    case 3:
    case "MEETING_SIGNAL_ENDED":
      return MeetingSignalType.MEETING_SIGNAL_ENDED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return MeetingSignalType.UNRECOGNIZED;
  }
}

export function meetingSignalTypeToJSON(object: MeetingSignalType): string {
  switch (object) {
    case MeetingSignalType.MEETING_SIGNAL_STARTED:
      return "MEETING_SIGNAL_STARTED";
    case MeetingSignalType.MEETING_SIGNAL_JOINED:
      return "MEETING_SIGNAL_JOINED";
    case MeetingSignalType.MEETING_SIGNAL_LEFT:
      return "MEETING_SIGNAL_LEFT";
    case MeetingSignalType.MEETING_SIGNAL_ENDED:
      return "MEETING_SIGNAL_ENDED";
    case MeetingSignalType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export enum HistorySignalType {
  HISTORY_SIGNAL_REQUEST_CREATED = 0,
  HISTORY_SIGNAL_CHUNK_PUBLISHED = 1,
  HISTORY_SIGNAL_CHUNK_DISAPPROVED = 2,
  HISTORY_SIGNAL_REQUEST_FULFILLED = 3,
  HISTORY_SIGNAL_CHUNK_VERIFIED = 4,
  UNRECOGNIZED = -1,
}

export function historySignalTypeFromJSON(object: any): HistorySignalType {
  switch (object) {
    case 0:
    case "HISTORY_SIGNAL_REQUEST_CREATED":
      return HistorySignalType.HISTORY_SIGNAL_REQUEST_CREATED;
    case 1:
    case "HISTORY_SIGNAL_CHUNK_PUBLISHED":
      return HistorySignalType.HISTORY_SIGNAL_CHUNK_PUBLISHED;
    case 2:
    case "HISTORY_SIGNAL_CHUNK_DISAPPROVED":
      return HistorySignalType.HISTORY_SIGNAL_CHUNK_DISAPPROVED;
    case 3:
    case "HISTORY_SIGNAL_REQUEST_FULFILLED":
      return HistorySignalType.HISTORY_SIGNAL_REQUEST_FULFILLED;
    case 4:
    case "HISTORY_SIGNAL_CHUNK_VERIFIED":
      return HistorySignalType.HISTORY_SIGNAL_CHUNK_VERIFIED;
    case -1:
    case "UNRECOGNIZED":
    default:
      return HistorySignalType.UNRECOGNIZED;
  }
}

export function historySignalTypeToJSON(object: HistorySignalType): string {
  switch (object) {
    case HistorySignalType.HISTORY_SIGNAL_REQUEST_CREATED:
      return "HISTORY_SIGNAL_REQUEST_CREATED";
    case HistorySignalType.HISTORY_SIGNAL_CHUNK_PUBLISHED:
      return "HISTORY_SIGNAL_CHUNK_PUBLISHED";
    case HistorySignalType.HISTORY_SIGNAL_CHUNK_DISAPPROVED:
      return "HISTORY_SIGNAL_CHUNK_DISAPPROVED";
    case HistorySignalType.HISTORY_SIGNAL_REQUEST_FULFILLED:
      return "HISTORY_SIGNAL_REQUEST_FULFILLED";
    case HistorySignalType.HISTORY_SIGNAL_CHUNK_VERIFIED:
      return "HISTORY_SIGNAL_CHUNK_VERIFIED";
    case HistorySignalType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface UserMessage {
  id: bigint;
  toId: bigint;
  fromId: bigint;
  text: Buffer;
  type: number;
  /** flags for server to notify or just send or don't send */
  settings: number;
  hashValue: bigint;
  /** optional sends these for decryption purposes */
  fromUsername: string;
  fromDeviceId: number;
}

export interface Group {
  id: bigint;
  name: string;
  description: string;
  state: Buffer;
  settings: number;
  upgraded: boolean;
  pending: boolean;
  owner: string;
}

export interface GroupDetails {
  id: bigint;
  name: string;
  description: string;
  settings: number;
  upgraded: boolean;
  owner: string;
  inviter: string;
}

export interface Groups {
  groups: Group[];
}

export interface UserMessages {
  messages: UserMessage[];
}

export interface GroupInvite {
  groupId: bigint;
  inviter: string;
  invitee: string;
  welcomeMessage: Buffer;
  commitId: bigint;
}

export interface GroupCommitAndWelcome {
  id: bigint;
  groupId: bigint;
  commitMessage: Buffer;
  inviter: string;
  invitee: string;
  welcomeMessages: Buffer[];
  inviteeAddresses: bigint[];
}

export interface GroupInvites {
  invites: GroupInvite[];
}

export interface GroupMessage {
  id: bigint;
  groupId: bigint;
  message: Buffer;
  epoch: number;
}

export interface GroupKeyPackage {
  address: bigint;
  package: Buffer;
  username: string;
  id: number;
}

export interface GroupKeyPackages {
  packages: GroupKeyPackage[];
}

export interface GroupMessages {
  messages: GroupMessage[];
}

export interface GroupSyncRequest {
  groupId: bigint;
  startAfter: bigint;
  until: bigint;
  limit: number;
}

export interface GroupSyncRequests {
  requests: GroupSyncRequest[];
}

export interface GroupMemberUpdate {
  groupId: bigint;
  lastMessageSeen: bigint;
  lastEpoch: number;
}

export interface GroupMemberUpdates {
  updates: GroupMemberUpdate[];
}

export interface GroupCommit {
  id: bigint;
  groupId: bigint;
  commit: Buffer;
  epoch: number;
}

export interface GroupCommits {
  commits: GroupCommit[];
}

export interface GroupCommitSyncRequest {
  groupId: bigint;
  epoch: number;
}

export interface GroupReAddRequest {
  groupId: bigint;
  addressId: bigint;
  username: string;
}

export interface GroupReAddRequests {
  requests: GroupReAddRequest[];
}

export interface GroupMemberOnlineStatus {
  addressId: bigint;
  username: string;
  deviceId: number;
  lastConnectedAt: bigint;
  isOnline: boolean;
}

export interface GroupMembersOnlineStatus {
  members: GroupMemberOnlineStatus[];
}

export interface Error {
  error: string;
  errorCode: number;
}

export interface Result {
  body: Buffer;
  resultCode: number;
}

export interface Address {
  id: bigint;
  username: string;
  fcmToken: string;
  deviceId: number;
}

export interface Addresses {
  addresses: Address[];
}

export interface UploadUserMessage {
  messages: UserMessage[];
}

export interface MessageIdAndTo {
  id: bigint;
  to: bigint;
  isSelf: boolean;
}

export interface UserMessageUploaded {
  messageIds: MessageIdAndTo[];
}

export interface UserOnlineStatusRequest {
  /** max 32 usernames */
  usernames: string[];
}

export interface UserOnlineStatusResponse {
  /** bit i is 1 if usernames[i] is online */
  onlineBits: number;
}

export interface Request {
  id: number;
  createUserMessage?: UserMessage | undefined;
  uploadUserMessage?: UploadUserMessage | undefined;
  uploadGroupMessage?: GroupMessage | undefined;
  requestGroupReAdds?: RequestGroupReAdds | undefined;
  requestGroupSync?: RequestGroupSync | undefined;
  userOnlineStatus?: UserOnlineStatusRequest | undefined;
  createJoinLink?: CreateJoinLinkRequest | undefined;
  joinViaLink?: JoinViaLinkRequest | undefined;
  createMeeting?: CreateMeetingRequest | undefined;
  joinMeeting?: JoinMeetingRequest | undefined;
  leaveMeeting?: LeaveMeetingRequest | undefined;
  endMeeting?: EndMeetingRequest | undefined;
  getActiveSession?: GetActiveSessionRequest | undefined;
  createHistoryRequest?: CreateHistoryRequest | undefined;
  claimHistoryRequest?: ClaimHistoryRequest | undefined;
  publishHistoryChunk?: PublishHistoryChunkRequest | undefined;
  disapproveHistoryChunk?: DisapproveHistoryChunkRequest | undefined;
  getHistoryChunks?: GetHistoryChunksRequest | undefined;
  getPendingHistoryRequests?: GetPendingHistoryRequests | undefined;
  closeHistoryRequest?: CloseHistoryRequest | undefined;
  approveHistoryChunk?: ApproveHistoryChunkRequest | undefined;
}

export interface Response {
  id: number;
  error: Error | undefined;
  createdUserMessage?: UserMessage | undefined;
  userMessageUploaded?: UserMessageUploaded | undefined;
  groupMessageUploaded?: GroupMessage | undefined;
  groupReAddRequestSuccess?: GroupReAddRequestSuccess | undefined;
  userOnlineStatus?: UserOnlineStatusResponse | undefined;
  createJoinLink?: CreateJoinLinkResponse | undefined;
  joinViaLinkSuccess?: JoinViaLinkSuccess | undefined;
  createMeetingResponse?: CreateMeetingResponse | undefined;
  joinMeetingResponse?: JoinMeetingResponse | undefined;
  getActiveSessionResponse?: GetActiveSessionResponse | undefined;
  getHistoryChunksResponse?: GetHistoryChunksResponse | undefined;
  getPendingHistoryRequestsResponse?: GetPendingHistoryRequestsResponse | undefined;
  historySuccess?: HistoryOperationSuccess | undefined;
  claimHistoryResponse?: ClaimHistoryResponse | undefined;
}

export interface ServerMessage {
  userMessage?: UserMessage | undefined;
  groupMessage?: GroupMessage | undefined;
  userMessages?: UserMessages | undefined;
  groupMessages?: GroupMessages | undefined;
  response?: Response | undefined;
  ping?: Buffer | undefined;
  pong?: Buffer | undefined;
  readUserMessagesUpto?: ReadUserMessagesUpto | undefined;
  groupInvite?: GroupInvite | undefined;
  groupCommits?: GroupCommits | undefined;
  groupReAddRequests?: GroupReAddRequests | undefined;
  groupJoinRequests?: GroupJoinRequests | undefined;
  callSignal?: CallSignal | undefined;
  groupMeetingSignal?: GroupMeetingSignal | undefined;
  groupHistorySignal?: GroupHistorySignal | undefined;
}

export interface ClientMessage {
  userMessage?: UserMessage | undefined;
  groupMessage?: GroupMessage | undefined;
  verifiedUserMessage?: UserMessage | undefined;
  request?: Request | undefined;
  ping?: Buffer | undefined;
  pong?: Buffer | undefined;
  readUserMessagesUpto?: ReadUserMessagesUpto | undefined;
  callSignal?: CallSignal | undefined;
  groupMeetingSignal?: GroupMeetingSignal | undefined;
}

export interface ReadUserMessagesUpto {
  other: string;
  uptoMessageId: bigint;
}

export interface CallSignal {
  callId: bigint;
  senderUsername: string;
  receiverUsername: string;
  type: CallSignalType;
  sdp: string;
  candidate: string;
  sdpMLineIndex: number;
  sdpMid: string;
  senderDeviceId: number;
}

export interface GroupId {
  id: bigint;
}

export interface AuthToken {
  username: string;
  validUntil: bigint;
  credential: Buffer;
  addressId: bigint;
  deviceId: number;
}

export interface SignedToken {
  kid: string;
  payload: Buffer;
  signature: Buffer;
}

export interface FireflyIdentity {
  secret: Buffer;
  public: Buffer;
  credential: Buffer;
}

export interface FireflyGroupExtension {
  name: string;
  roles: FireflyGroupRole[];
  channels: FireflyGroupChannel[];
  members: FireflyGroupMember[];
  defaultPermissions: number;
}

export interface FireflyGroupRole {
  id: number;
  name: string;
  permissions: number;
  color: number;
}

export interface FireflyGroupMember {
  username: string;
  role: number;
}

export interface FireflyGroupChannel {
  id: number;
  name: string;
  type: number;
  roles: FireflyGroupRole[];
  defaultPermissions: number;
}

export interface PreKeyBundle {
  registrationId: number;
  deviceId: number;
  preKeyId: number;
  prePublicKey: Buffer;
  signedPreKeyId: number;
  signedPrePublicKey: Buffer;
  signedPreKeySignature: Buffer;
  identityPublicKey: Buffer;
  KEMPreKeyId: number;
  KEMPrePublicKey: Buffer;
  KEMPreKeySignature: Buffer;
}

export interface PreKeyBundleEntry {
  id: number;
  address: bigint;
  bundle: PreKeyBundle | undefined;
  username: string;
  deviceId: number;
}

export interface PreKeyBundleEntries {
  entries: PreKeyBundleEntry[];
}

export interface ConversationStart {
  conversationId: bigint;
  startedBy: string;
  other: string;
  bundle: PreKeyBundle | undefined;
}

export interface PreKeyBundles {
  bundles: PreKeyBundle[];
}

export interface Conversation {
  user1: string;
  user2: string;
  settings: bigint;
}

export interface Conversations {
  conversations: Conversation[];
}

export interface EncryptedFile {
  url: string;
  secretKey: Buffer;
  contentType: number;
  contentLength: number;
}

export interface EncryptedFiles {
  files: EncryptedFile[];
}

export interface MessagePayload {
  text: string;
  files: EncryptedFiles | undefined;
  editedOf?: bigint | undefined;
  replyingTo?: bigint | undefined;
  deleted?: bigint | undefined;
  messageType: number;
}

export interface CallMessage {
  message: Buffer;
  type: CallMessageType;
  jsonBody: string;
  sessionId: number;
}

export interface SelfUserMessage {
  to: string;
  /** UserMessageInner encrypted */
  inner: Buffer;
}

export interface Reaction {
  reactingTo: bigint;
  reaction: number;
}

export interface UserMessageInner {
  plainText?: Buffer | undefined;
  callMessage?: CallMessage | undefined;
  messagePayload?: MessagePayload | undefined;
  selfMessage?: SelfUserMessage | undefined;
  reaction?: Reaction | undefined;
  backupKeySync?: DirectBackupKeySync | undefined;
  directPinSnapshot?: DirectPinSnapshotSecret | undefined;
  nonce: number;
  messageType: number;
}

export interface GroupPinUpdate {
  messageId: bigint;
  pinned: boolean;
}

export interface GroupPinSnapshot {
  messages: GroupHistoryRecord[];
}

export interface GroupMessageInner {
  channelId: number;
  messagePayload?: MessagePayload | undefined;
  pinUpdate?: GroupPinUpdate | undefined;
  pinSnapshot?: GroupPinSnapshot | undefined;
  syncBundle?: GroupSyncBundle | undefined;
  reaction?: Reaction | undefined;
  messageType: number;
}

export interface RequestGroupReAdds {
  groupIds: bigint[];
}

export interface RequestGroupSync {
  groupId: bigint;
  epoch: number;
}

export interface GroupReAddRequestSuccess {
}

export interface CreateJoinLinkRequest {
  groupId: bigint;
  expiresInSeconds: bigint;
  maxUses: number;
}

export interface CreateJoinLinkResponse {
  token: string;
}

export interface JoinViaLinkRequest {
  token: string;
}

export interface JoinViaLinkSuccess {
}

export interface GroupJoinRequest {
  groupId: bigint;
  addressId: bigint;
  username: string;
}

export interface GroupJoinRequests {
  requests: GroupJoinRequest[];
}

export interface GroupMeetingSession {
  /** UUIDv7 timestamp-based ID */
  sessionId: bigint;
  groupId: bigint;
  /** Which voice/text channel this meeting is in */
  channelId: number;
  creatorUsername: string;
  status: MeetingSessionStatus;
  /** Microsecond timestamp */
  createdAt: bigint;
  endedAt: bigint;
  /** Current participant usernames */
  participants: string[];
  /** Cloudflare RealtimeKit meeting ID */
  cfMeetingId: string;
  e2eeEnabled: boolean;
}

export interface CreateMeetingRequest {
  groupId: bigint;
  channelId: number;
  e2eeEnabled: boolean;
}

export interface CreateMeetingResponse {
  session:
    | GroupMeetingSession
    | undefined;
  /** CF RealtimeKit auth token for creator */
  participantToken: string;
}

export interface JoinMeetingRequest {
  groupId: bigint;
  sessionId: bigint;
}

export interface JoinMeetingResponse {
  session:
    | GroupMeetingSession
    | undefined;
  /** CF RealtimeKit auth token for joiner */
  participantToken: string;
}

export interface LeaveMeetingRequest {
  groupId: bigint;
  sessionId: bigint;
}

export interface EndMeetingRequest {
  groupId: bigint;
  sessionId: bigint;
}

export interface GetActiveSessionRequest {
  groupId: bigint;
  channelId: number;
}

export interface GetActiveSessionResponse {
  /** null/empty if no active session */
  session: GroupMeetingSession | undefined;
}

/** Signal sent over WebSocket to notify group members of meeting events */
export interface GroupMeetingSignal {
  groupId: bigint;
  channelId: number;
  sessionId: bigint;
  type: MeetingSignalType;
  /** Who triggered the event */
  username: string;
  cfMeetingId: string;
}

export interface GroupHistoryRequestItem {
  id: bigint;
  groupId: bigint;
  requesterAddress: bigint;
  requesterUsername: string;
  startMsgId: bigint;
  endMsgId: bigint;
  /** 0=pending, 1=in_progress, 2=fulfilled, 3=closed */
  status: number;
  claimedBy: bigint;
  createdAt: bigint;
}

export interface GroupHistoryChunkItem {
  id: bigint;
  groupId: bigint;
  startMsgId: bigint;
  endMsgId: bigint;
  msgCount: number;
  unencryptedHash: Buffer;
  chunkUrl: string;
  uploadedBy: string;
  /** 0=valid, 1=disapproved */
  status: number;
  disapprovedBy: string;
  disapprovedReason: string;
  createdAt: bigint;
  verified: boolean;
}

export interface GroupHistoryChunkKey {
  groupId: bigint;
  startMsgId: bigint;
  endMsgId: bigint;
  key: Buffer;
  nonce: Buffer;
  unencryptedHash: Buffer;
}

export interface GroupHistoryKeysPayload {
  keys: GroupHistoryChunkKey[];
  chunks: GroupHistoryChunkItem[];
}

export interface CreateHistoryRequest {
  groupId: bigint;
  startMsgId: bigint;
  endMsgId: bigint;
}

export interface ClaimHistoryRequest {
  groupId: bigint;
  requestId: bigint;
  startMsgId: bigint;
  endMsgId: bigint;
}

export interface ClaimHistoryResponse {
  granted: boolean;
  message: string;
}

export interface PublishHistoryChunkRequest {
  reserveOnly: boolean;
  groupId: bigint;
  startMsgId: bigint;
  endMsgId: bigint;
  msgCount: number;
  unencryptedHash: Buffer;
  chunkUrl: string;
}

export interface DisapproveHistoryChunkRequest {
  groupId: bigint;
  chunkId: bigint;
  reason: string;
}

export interface GetHistoryChunksRequest {
  groupId: bigint;
  sinceMsgId: bigint;
  untilMsgId: bigint;
  includeUnverified: boolean;
}

export interface GetHistoryChunksResponse {
  chunks: GroupHistoryChunkItem[];
}

export interface GetPendingHistoryRequests {
  groupIds: bigint[];
}

export interface GetPendingHistoryRequestsResponse {
  requests: GroupHistoryRequestItem[];
}

export interface CloseHistoryRequest {
  groupId: bigint;
  requestId: bigint;
}

export interface HistoryOperationSuccess {
}

export interface GroupHistorySignal {
  groupId: bigint;
  type: HistorySignalType;
  requestId: bigint;
  chunkId: bigint;
  username: string;
}

/** Additive, versioned history envelope. Fields 1-4 retain the old record layout. */
export interface GroupHistoryRecord {
  id: bigint;
  groupId: bigint;
  message: Buffer;
  epoch: number;
  sender: string;
}

export interface GroupHistoryRecords {
  messages: GroupHistoryRecord[];
  formatVersion: number;
}

export interface ApproveHistoryChunkRequest {
  groupId: bigint;
  chunkId: bigint;
  unencryptedHash: Buffer;
}

/** Secrets travel ONLY in an authenticated MLS hidden message, never in HTTP URLs. */
export interface GroupSnapshotSecret {
  /** 1=pins, 2=recent messages */
  kind: number;
  snapshotId: string;
  key: Buffer;
  plaintextHash: Buffer;
  ciphertextHash: Buffer;
  messageCount: number;
  startId: bigint;
  endId: bigint;
}

export interface GroupSyncBundle {
  formatVersion: number;
  groupId: bigint;
  pinned: GroupSnapshotSecret | undefined;
  recent: GroupSnapshotSecret | undefined;
  keys: GroupHistoryChunkKey[];
  chunks: GroupHistoryChunkItem[];
  bundleId: string;
  page: number;
  lastPage: boolean;
}

/** Account-wide password-encrypted direct chat backups. No password/key on the wire. */
export interface DirectBackupChunk {
  id: string;
  /** Archive ordinal, not a device's local message ID. */
  start: bigint;
  /** 1..1000; only the final chunk may be partial. */
  count: number;
  /** Relative to configured trusted CDN origin. */
  blobPath: string;
  byteSize: bigint;
  ciphertextHash: Buffer;
}

export interface DirectBackupManifest {
  version: number;
  epoch: string;
  /** Argon2id v19: m=65536 KiB, t=3, p=1, output=32 bytes. */
  salt: Buffer;
  /** AES-GCM authenticated format/account/key check. */
  verifier: Buffer;
  /** 7 or 30. */
  scheduleDays: number;
  chunks: DirectBackupChunk[];
  /** AES-GCM sealed SHA-256 of this manifest with authentication cleared. */
  authentication: Buffer;
}

export interface DirectBackupState {
  revision: bigint;
  updatedAt: bigint;
  manifest: DirectBackupManifest | undefined;
}

export interface DirectBackupRequest {
  action: DirectBackupRequest_Action;
  revision: bigint;
  /** Random operation ID, fenced by revision and server expiry. */
  leaseId: string;
  id: string;
  ciphertext: Buffer;
  manifest:
    | DirectBackupManifest
    | undefined;
  /** Reservation metadata, never ciphertext. */
  object: DirectBackupChunk | undefined;
}

export enum DirectBackupRequest_Action {
  invalid = 0,
  lease = 1,
  put = 2,
  commit = 3,
  release = 4,
  UNRECOGNIZED = -1,
}

export function directBackupRequest_ActionFromJSON(object: any): DirectBackupRequest_Action {
  switch (object) {
    case 0:
    case "invalid":
      return DirectBackupRequest_Action.invalid;
    case 1:
    case "lease":
      return DirectBackupRequest_Action.lease;
    case 2:
    case "put":
      return DirectBackupRequest_Action.put;
    case 3:
    case "commit":
      return DirectBackupRequest_Action.commit;
    case 4:
    case "release":
      return DirectBackupRequest_Action.release;
    case -1:
    case "UNRECOGNIZED":
    default:
      return DirectBackupRequest_Action.UNRECOGNIZED;
  }
}

export function directBackupRequest_ActionToJSON(object: DirectBackupRequest_Action): string {
  switch (object) {
    case DirectBackupRequest_Action.invalid:
      return "invalid";
    case DirectBackupRequest_Action.lease:
      return "lease";
    case DirectBackupRequest_Action.put:
      return "put";
    case DirectBackupRequest_Action.commit:
      return "commit";
    case DirectBackupRequest_Action.release:
      return "release";
    case DirectBackupRequest_Action.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

export interface DirectBackupRecord {
  /** Display/import hint only; never dedup identity. */
  sourceId: bigint;
  other: string;
  sentByOther: boolean;
  /** Canonically encoded UserMessageInner, including send nonce. */
  message: Buffer;
  messageType: number;
}

export interface DirectBackupRecords {
  version: number;
  records: DirectBackupRecord[];
}

export interface DirectBackupBinding {
  version: number;
  username: string;
  epoch: string;
  chunkId: string;
  start: bigint;
  count: number;
}

/** ONLY valid inside a decrypted SelfUserMessage from the same account. */
export interface DirectBackupKeySync {
  version: number;
  epoch: string;
  /** 32-byte derived unlock key; absent for requests. */
  key: Buffer;
  requestKey: boolean;
  requestId: string;
}

/** Opaque compressed/encrypted user-to-user pin snapshot capability. */
export interface DirectPinSnapshotSecret {
  version: number;
  other: string;
  snapshotId: string;
  /** 32 bytes; delivered only in E2EE user messages. */
  key: Buffer;
  ciphertextHash: Buffer;
  messageCount: number;
}

function createBaseUserMessage(): UserMessage {
  return {
    id: 0n,
    toId: 0n,
    fromId: 0n,
    text: Buffer.alloc(0),
    type: 0,
    settings: 0,
    hashValue: 0n,
    fromUsername: "",
    fromDeviceId: 0,
  };
}

export const UserMessage: MessageFns<UserMessage> = {
  encode(message: UserMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.id);
    }
    if (message.toId !== 0n) {
      if (BigInt.asUintN(64, message.toId) !== message.toId) {
        throw new globalThis.Error("value provided for field message.toId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.toId);
    }
    if (message.fromId !== 0n) {
      if (BigInt.asUintN(64, message.fromId) !== message.fromId) {
        throw new globalThis.Error("value provided for field message.fromId of type uint64 too large");
      }
      writer.uint32(24).uint64(message.fromId);
    }
    if (message.text.length !== 0) {
      writer.uint32(34).bytes(message.text);
    }
    if (message.type !== 0) {
      writer.uint32(48).uint32(message.type);
    }
    if (message.settings !== 0) {
      writer.uint32(56).uint32(message.settings);
    }
    if (message.hashValue !== 0n) {
      if (BigInt.asUintN(64, message.hashValue) !== message.hashValue) {
        throw new globalThis.Error("value provided for field message.hashValue of type fixed64 too large");
      }
      writer.uint32(81).fixed64(message.hashValue);
    }
    if (message.fromUsername !== "") {
      writer.uint32(66).string(message.fromUsername);
    }
    if (message.fromDeviceId !== 0) {
      writer.uint32(72).uint32(message.fromDeviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUserMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.id = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.toId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.fromId = reader.uint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.text = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.type = reader.uint32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.settings = reader.uint32();
            continue;
          }
          case 10: {
            if (tag !== 81) {
              break;
            }

            message.hashValue = reader.fixed64() as bigint;
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.fromUsername = reader.string();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.fromDeviceId = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UserMessage {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      toId: isSet(object.toId) ? BigInt(object.toId) : 0n,
      fromId: isSet(object.fromId) ? BigInt(object.fromId) : 0n,
      text: isSet(object.text) ? Buffer.from(bytesFromBase64(object.text)) : Buffer.alloc(0),
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      settings: isSet(object.settings) ? globalThis.Number(object.settings) : 0,
      hashValue: isSet(object.hashValue) ? BigInt(object.hashValue) : 0n,
      fromUsername: isSet(object.fromUsername) ? globalThis.String(object.fromUsername) : "",
      fromDeviceId: isSet(object.fromDeviceId) ? globalThis.Number(object.fromDeviceId) : 0,
    };
  },

  toJSON(message: UserMessage): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.toId !== 0n) {
      obj.toId = message.toId.toString();
    }
    if (message.fromId !== 0n) {
      obj.fromId = message.fromId.toString();
    }
    if (message.text.length !== 0) {
      obj.text = base64FromBytes(message.text);
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    if (message.settings !== 0) {
      obj.settings = Math.round(message.settings);
    }
    if (message.hashValue !== 0n) {
      obj.hashValue = message.hashValue.toString();
    }
    if (message.fromUsername !== "") {
      obj.fromUsername = message.fromUsername;
    }
    if (message.fromDeviceId !== 0) {
      obj.fromDeviceId = Math.round(message.fromDeviceId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserMessage>, I>>(base?: I): UserMessage {
    return UserMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserMessage>, I>>(object: I): UserMessage {
    const message = createBaseUserMessage();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.toId = (object.toId !== undefined && object.toId !== null) ? BigInt(object.toId) : 0n;
    message.fromId = (object.fromId !== undefined && object.fromId !== null) ? BigInt(object.fromId) : 0n;
    message.text = object.text ?? Buffer.alloc(0);
    message.type = object.type ?? 0;
    message.settings = object.settings ?? 0;
    message.hashValue = (object.hashValue !== undefined && object.hashValue !== null) ? BigInt(object.hashValue) : 0n;
    message.fromUsername = object.fromUsername ?? "";
    message.fromDeviceId = object.fromDeviceId ?? 0;
    return message;
  },
};

function createBaseGroup(): Group {
  return {
    id: 0n,
    name: "",
    description: "",
    state: Buffer.alloc(0),
    settings: 0,
    upgraded: false,
    pending: false,
    owner: "",
  };
}

export const Group: MessageFns<Group> = {
  encode(message: Group, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.state.length !== 0) {
      writer.uint32(42).bytes(message.state);
    }
    if (message.settings !== 0) {
      writer.uint32(48).uint32(message.settings);
    }
    if (message.upgraded !== false) {
      writer.uint32(56).bool(message.upgraded);
    }
    if (message.pending !== false) {
      writer.uint32(64).bool(message.pending);
    }
    if (message.owner !== "") {
      writer.uint32(74).string(message.owner);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Group {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroup();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.id = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.description = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.state = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.settings = reader.uint32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.upgraded = reader.bool();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.pending = reader.bool();
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.owner = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Group {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      state: isSet(object.state) ? Buffer.from(bytesFromBase64(object.state)) : Buffer.alloc(0),
      settings: isSet(object.settings) ? globalThis.Number(object.settings) : 0,
      upgraded: isSet(object.upgraded) ? globalThis.Boolean(object.upgraded) : false,
      pending: isSet(object.pending) ? globalThis.Boolean(object.pending) : false,
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
    };
  },

  toJSON(message: Group): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.state.length !== 0) {
      obj.state = base64FromBytes(message.state);
    }
    if (message.settings !== 0) {
      obj.settings = Math.round(message.settings);
    }
    if (message.upgraded !== false) {
      obj.upgraded = message.upgraded;
    }
    if (message.pending !== false) {
      obj.pending = message.pending;
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Group>, I>>(base?: I): Group {
    return Group.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Group>, I>>(object: I): Group {
    const message = createBaseGroup();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.state = object.state ?? Buffer.alloc(0);
    message.settings = object.settings ?? 0;
    message.upgraded = object.upgraded ?? false;
    message.pending = object.pending ?? false;
    message.owner = object.owner ?? "";
    return message;
  },
};

function createBaseGroupDetails(): GroupDetails {
  return { id: 0n, name: "", description: "", settings: 0, upgraded: false, owner: "", inviter: "" };
}

export const GroupDetails: MessageFns<GroupDetails> = {
  encode(message: GroupDetails, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.description !== "") {
      writer.uint32(26).string(message.description);
    }
    if (message.settings !== 0) {
      writer.uint32(32).uint32(message.settings);
    }
    if (message.upgraded !== false) {
      writer.uint32(40).bool(message.upgraded);
    }
    if (message.owner !== "") {
      writer.uint32(50).string(message.owner);
    }
    if (message.inviter !== "") {
      writer.uint32(58).string(message.inviter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupDetails {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupDetails();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.description = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.settings = reader.uint32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.upgraded = reader.bool();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.owner = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.inviter = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupDetails {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      description: isSet(object.description) ? globalThis.String(object.description) : "",
      settings: isSet(object.settings) ? globalThis.Number(object.settings) : 0,
      upgraded: isSet(object.upgraded) ? globalThis.Boolean(object.upgraded) : false,
      owner: isSet(object.owner) ? globalThis.String(object.owner) : "",
      inviter: isSet(object.inviter) ? globalThis.String(object.inviter) : "",
    };
  },

  toJSON(message: GroupDetails): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.description !== "") {
      obj.description = message.description;
    }
    if (message.settings !== 0) {
      obj.settings = Math.round(message.settings);
    }
    if (message.upgraded !== false) {
      obj.upgraded = message.upgraded;
    }
    if (message.owner !== "") {
      obj.owner = message.owner;
    }
    if (message.inviter !== "") {
      obj.inviter = message.inviter;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupDetails>, I>>(base?: I): GroupDetails {
    return GroupDetails.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupDetails>, I>>(object: I): GroupDetails {
    const message = createBaseGroupDetails();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.name = object.name ?? "";
    message.description = object.description ?? "";
    message.settings = object.settings ?? 0;
    message.upgraded = object.upgraded ?? false;
    message.owner = object.owner ?? "";
    message.inviter = object.inviter ?? "";
    return message;
  },
};

function createBaseGroups(): Groups {
  return { groups: [] };
}

export const Groups: MessageFns<Groups> = {
  encode(message: Groups, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.groups) {
      Group.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Groups {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroups();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.groups.push(Group.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Groups {
    return { groups: globalThis.Array.isArray(object?.groups) ? object.groups.map((e: any) => Group.fromJSON(e)) : [] };
  },

  toJSON(message: Groups): unknown {
    const obj: any = {};
    if (message.groups?.length) {
      obj.groups = message.groups.map((e) => Group.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Groups>, I>>(base?: I): Groups {
    return Groups.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Groups>, I>>(object: I): Groups {
    const message = createBaseGroups();
    message.groups = object.groups?.map((e) => Group.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUserMessages(): UserMessages {
  return { messages: [] };
}

export const UserMessages: MessageFns<UserMessages> = {
  encode(message: UserMessages, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      UserMessage.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserMessages {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUserMessages();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.messages.push(UserMessage.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UserMessages {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => UserMessage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: UserMessages): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => UserMessage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserMessages>, I>>(base?: I): UserMessages {
    return UserMessages.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserMessages>, I>>(object: I): UserMessages {
    const message = createBaseUserMessages();
    message.messages = object.messages?.map((e) => UserMessage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupInvite(): GroupInvite {
  return { groupId: 0n, inviter: "", invitee: "", welcomeMessage: Buffer.alloc(0), commitId: 0n };
}

export const GroupInvite: MessageFns<GroupInvite> = {
  encode(message: GroupInvite, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.inviter !== "") {
      writer.uint32(18).string(message.inviter);
    }
    if (message.invitee !== "") {
      writer.uint32(26).string(message.invitee);
    }
    if (message.welcomeMessage.length !== 0) {
      writer.uint32(34).bytes(message.welcomeMessage);
    }
    if (message.commitId !== 0n) {
      if (BigInt.asUintN(64, message.commitId) !== message.commitId) {
        throw new globalThis.Error("value provided for field message.commitId of type fixed64 too large");
      }
      writer.uint32(41).fixed64(message.commitId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupInvite {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupInvite();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.inviter = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.invitee = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.welcomeMessage = Buffer.from(reader.bytes());
            continue;
          }
          case 5: {
            if (tag !== 41) {
              break;
            }

            message.commitId = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupInvite {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : 0n,
      inviter: isSet(object.inviter) ? globalThis.String(object.inviter) : "",
      invitee: isSet(object.invitee) ? globalThis.String(object.invitee) : "",
      welcomeMessage: isSet(object.welcomeMessage)
        ? Buffer.from(bytesFromBase64(object.welcomeMessage))
        : Buffer.alloc(0),
      commitId: isSet(object.commitId) ? BigInt(object.commitId) : 0n,
    };
  },

  toJSON(message: GroupInvite): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.inviter !== "") {
      obj.inviter = message.inviter;
    }
    if (message.invitee !== "") {
      obj.invitee = message.invitee;
    }
    if (message.welcomeMessage.length !== 0) {
      obj.welcomeMessage = base64FromBytes(message.welcomeMessage);
    }
    if (message.commitId !== 0n) {
      obj.commitId = message.commitId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupInvite>, I>>(base?: I): GroupInvite {
    return GroupInvite.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupInvite>, I>>(object: I): GroupInvite {
    const message = createBaseGroupInvite();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.inviter = object.inviter ?? "";
    message.invitee = object.invitee ?? "";
    message.welcomeMessage = object.welcomeMessage ?? Buffer.alloc(0);
    message.commitId = (object.commitId !== undefined && object.commitId !== null) ? BigInt(object.commitId) : 0n;
    return message;
  },
};

function createBaseGroupCommitAndWelcome(): GroupCommitAndWelcome {
  return {
    id: 0n,
    groupId: 0n,
    commitMessage: Buffer.alloc(0),
    inviter: "",
    invitee: "",
    welcomeMessages: [],
    inviteeAddresses: [],
  };
}

export const GroupCommitAndWelcome: MessageFns<GroupCommitAndWelcome> = {
  encode(message: GroupCommitAndWelcome, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.id);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.commitMessage.length !== 0) {
      writer.uint32(26).bytes(message.commitMessage);
    }
    if (message.inviter !== "") {
      writer.uint32(34).string(message.inviter);
    }
    if (message.invitee !== "") {
      writer.uint32(42).string(message.invitee);
    }
    for (const v of message.welcomeMessages) {
      writer.uint32(50).bytes(v!);
    }
    writer.uint32(58).fork();
    for (const v of message.inviteeAddresses) {
      if (BigInt.asUintN(64, v) !== v) {
        throw new globalThis.Error("a value provided in array field inviteeAddresses of type uint64 is too large");
      }
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupCommitAndWelcome {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupCommitAndWelcome();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.id = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.commitMessage = Buffer.from(reader.bytes());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.inviter = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.invitee = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.welcomeMessages.push(Buffer.from(reader.bytes()));
            continue;
          }
          case 7: {
            if (tag === 56) {
              message.inviteeAddresses.push(reader.uint64() as bigint);

              continue;
            }

            if (tag === 58) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.inviteeAddresses.push(reader.uint64() as bigint);
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupCommitAndWelcome {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : 0n,
      commitMessage: isSet(object.commitMessage) ? Buffer.from(bytesFromBase64(object.commitMessage)) : Buffer.alloc(0),
      inviter: isSet(object.inviter) ? globalThis.String(object.inviter) : "",
      invitee: isSet(object.invitee) ? globalThis.String(object.invitee) : "",
      welcomeMessages: globalThis.Array.isArray(object?.welcomeMessages)
        ? object.welcomeMessages.map((e: any) => Buffer.from(bytesFromBase64(e)))
        : [],
      inviteeAddresses: globalThis.Array.isArray(object?.inviteeAddresses)
        ? object.inviteeAddresses.map((e: any) => BigInt(e))
        : [],
    };
  },

  toJSON(message: GroupCommitAndWelcome): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.commitMessage.length !== 0) {
      obj.commitMessage = base64FromBytes(message.commitMessage);
    }
    if (message.inviter !== "") {
      obj.inviter = message.inviter;
    }
    if (message.invitee !== "") {
      obj.invitee = message.invitee;
    }
    if (message.welcomeMessages?.length) {
      obj.welcomeMessages = message.welcomeMessages.map((e) => base64FromBytes(e));
    }
    if (message.inviteeAddresses?.length) {
      obj.inviteeAddresses = message.inviteeAddresses.map((e) => e.toString());
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupCommitAndWelcome>, I>>(base?: I): GroupCommitAndWelcome {
    return GroupCommitAndWelcome.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupCommitAndWelcome>, I>>(object: I): GroupCommitAndWelcome {
    const message = createBaseGroupCommitAndWelcome();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.commitMessage = object.commitMessage ?? Buffer.alloc(0);
    message.inviter = object.inviter ?? "";
    message.invitee = object.invitee ?? "";
    message.welcomeMessages = object.welcomeMessages?.map((e) => e) || [];
    message.inviteeAddresses = object.inviteeAddresses?.map((e) => BigInt(e)) || [];
    return message;
  },
};

function createBaseGroupInvites(): GroupInvites {
  return { invites: [] };
}

export const GroupInvites: MessageFns<GroupInvites> = {
  encode(message: GroupInvites, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.invites) {
      GroupInvite.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupInvites {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupInvites();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.invites.push(GroupInvite.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupInvites {
    return {
      invites: globalThis.Array.isArray(object?.invites) ? object.invites.map((e: any) => GroupInvite.fromJSON(e)) : [],
    };
  },

  toJSON(message: GroupInvites): unknown {
    const obj: any = {};
    if (message.invites?.length) {
      obj.invites = message.invites.map((e) => GroupInvite.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupInvites>, I>>(base?: I): GroupInvites {
    return GroupInvites.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupInvites>, I>>(object: I): GroupInvites {
    const message = createBaseGroupInvites();
    message.invites = object.invites?.map((e) => GroupInvite.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupMessage(): GroupMessage {
  return { id: 0n, groupId: 0n, message: Buffer.alloc(0), epoch: 0 };
}

export const GroupMessage: MessageFns<GroupMessage> = {
  encode(message: GroupMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.id);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.message.length !== 0) {
      writer.uint32(26).bytes(message.message);
    }
    if (message.epoch !== 0) {
      writer.uint32(32).uint32(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.id = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.message = Buffer.from(reader.bytes());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.epoch = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMessage {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : 0n,
      message: isSet(object.message) ? Buffer.from(bytesFromBase64(object.message)) : Buffer.alloc(0),
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
    };
  },

  toJSON(message: GroupMessage): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.message.length !== 0) {
      obj.message = base64FromBytes(message.message);
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMessage>, I>>(base?: I): GroupMessage {
    return GroupMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMessage>, I>>(object: I): GroupMessage {
    const message = createBaseGroupMessage();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.message = object.message ?? Buffer.alloc(0);
    message.epoch = object.epoch ?? 0;
    return message;
  },
};

function createBaseGroupKeyPackage(): GroupKeyPackage {
  return { address: 0n, package: Buffer.alloc(0), username: "", id: 0 };
}

export const GroupKeyPackage: MessageFns<GroupKeyPackage> = {
  encode(message: GroupKeyPackage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== 0n) {
      if (BigInt.asUintN(64, message.address) !== message.address) {
        throw new globalThis.Error("value provided for field message.address of type uint64 too large");
      }
      writer.uint32(24).uint64(message.address);
    }
    if (message.package.length !== 0) {
      writer.uint32(18).bytes(message.package);
    }
    if (message.username !== "") {
      writer.uint32(34).string(message.username);
    }
    if (message.id !== 0) {
      writer.uint32(8).int32(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupKeyPackage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupKeyPackage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.address = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.package = Buffer.from(reader.bytes());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.int32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupKeyPackage {
    return {
      address: isSet(object.address) ? BigInt(object.address) : 0n,
      package: isSet(object.package) ? Buffer.from(bytesFromBase64(object.package)) : Buffer.alloc(0),
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
    };
  },

  toJSON(message: GroupKeyPackage): unknown {
    const obj: any = {};
    if (message.address !== 0n) {
      obj.address = message.address.toString();
    }
    if (message.package.length !== 0) {
      obj.package = base64FromBytes(message.package);
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupKeyPackage>, I>>(base?: I): GroupKeyPackage {
    return GroupKeyPackage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupKeyPackage>, I>>(object: I): GroupKeyPackage {
    const message = createBaseGroupKeyPackage();
    message.address = (object.address !== undefined && object.address !== null) ? BigInt(object.address) : 0n;
    message.package = object.package ?? Buffer.alloc(0);
    message.username = object.username ?? "";
    message.id = object.id ?? 0;
    return message;
  },
};

function createBaseGroupKeyPackages(): GroupKeyPackages {
  return { packages: [] };
}

export const GroupKeyPackages: MessageFns<GroupKeyPackages> = {
  encode(message: GroupKeyPackages, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.packages) {
      GroupKeyPackage.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupKeyPackages {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupKeyPackages();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.packages.push(GroupKeyPackage.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupKeyPackages {
    return {
      packages: globalThis.Array.isArray(object?.packages)
        ? object.packages.map((e: any) => GroupKeyPackage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupKeyPackages): unknown {
    const obj: any = {};
    if (message.packages?.length) {
      obj.packages = message.packages.map((e) => GroupKeyPackage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupKeyPackages>, I>>(base?: I): GroupKeyPackages {
    return GroupKeyPackages.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupKeyPackages>, I>>(object: I): GroupKeyPackages {
    const message = createBaseGroupKeyPackages();
    message.packages = object.packages?.map((e) => GroupKeyPackage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupMessages(): GroupMessages {
  return { messages: [] };
}

export const GroupMessages: MessageFns<GroupMessages> = {
  encode(message: GroupMessages, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      GroupMessage.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMessages {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMessages();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.messages.push(GroupMessage.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMessages {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => GroupMessage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupMessages): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => GroupMessage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMessages>, I>>(base?: I): GroupMessages {
    return GroupMessages.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMessages>, I>>(object: I): GroupMessages {
    const message = createBaseGroupMessages();
    message.messages = object.messages?.map((e) => GroupMessage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupSyncRequest(): GroupSyncRequest {
  return { groupId: 0n, startAfter: 0n, until: 0n, limit: 0 };
}

export const GroupSyncRequest: MessageFns<GroupSyncRequest> = {
  encode(message: GroupSyncRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.startAfter !== 0n) {
      if (BigInt.asUintN(64, message.startAfter) !== message.startAfter) {
        throw new globalThis.Error("value provided for field message.startAfter of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.startAfter);
    }
    if (message.until !== 0n) {
      if (BigInt.asUintN(64, message.until) !== message.until) {
        throw new globalThis.Error("value provided for field message.until of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.until);
    }
    if (message.limit !== 0) {
      writer.uint32(32).uint32(message.limit);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupSyncRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupSyncRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.startAfter = reader.fixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.until = reader.fixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.limit = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupSyncRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      startAfter: isSet(object.startAfter)
        ? BigInt(object.startAfter)
        : isSet(object.start_after)
        ? BigInt(object.start_after)
        : 0n,
      until: isSet(object.until) ? BigInt(object.until) : 0n,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : 0,
    };
  },

  toJSON(message: GroupSyncRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.startAfter !== 0n) {
      obj.startAfter = message.startAfter.toString();
    }
    if (message.until !== 0n) {
      obj.until = message.until.toString();
    }
    if (message.limit !== 0) {
      obj.limit = Math.round(message.limit);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupSyncRequest>, I>>(base?: I): GroupSyncRequest {
    return GroupSyncRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupSyncRequest>, I>>(object: I): GroupSyncRequest {
    const message = createBaseGroupSyncRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.startAfter = (object.startAfter !== undefined && object.startAfter !== null)
      ? BigInt(object.startAfter)
      : 0n;
    message.until = (object.until !== undefined && object.until !== null) ? BigInt(object.until) : 0n;
    message.limit = object.limit ?? 0;
    return message;
  },
};

function createBaseGroupSyncRequests(): GroupSyncRequests {
  return { requests: [] };
}

export const GroupSyncRequests: MessageFns<GroupSyncRequests> = {
  encode(message: GroupSyncRequests, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.requests) {
      GroupSyncRequest.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupSyncRequests {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupSyncRequests();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requests.push(GroupSyncRequest.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupSyncRequests {
    return {
      requests: globalThis.Array.isArray(object?.requests)
        ? object.requests.map((e: any) => GroupSyncRequest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupSyncRequests): unknown {
    const obj: any = {};
    if (message.requests?.length) {
      obj.requests = message.requests.map((e) => GroupSyncRequest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupSyncRequests>, I>>(base?: I): GroupSyncRequests {
    return GroupSyncRequests.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupSyncRequests>, I>>(object: I): GroupSyncRequests {
    const message = createBaseGroupSyncRequests();
    message.requests = object.requests?.map((e) => GroupSyncRequest.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupMemberUpdate(): GroupMemberUpdate {
  return { groupId: 0n, lastMessageSeen: 0n, lastEpoch: 0 };
}

export const GroupMemberUpdate: MessageFns<GroupMemberUpdate> = {
  encode(message: GroupMemberUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.lastMessageSeen !== 0n) {
      if (BigInt.asUintN(64, message.lastMessageSeen) !== message.lastMessageSeen) {
        throw new globalThis.Error("value provided for field message.lastMessageSeen of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.lastMessageSeen);
    }
    if (message.lastEpoch !== 0) {
      writer.uint32(24).uint32(message.lastEpoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMemberUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMemberUpdate();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.lastMessageSeen = reader.fixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.lastEpoch = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMemberUpdate {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      lastMessageSeen: isSet(object.lastMessageSeen)
        ? BigInt(object.lastMessageSeen)
        : isSet(object.last_message_seen)
        ? BigInt(object.last_message_seen)
        : 0n,
      lastEpoch: isSet(object.lastEpoch)
        ? globalThis.Number(object.lastEpoch)
        : isSet(object.last_epoch)
        ? globalThis.Number(object.last_epoch)
        : 0,
    };
  },

  toJSON(message: GroupMemberUpdate): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.lastMessageSeen !== 0n) {
      obj.lastMessageSeen = message.lastMessageSeen.toString();
    }
    if (message.lastEpoch !== 0) {
      obj.lastEpoch = Math.round(message.lastEpoch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMemberUpdate>, I>>(base?: I): GroupMemberUpdate {
    return GroupMemberUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMemberUpdate>, I>>(object: I): GroupMemberUpdate {
    const message = createBaseGroupMemberUpdate();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.lastMessageSeen = (object.lastMessageSeen !== undefined && object.lastMessageSeen !== null)
      ? BigInt(object.lastMessageSeen)
      : 0n;
    message.lastEpoch = object.lastEpoch ?? 0;
    return message;
  },
};

function createBaseGroupMemberUpdates(): GroupMemberUpdates {
  return { updates: [] };
}

export const GroupMemberUpdates: MessageFns<GroupMemberUpdates> = {
  encode(message: GroupMemberUpdates, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.updates) {
      GroupMemberUpdate.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMemberUpdates {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMemberUpdates();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.updates.push(GroupMemberUpdate.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMemberUpdates {
    return {
      updates: globalThis.Array.isArray(object?.updates)
        ? object.updates.map((e: any) => GroupMemberUpdate.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupMemberUpdates): unknown {
    const obj: any = {};
    if (message.updates?.length) {
      obj.updates = message.updates.map((e) => GroupMemberUpdate.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMemberUpdates>, I>>(base?: I): GroupMemberUpdates {
    return GroupMemberUpdates.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMemberUpdates>, I>>(object: I): GroupMemberUpdates {
    const message = createBaseGroupMemberUpdates();
    message.updates = object.updates?.map((e) => GroupMemberUpdate.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupCommit(): GroupCommit {
  return { id: 0n, groupId: 0n, commit: Buffer.alloc(0), epoch: 0 };
}

export const GroupCommit: MessageFns<GroupCommit> = {
  encode(message: GroupCommit, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.id);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.commit.length !== 0) {
      writer.uint32(34).bytes(message.commit);
    }
    if (message.epoch !== 0) {
      writer.uint32(24).uint32(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupCommit {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupCommit();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.id = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.commit = Buffer.from(reader.bytes());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.epoch = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupCommit {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : 0n,
      commit: isSet(object.commit) ? Buffer.from(bytesFromBase64(object.commit)) : Buffer.alloc(0),
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
    };
  },

  toJSON(message: GroupCommit): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.commit.length !== 0) {
      obj.commit = base64FromBytes(message.commit);
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupCommit>, I>>(base?: I): GroupCommit {
    return GroupCommit.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupCommit>, I>>(object: I): GroupCommit {
    const message = createBaseGroupCommit();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.commit = object.commit ?? Buffer.alloc(0);
    message.epoch = object.epoch ?? 0;
    return message;
  },
};

function createBaseGroupCommits(): GroupCommits {
  return { commits: [] };
}

export const GroupCommits: MessageFns<GroupCommits> = {
  encode(message: GroupCommits, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.commits) {
      GroupCommit.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupCommits {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupCommits();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.commits.push(GroupCommit.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupCommits {
    return {
      commits: globalThis.Array.isArray(object?.commits) ? object.commits.map((e: any) => GroupCommit.fromJSON(e)) : [],
    };
  },

  toJSON(message: GroupCommits): unknown {
    const obj: any = {};
    if (message.commits?.length) {
      obj.commits = message.commits.map((e) => GroupCommit.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupCommits>, I>>(base?: I): GroupCommits {
    return GroupCommits.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupCommits>, I>>(object: I): GroupCommits {
    const message = createBaseGroupCommits();
    message.commits = object.commits?.map((e) => GroupCommit.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupCommitSyncRequest(): GroupCommitSyncRequest {
  return { groupId: 0n, epoch: 0 };
}

export const GroupCommitSyncRequest: MessageFns<GroupCommitSyncRequest> = {
  encode(message: GroupCommitSyncRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.epoch !== 0) {
      writer.uint32(16).uint32(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupCommitSyncRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupCommitSyncRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.epoch = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupCommitSyncRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
    };
  },

  toJSON(message: GroupCommitSyncRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupCommitSyncRequest>, I>>(base?: I): GroupCommitSyncRequest {
    return GroupCommitSyncRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupCommitSyncRequest>, I>>(object: I): GroupCommitSyncRequest {
    const message = createBaseGroupCommitSyncRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.epoch = object.epoch ?? 0;
    return message;
  },
};

function createBaseGroupReAddRequest(): GroupReAddRequest {
  return { groupId: 0n, addressId: 0n, username: "" };
}

export const GroupReAddRequest: MessageFns<GroupReAddRequest> = {
  encode(message: GroupReAddRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.addressId !== 0n) {
      if (BigInt.asUintN(64, message.addressId) !== message.addressId) {
        throw new globalThis.Error("value provided for field message.addressId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.addressId);
    }
    if (message.username !== "") {
      writer.uint32(26).string(message.username);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupReAddRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupReAddRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.addressId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.username = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupReAddRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      addressId: isSet(object.addressId)
        ? BigInt(object.addressId)
        : isSet(object.address_id)
        ? BigInt(object.address_id)
        : 0n,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
    };
  },

  toJSON(message: GroupReAddRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.addressId !== 0n) {
      obj.addressId = message.addressId.toString();
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupReAddRequest>, I>>(base?: I): GroupReAddRequest {
    return GroupReAddRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupReAddRequest>, I>>(object: I): GroupReAddRequest {
    const message = createBaseGroupReAddRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.addressId = (object.addressId !== undefined && object.addressId !== null) ? BigInt(object.addressId) : 0n;
    message.username = object.username ?? "";
    return message;
  },
};

function createBaseGroupReAddRequests(): GroupReAddRequests {
  return { requests: [] };
}

export const GroupReAddRequests: MessageFns<GroupReAddRequests> = {
  encode(message: GroupReAddRequests, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.requests) {
      GroupReAddRequest.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupReAddRequests {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupReAddRequests();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requests.push(GroupReAddRequest.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupReAddRequests {
    return {
      requests: globalThis.Array.isArray(object?.requests)
        ? object.requests.map((e: any) => GroupReAddRequest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupReAddRequests): unknown {
    const obj: any = {};
    if (message.requests?.length) {
      obj.requests = message.requests.map((e) => GroupReAddRequest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupReAddRequests>, I>>(base?: I): GroupReAddRequests {
    return GroupReAddRequests.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupReAddRequests>, I>>(object: I): GroupReAddRequests {
    const message = createBaseGroupReAddRequests();
    message.requests = object.requests?.map((e) => GroupReAddRequest.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupMemberOnlineStatus(): GroupMemberOnlineStatus {
  return { addressId: 0n, username: "", deviceId: 0, lastConnectedAt: 0n, isOnline: false };
}

export const GroupMemberOnlineStatus: MessageFns<GroupMemberOnlineStatus> = {
  encode(message: GroupMemberOnlineStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.addressId !== 0n) {
      if (BigInt.asUintN(64, message.addressId) !== message.addressId) {
        throw new globalThis.Error("value provided for field message.addressId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.addressId);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.deviceId !== 0) {
      writer.uint32(24).uint32(message.deviceId);
    }
    if (message.lastConnectedAt !== 0n) {
      if (BigInt.asUintN(64, message.lastConnectedAt) !== message.lastConnectedAt) {
        throw new globalThis.Error("value provided for field message.lastConnectedAt of type uint64 too large");
      }
      writer.uint32(32).uint64(message.lastConnectedAt);
    }
    if (message.isOnline !== false) {
      writer.uint32(40).bool(message.isOnline);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMemberOnlineStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMemberOnlineStatus();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.addressId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.deviceId = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.lastConnectedAt = reader.uint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.isOnline = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMemberOnlineStatus {
    return {
      addressId: isSet(object.addressId)
        ? BigInt(object.addressId)
        : isSet(object.address_id)
        ? BigInt(object.address_id)
        : 0n,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      deviceId: isSet(object.deviceId)
        ? globalThis.Number(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.Number(object.device_id)
        : 0,
      lastConnectedAt: isSet(object.lastConnectedAt)
        ? BigInt(object.lastConnectedAt)
        : isSet(object.last_connected_at)
        ? BigInt(object.last_connected_at)
        : 0n,
      isOnline: isSet(object.isOnline)
        ? globalThis.Boolean(object.isOnline)
        : isSet(object.is_online)
        ? globalThis.Boolean(object.is_online)
        : false,
    };
  },

  toJSON(message: GroupMemberOnlineStatus): unknown {
    const obj: any = {};
    if (message.addressId !== 0n) {
      obj.addressId = message.addressId.toString();
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    if (message.lastConnectedAt !== 0n) {
      obj.lastConnectedAt = message.lastConnectedAt.toString();
    }
    if (message.isOnline !== false) {
      obj.isOnline = message.isOnline;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMemberOnlineStatus>, I>>(base?: I): GroupMemberOnlineStatus {
    return GroupMemberOnlineStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMemberOnlineStatus>, I>>(object: I): GroupMemberOnlineStatus {
    const message = createBaseGroupMemberOnlineStatus();
    message.addressId = (object.addressId !== undefined && object.addressId !== null) ? BigInt(object.addressId) : 0n;
    message.username = object.username ?? "";
    message.deviceId = object.deviceId ?? 0;
    message.lastConnectedAt = (object.lastConnectedAt !== undefined && object.lastConnectedAt !== null)
      ? BigInt(object.lastConnectedAt)
      : 0n;
    message.isOnline = object.isOnline ?? false;
    return message;
  },
};

function createBaseGroupMembersOnlineStatus(): GroupMembersOnlineStatus {
  return { members: [] };
}

export const GroupMembersOnlineStatus: MessageFns<GroupMembersOnlineStatus> = {
  encode(message: GroupMembersOnlineStatus, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.members) {
      GroupMemberOnlineStatus.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMembersOnlineStatus {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMembersOnlineStatus();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.members.push(GroupMemberOnlineStatus.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMembersOnlineStatus {
    return {
      members: globalThis.Array.isArray(object?.members)
        ? object.members.map((e: any) => GroupMemberOnlineStatus.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupMembersOnlineStatus): unknown {
    const obj: any = {};
    if (message.members?.length) {
      obj.members = message.members.map((e) => GroupMemberOnlineStatus.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMembersOnlineStatus>, I>>(base?: I): GroupMembersOnlineStatus {
    return GroupMembersOnlineStatus.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMembersOnlineStatus>, I>>(object: I): GroupMembersOnlineStatus {
    const message = createBaseGroupMembersOnlineStatus();
    message.members = object.members?.map((e) => GroupMemberOnlineStatus.fromPartial(e)) || [];
    return message;
  },
};

function createBaseError(): Error {
  return { error: "", errorCode: 0 };
}

export const Error: MessageFns<Error> = {
  encode(message: Error, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.error !== "") {
      writer.uint32(18).string(message.error);
    }
    if (message.errorCode !== 0) {
      writer.uint32(8).uint32(message.errorCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Error {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseError();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.error = reader.string();
            continue;
          }
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.errorCode = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Error {
    return {
      error: isSet(object.error) ? globalThis.String(object.error) : "",
      errorCode: isSet(object.errorCode) ? globalThis.Number(object.errorCode) : 0,
    };
  },

  toJSON(message: Error): unknown {
    const obj: any = {};
    if (message.error !== "") {
      obj.error = message.error;
    }
    if (message.errorCode !== 0) {
      obj.errorCode = Math.round(message.errorCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Error>, I>>(base?: I): Error {
    return Error.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Error>, I>>(object: I): Error {
    const message = createBaseError();
    message.error = object.error ?? "";
    message.errorCode = object.errorCode ?? 0;
    return message;
  },
};

function createBaseResult(): Result {
  return { body: Buffer.alloc(0), resultCode: 0 };
}

export const Result: MessageFns<Result> = {
  encode(message: Result, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.body.length !== 0) {
      writer.uint32(18).bytes(message.body);
    }
    if (message.resultCode !== 0) {
      writer.uint32(8).uint32(message.resultCode);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Result {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.body = Buffer.from(reader.bytes());
            continue;
          }
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.resultCode = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Result {
    return {
      body: isSet(object.body) ? Buffer.from(bytesFromBase64(object.body)) : Buffer.alloc(0),
      resultCode: isSet(object.resultCode) ? globalThis.Number(object.resultCode) : 0,
    };
  },

  toJSON(message: Result): unknown {
    const obj: any = {};
    if (message.body.length !== 0) {
      obj.body = base64FromBytes(message.body);
    }
    if (message.resultCode !== 0) {
      obj.resultCode = Math.round(message.resultCode);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Result>, I>>(base?: I): Result {
    return Result.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Result>, I>>(object: I): Result {
    const message = createBaseResult();
    message.body = object.body ?? Buffer.alloc(0);
    message.resultCode = object.resultCode ?? 0;
    return message;
  },
};

function createBaseAddress(): Address {
  return { id: 0n, username: "", fcmToken: "", deviceId: 0 };
}

export const Address: MessageFns<Address> = {
  encode(message: Address, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.fcmToken !== "") {
      writer.uint32(34).string(message.fcmToken);
    }
    if (message.deviceId !== 0) {
      writer.uint32(24).uint32(message.deviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Address {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAddress();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.fcmToken = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.deviceId = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Address {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      fcmToken: isSet(object.fcmToken) ? globalThis.String(object.fcmToken) : "",
      deviceId: isSet(object.deviceId) ? globalThis.Number(object.deviceId) : 0,
    };
  },

  toJSON(message: Address): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.fcmToken !== "") {
      obj.fcmToken = message.fcmToken;
    }
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Address>, I>>(base?: I): Address {
    return Address.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Address>, I>>(object: I): Address {
    const message = createBaseAddress();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.username = object.username ?? "";
    message.fcmToken = object.fcmToken ?? "";
    message.deviceId = object.deviceId ?? 0;
    return message;
  },
};

function createBaseAddresses(): Addresses {
  return { addresses: [] };
}

export const Addresses: MessageFns<Addresses> = {
  encode(message: Addresses, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.addresses) {
      Address.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Addresses {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAddresses();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.addresses.push(Address.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Addresses {
    return {
      addresses: globalThis.Array.isArray(object?.addresses)
        ? object.addresses.map((e: any) => Address.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Addresses): unknown {
    const obj: any = {};
    if (message.addresses?.length) {
      obj.addresses = message.addresses.map((e) => Address.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Addresses>, I>>(base?: I): Addresses {
    return Addresses.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Addresses>, I>>(object: I): Addresses {
    const message = createBaseAddresses();
    message.addresses = object.addresses?.map((e) => Address.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUploadUserMessage(): UploadUserMessage {
  return { messages: [] };
}

export const UploadUserMessage: MessageFns<UploadUserMessage> = {
  encode(message: UploadUserMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      UserMessage.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UploadUserMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUploadUserMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.messages.push(UserMessage.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UploadUserMessage {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => UserMessage.fromJSON(e))
        : [],
    };
  },

  toJSON(message: UploadUserMessage): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => UserMessage.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UploadUserMessage>, I>>(base?: I): UploadUserMessage {
    return UploadUserMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UploadUserMessage>, I>>(object: I): UploadUserMessage {
    const message = createBaseUploadUserMessage();
    message.messages = object.messages?.map((e) => UserMessage.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMessageIdAndTo(): MessageIdAndTo {
  return { id: 0n, to: 0n, isSelf: false };
}

export const MessageIdAndTo: MessageFns<MessageIdAndTo> = {
  encode(message: MessageIdAndTo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.id);
    }
    if (message.to !== 0n) {
      if (BigInt.asUintN(64, message.to) !== message.to) {
        throw new globalThis.Error("value provided for field message.to of type uint64 too large");
      }
      writer.uint32(16).uint64(message.to);
    }
    if (message.isSelf !== false) {
      writer.uint32(24).bool(message.isSelf);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessageIdAndTo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMessageIdAndTo();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.id = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.to = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.isSelf = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MessageIdAndTo {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      to: isSet(object.to) ? BigInt(object.to) : 0n,
      isSelf: isSet(object.isSelf)
        ? globalThis.Boolean(object.isSelf)
        : isSet(object.is_self)
        ? globalThis.Boolean(object.is_self)
        : false,
    };
  },

  toJSON(message: MessageIdAndTo): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.to !== 0n) {
      obj.to = message.to.toString();
    }
    if (message.isSelf !== false) {
      obj.isSelf = message.isSelf;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessageIdAndTo>, I>>(base?: I): MessageIdAndTo {
    return MessageIdAndTo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessageIdAndTo>, I>>(object: I): MessageIdAndTo {
    const message = createBaseMessageIdAndTo();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.to = (object.to !== undefined && object.to !== null) ? BigInt(object.to) : 0n;
    message.isSelf = object.isSelf ?? false;
    return message;
  },
};

function createBaseUserMessageUploaded(): UserMessageUploaded {
  return { messageIds: [] };
}

export const UserMessageUploaded: MessageFns<UserMessageUploaded> = {
  encode(message: UserMessageUploaded, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messageIds) {
      MessageIdAndTo.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserMessageUploaded {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUserMessageUploaded();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.messageIds.push(MessageIdAndTo.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UserMessageUploaded {
    return {
      messageIds: globalThis.Array.isArray(object?.messageIds)
        ? object.messageIds.map((e: any) => MessageIdAndTo.fromJSON(e))
        : [],
    };
  },

  toJSON(message: UserMessageUploaded): unknown {
    const obj: any = {};
    if (message.messageIds?.length) {
      obj.messageIds = message.messageIds.map((e) => MessageIdAndTo.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserMessageUploaded>, I>>(base?: I): UserMessageUploaded {
    return UserMessageUploaded.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserMessageUploaded>, I>>(object: I): UserMessageUploaded {
    const message = createBaseUserMessageUploaded();
    message.messageIds = object.messageIds?.map((e) => MessageIdAndTo.fromPartial(e)) || [];
    return message;
  },
};

function createBaseUserOnlineStatusRequest(): UserOnlineStatusRequest {
  return { usernames: [] };
}

export const UserOnlineStatusRequest: MessageFns<UserOnlineStatusRequest> = {
  encode(message: UserOnlineStatusRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.usernames) {
      writer.uint32(10).string(v!);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserOnlineStatusRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUserOnlineStatusRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.usernames.push(reader.string());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UserOnlineStatusRequest {
    return {
      usernames: globalThis.Array.isArray(object?.usernames)
        ? object.usernames.map((e: any) => globalThis.String(e))
        : [],
    };
  },

  toJSON(message: UserOnlineStatusRequest): unknown {
    const obj: any = {};
    if (message.usernames?.length) {
      obj.usernames = message.usernames;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserOnlineStatusRequest>, I>>(base?: I): UserOnlineStatusRequest {
    return UserOnlineStatusRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserOnlineStatusRequest>, I>>(object: I): UserOnlineStatusRequest {
    const message = createBaseUserOnlineStatusRequest();
    message.usernames = object.usernames?.map((e) => e) || [];
    return message;
  },
};

function createBaseUserOnlineStatusResponse(): UserOnlineStatusResponse {
  return { onlineBits: 0 };
}

export const UserOnlineStatusResponse: MessageFns<UserOnlineStatusResponse> = {
  encode(message: UserOnlineStatusResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.onlineBits !== 0) {
      writer.uint32(13).fixed32(message.onlineBits);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserOnlineStatusResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUserOnlineStatusResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 13) {
              break;
            }

            message.onlineBits = reader.fixed32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UserOnlineStatusResponse {
    return {
      onlineBits: isSet(object.onlineBits)
        ? globalThis.Number(object.onlineBits)
        : isSet(object.online_bits)
        ? globalThis.Number(object.online_bits)
        : 0,
    };
  },

  toJSON(message: UserOnlineStatusResponse): unknown {
    const obj: any = {};
    if (message.onlineBits !== 0) {
      obj.onlineBits = Math.round(message.onlineBits);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserOnlineStatusResponse>, I>>(base?: I): UserOnlineStatusResponse {
    return UserOnlineStatusResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserOnlineStatusResponse>, I>>(object: I): UserOnlineStatusResponse {
    const message = createBaseUserOnlineStatusResponse();
    message.onlineBits = object.onlineBits ?? 0;
    return message;
  },
};

function createBaseRequest(): Request {
  return {
    id: 0,
    createUserMessage: undefined,
    uploadUserMessage: undefined,
    uploadGroupMessage: undefined,
    requestGroupReAdds: undefined,
    requestGroupSync: undefined,
    userOnlineStatus: undefined,
    createJoinLink: undefined,
    joinViaLink: undefined,
    createMeeting: undefined,
    joinMeeting: undefined,
    leaveMeeting: undefined,
    endMeeting: undefined,
    getActiveSession: undefined,
    createHistoryRequest: undefined,
    claimHistoryRequest: undefined,
    publishHistoryChunk: undefined,
    disapproveHistoryChunk: undefined,
    getHistoryChunks: undefined,
    getPendingHistoryRequests: undefined,
    closeHistoryRequest: undefined,
    approveHistoryChunk: undefined,
  };
}

export const Request: MessageFns<Request> = {
  encode(message: Request, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.createUserMessage !== undefined) {
      UserMessage.encode(message.createUserMessage, writer.uint32(18).fork()).join();
    }
    if (message.uploadUserMessage !== undefined) {
      UploadUserMessage.encode(message.uploadUserMessage, writer.uint32(26).fork()).join();
    }
    if (message.uploadGroupMessage !== undefined) {
      GroupMessage.encode(message.uploadGroupMessage, writer.uint32(34).fork()).join();
    }
    if (message.requestGroupReAdds !== undefined) {
      RequestGroupReAdds.encode(message.requestGroupReAdds, writer.uint32(42).fork()).join();
    }
    if (message.requestGroupSync !== undefined) {
      RequestGroupSync.encode(message.requestGroupSync, writer.uint32(50).fork()).join();
    }
    if (message.userOnlineStatus !== undefined) {
      UserOnlineStatusRequest.encode(message.userOnlineStatus, writer.uint32(58).fork()).join();
    }
    if (message.createJoinLink !== undefined) {
      CreateJoinLinkRequest.encode(message.createJoinLink, writer.uint32(66).fork()).join();
    }
    if (message.joinViaLink !== undefined) {
      JoinViaLinkRequest.encode(message.joinViaLink, writer.uint32(74).fork()).join();
    }
    if (message.createMeeting !== undefined) {
      CreateMeetingRequest.encode(message.createMeeting, writer.uint32(82).fork()).join();
    }
    if (message.joinMeeting !== undefined) {
      JoinMeetingRequest.encode(message.joinMeeting, writer.uint32(90).fork()).join();
    }
    if (message.leaveMeeting !== undefined) {
      LeaveMeetingRequest.encode(message.leaveMeeting, writer.uint32(98).fork()).join();
    }
    if (message.endMeeting !== undefined) {
      EndMeetingRequest.encode(message.endMeeting, writer.uint32(106).fork()).join();
    }
    if (message.getActiveSession !== undefined) {
      GetActiveSessionRequest.encode(message.getActiveSession, writer.uint32(114).fork()).join();
    }
    if (message.createHistoryRequest !== undefined) {
      CreateHistoryRequest.encode(message.createHistoryRequest, writer.uint32(122).fork()).join();
    }
    if (message.claimHistoryRequest !== undefined) {
      ClaimHistoryRequest.encode(message.claimHistoryRequest, writer.uint32(130).fork()).join();
    }
    if (message.publishHistoryChunk !== undefined) {
      PublishHistoryChunkRequest.encode(message.publishHistoryChunk, writer.uint32(138).fork()).join();
    }
    if (message.disapproveHistoryChunk !== undefined) {
      DisapproveHistoryChunkRequest.encode(message.disapproveHistoryChunk, writer.uint32(146).fork()).join();
    }
    if (message.getHistoryChunks !== undefined) {
      GetHistoryChunksRequest.encode(message.getHistoryChunks, writer.uint32(154).fork()).join();
    }
    if (message.getPendingHistoryRequests !== undefined) {
      GetPendingHistoryRequests.encode(message.getPendingHistoryRequests, writer.uint32(162).fork()).join();
    }
    if (message.closeHistoryRequest !== undefined) {
      CloseHistoryRequest.encode(message.closeHistoryRequest, writer.uint32(170).fork()).join();
    }
    if (message.approveHistoryChunk !== undefined) {
      ApproveHistoryChunkRequest.encode(message.approveHistoryChunk, writer.uint32(178).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Request {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.createUserMessage = UserMessage.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.uploadUserMessage = UploadUserMessage.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.uploadGroupMessage = GroupMessage.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.requestGroupReAdds = RequestGroupReAdds.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.requestGroupSync = RequestGroupSync.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.userOnlineStatus = UserOnlineStatusRequest.decode(reader, reader.uint32());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.createJoinLink = CreateJoinLinkRequest.decode(reader, reader.uint32());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.joinViaLink = JoinViaLinkRequest.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.createMeeting = CreateMeetingRequest.decode(reader, reader.uint32());
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.joinMeeting = JoinMeetingRequest.decode(reader, reader.uint32());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.leaveMeeting = LeaveMeetingRequest.decode(reader, reader.uint32());
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.endMeeting = EndMeetingRequest.decode(reader, reader.uint32());
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.getActiveSession = GetActiveSessionRequest.decode(reader, reader.uint32());
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.createHistoryRequest = CreateHistoryRequest.decode(reader, reader.uint32());
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.claimHistoryRequest = ClaimHistoryRequest.decode(reader, reader.uint32());
            continue;
          }
          case 17: {
            if (tag !== 138) {
              break;
            }

            message.publishHistoryChunk = PublishHistoryChunkRequest.decode(reader, reader.uint32());
            continue;
          }
          case 18: {
            if (tag !== 146) {
              break;
            }

            message.disapproveHistoryChunk = DisapproveHistoryChunkRequest.decode(reader, reader.uint32());
            continue;
          }
          case 19: {
            if (tag !== 154) {
              break;
            }

            message.getHistoryChunks = GetHistoryChunksRequest.decode(reader, reader.uint32());
            continue;
          }
          case 20: {
            if (tag !== 162) {
              break;
            }

            message.getPendingHistoryRequests = GetPendingHistoryRequests.decode(reader, reader.uint32());
            continue;
          }
          case 21: {
            if (tag !== 170) {
              break;
            }

            message.closeHistoryRequest = CloseHistoryRequest.decode(reader, reader.uint32());
            continue;
          }
          case 22: {
            if (tag !== 178) {
              break;
            }

            message.approveHistoryChunk = ApproveHistoryChunkRequest.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Request {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      createUserMessage: isSet(object.createUserMessage) ? UserMessage.fromJSON(object.createUserMessage) : undefined,
      uploadUserMessage: isSet(object.uploadUserMessage)
        ? UploadUserMessage.fromJSON(object.uploadUserMessage)
        : undefined,
      uploadGroupMessage: isSet(object.uploadGroupMessage)
        ? GroupMessage.fromJSON(object.uploadGroupMessage)
        : undefined,
      requestGroupReAdds: isSet(object.requestGroupReAdds)
        ? RequestGroupReAdds.fromJSON(object.requestGroupReAdds)
        : undefined,
      requestGroupSync: isSet(object.requestGroupSync) ? RequestGroupSync.fromJSON(object.requestGroupSync) : undefined,
      userOnlineStatus: isSet(object.userOnlineStatus)
        ? UserOnlineStatusRequest.fromJSON(object.userOnlineStatus)
        : undefined,
      createJoinLink: isSet(object.createJoinLink) ? CreateJoinLinkRequest.fromJSON(object.createJoinLink) : undefined,
      joinViaLink: isSet(object.joinViaLink) ? JoinViaLinkRequest.fromJSON(object.joinViaLink) : undefined,
      createMeeting: isSet(object.createMeeting) ? CreateMeetingRequest.fromJSON(object.createMeeting) : undefined,
      joinMeeting: isSet(object.joinMeeting) ? JoinMeetingRequest.fromJSON(object.joinMeeting) : undefined,
      leaveMeeting: isSet(object.leaveMeeting) ? LeaveMeetingRequest.fromJSON(object.leaveMeeting) : undefined,
      endMeeting: isSet(object.endMeeting) ? EndMeetingRequest.fromJSON(object.endMeeting) : undefined,
      getActiveSession: isSet(object.getActiveSession)
        ? GetActiveSessionRequest.fromJSON(object.getActiveSession)
        : undefined,
      createHistoryRequest: isSet(object.createHistoryRequest)
        ? CreateHistoryRequest.fromJSON(object.createHistoryRequest)
        : undefined,
      claimHistoryRequest: isSet(object.claimHistoryRequest)
        ? ClaimHistoryRequest.fromJSON(object.claimHistoryRequest)
        : undefined,
      publishHistoryChunk: isSet(object.publishHistoryChunk)
        ? PublishHistoryChunkRequest.fromJSON(object.publishHistoryChunk)
        : undefined,
      disapproveHistoryChunk: isSet(object.disapproveHistoryChunk)
        ? DisapproveHistoryChunkRequest.fromJSON(object.disapproveHistoryChunk)
        : undefined,
      getHistoryChunks: isSet(object.getHistoryChunks)
        ? GetHistoryChunksRequest.fromJSON(object.getHistoryChunks)
        : undefined,
      getPendingHistoryRequests: isSet(object.getPendingHistoryRequests)
        ? GetPendingHistoryRequests.fromJSON(object.getPendingHistoryRequests)
        : undefined,
      closeHistoryRequest: isSet(object.closeHistoryRequest)
        ? CloseHistoryRequest.fromJSON(object.closeHistoryRequest)
        : undefined,
      approveHistoryChunk: isSet(object.approveHistoryChunk)
        ? ApproveHistoryChunkRequest.fromJSON(object.approveHistoryChunk)
        : undefined,
    };
  },

  toJSON(message: Request): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.createUserMessage !== undefined) {
      obj.createUserMessage = UserMessage.toJSON(message.createUserMessage);
    }
    if (message.uploadUserMessage !== undefined) {
      obj.uploadUserMessage = UploadUserMessage.toJSON(message.uploadUserMessage);
    }
    if (message.uploadGroupMessage !== undefined) {
      obj.uploadGroupMessage = GroupMessage.toJSON(message.uploadGroupMessage);
    }
    if (message.requestGroupReAdds !== undefined) {
      obj.requestGroupReAdds = RequestGroupReAdds.toJSON(message.requestGroupReAdds);
    }
    if (message.requestGroupSync !== undefined) {
      obj.requestGroupSync = RequestGroupSync.toJSON(message.requestGroupSync);
    }
    if (message.userOnlineStatus !== undefined) {
      obj.userOnlineStatus = UserOnlineStatusRequest.toJSON(message.userOnlineStatus);
    }
    if (message.createJoinLink !== undefined) {
      obj.createJoinLink = CreateJoinLinkRequest.toJSON(message.createJoinLink);
    }
    if (message.joinViaLink !== undefined) {
      obj.joinViaLink = JoinViaLinkRequest.toJSON(message.joinViaLink);
    }
    if (message.createMeeting !== undefined) {
      obj.createMeeting = CreateMeetingRequest.toJSON(message.createMeeting);
    }
    if (message.joinMeeting !== undefined) {
      obj.joinMeeting = JoinMeetingRequest.toJSON(message.joinMeeting);
    }
    if (message.leaveMeeting !== undefined) {
      obj.leaveMeeting = LeaveMeetingRequest.toJSON(message.leaveMeeting);
    }
    if (message.endMeeting !== undefined) {
      obj.endMeeting = EndMeetingRequest.toJSON(message.endMeeting);
    }
    if (message.getActiveSession !== undefined) {
      obj.getActiveSession = GetActiveSessionRequest.toJSON(message.getActiveSession);
    }
    if (message.createHistoryRequest !== undefined) {
      obj.createHistoryRequest = CreateHistoryRequest.toJSON(message.createHistoryRequest);
    }
    if (message.claimHistoryRequest !== undefined) {
      obj.claimHistoryRequest = ClaimHistoryRequest.toJSON(message.claimHistoryRequest);
    }
    if (message.publishHistoryChunk !== undefined) {
      obj.publishHistoryChunk = PublishHistoryChunkRequest.toJSON(message.publishHistoryChunk);
    }
    if (message.disapproveHistoryChunk !== undefined) {
      obj.disapproveHistoryChunk = DisapproveHistoryChunkRequest.toJSON(message.disapproveHistoryChunk);
    }
    if (message.getHistoryChunks !== undefined) {
      obj.getHistoryChunks = GetHistoryChunksRequest.toJSON(message.getHistoryChunks);
    }
    if (message.getPendingHistoryRequests !== undefined) {
      obj.getPendingHistoryRequests = GetPendingHistoryRequests.toJSON(message.getPendingHistoryRequests);
    }
    if (message.closeHistoryRequest !== undefined) {
      obj.closeHistoryRequest = CloseHistoryRequest.toJSON(message.closeHistoryRequest);
    }
    if (message.approveHistoryChunk !== undefined) {
      obj.approveHistoryChunk = ApproveHistoryChunkRequest.toJSON(message.approveHistoryChunk);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Request>, I>>(base?: I): Request {
    return Request.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Request>, I>>(object: I): Request {
    const message = createBaseRequest();
    message.id = object.id ?? 0;
    message.createUserMessage = (object.createUserMessage !== undefined && object.createUserMessage !== null)
      ? UserMessage.fromPartial(object.createUserMessage)
      : undefined;
    message.uploadUserMessage = (object.uploadUserMessage !== undefined && object.uploadUserMessage !== null)
      ? UploadUserMessage.fromPartial(object.uploadUserMessage)
      : undefined;
    message.uploadGroupMessage = (object.uploadGroupMessage !== undefined && object.uploadGroupMessage !== null)
      ? GroupMessage.fromPartial(object.uploadGroupMessage)
      : undefined;
    message.requestGroupReAdds = (object.requestGroupReAdds !== undefined && object.requestGroupReAdds !== null)
      ? RequestGroupReAdds.fromPartial(object.requestGroupReAdds)
      : undefined;
    message.requestGroupSync = (object.requestGroupSync !== undefined && object.requestGroupSync !== null)
      ? RequestGroupSync.fromPartial(object.requestGroupSync)
      : undefined;
    message.userOnlineStatus = (object.userOnlineStatus !== undefined && object.userOnlineStatus !== null)
      ? UserOnlineStatusRequest.fromPartial(object.userOnlineStatus)
      : undefined;
    message.createJoinLink = (object.createJoinLink !== undefined && object.createJoinLink !== null)
      ? CreateJoinLinkRequest.fromPartial(object.createJoinLink)
      : undefined;
    message.joinViaLink = (object.joinViaLink !== undefined && object.joinViaLink !== null)
      ? JoinViaLinkRequest.fromPartial(object.joinViaLink)
      : undefined;
    message.createMeeting = (object.createMeeting !== undefined && object.createMeeting !== null)
      ? CreateMeetingRequest.fromPartial(object.createMeeting)
      : undefined;
    message.joinMeeting = (object.joinMeeting !== undefined && object.joinMeeting !== null)
      ? JoinMeetingRequest.fromPartial(object.joinMeeting)
      : undefined;
    message.leaveMeeting = (object.leaveMeeting !== undefined && object.leaveMeeting !== null)
      ? LeaveMeetingRequest.fromPartial(object.leaveMeeting)
      : undefined;
    message.endMeeting = (object.endMeeting !== undefined && object.endMeeting !== null)
      ? EndMeetingRequest.fromPartial(object.endMeeting)
      : undefined;
    message.getActiveSession = (object.getActiveSession !== undefined && object.getActiveSession !== null)
      ? GetActiveSessionRequest.fromPartial(object.getActiveSession)
      : undefined;
    message.createHistoryRequest = (object.createHistoryRequest !== undefined && object.createHistoryRequest !== null)
      ? CreateHistoryRequest.fromPartial(object.createHistoryRequest)
      : undefined;
    message.claimHistoryRequest = (object.claimHistoryRequest !== undefined && object.claimHistoryRequest !== null)
      ? ClaimHistoryRequest.fromPartial(object.claimHistoryRequest)
      : undefined;
    message.publishHistoryChunk = (object.publishHistoryChunk !== undefined && object.publishHistoryChunk !== null)
      ? PublishHistoryChunkRequest.fromPartial(object.publishHistoryChunk)
      : undefined;
    message.disapproveHistoryChunk =
      (object.disapproveHistoryChunk !== undefined && object.disapproveHistoryChunk !== null)
        ? DisapproveHistoryChunkRequest.fromPartial(object.disapproveHistoryChunk)
        : undefined;
    message.getHistoryChunks = (object.getHistoryChunks !== undefined && object.getHistoryChunks !== null)
      ? GetHistoryChunksRequest.fromPartial(object.getHistoryChunks)
      : undefined;
    message.getPendingHistoryRequests =
      (object.getPendingHistoryRequests !== undefined && object.getPendingHistoryRequests !== null)
        ? GetPendingHistoryRequests.fromPartial(object.getPendingHistoryRequests)
        : undefined;
    message.closeHistoryRequest = (object.closeHistoryRequest !== undefined && object.closeHistoryRequest !== null)
      ? CloseHistoryRequest.fromPartial(object.closeHistoryRequest)
      : undefined;
    message.approveHistoryChunk = (object.approveHistoryChunk !== undefined && object.approveHistoryChunk !== null)
      ? ApproveHistoryChunkRequest.fromPartial(object.approveHistoryChunk)
      : undefined;
    return message;
  },
};

function createBaseResponse(): Response {
  return {
    id: 0,
    error: undefined,
    createdUserMessage: undefined,
    userMessageUploaded: undefined,
    groupMessageUploaded: undefined,
    groupReAddRequestSuccess: undefined,
    userOnlineStatus: undefined,
    createJoinLink: undefined,
    joinViaLinkSuccess: undefined,
    createMeetingResponse: undefined,
    joinMeetingResponse: undefined,
    getActiveSessionResponse: undefined,
    getHistoryChunksResponse: undefined,
    getPendingHistoryRequestsResponse: undefined,
    historySuccess: undefined,
    claimHistoryResponse: undefined,
  };
}

export const Response: MessageFns<Response> = {
  encode(message: Response, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.error !== undefined) {
      Error.encode(message.error, writer.uint32(18).fork()).join();
    }
    if (message.createdUserMessage !== undefined) {
      UserMessage.encode(message.createdUserMessage, writer.uint32(26).fork()).join();
    }
    if (message.userMessageUploaded !== undefined) {
      UserMessageUploaded.encode(message.userMessageUploaded, writer.uint32(34).fork()).join();
    }
    if (message.groupMessageUploaded !== undefined) {
      GroupMessage.encode(message.groupMessageUploaded, writer.uint32(42).fork()).join();
    }
    if (message.groupReAddRequestSuccess !== undefined) {
      GroupReAddRequestSuccess.encode(message.groupReAddRequestSuccess, writer.uint32(50).fork()).join();
    }
    if (message.userOnlineStatus !== undefined) {
      UserOnlineStatusResponse.encode(message.userOnlineStatus, writer.uint32(58).fork()).join();
    }
    if (message.createJoinLink !== undefined) {
      CreateJoinLinkResponse.encode(message.createJoinLink, writer.uint32(66).fork()).join();
    }
    if (message.joinViaLinkSuccess !== undefined) {
      JoinViaLinkSuccess.encode(message.joinViaLinkSuccess, writer.uint32(74).fork()).join();
    }
    if (message.createMeetingResponse !== undefined) {
      CreateMeetingResponse.encode(message.createMeetingResponse, writer.uint32(82).fork()).join();
    }
    if (message.joinMeetingResponse !== undefined) {
      JoinMeetingResponse.encode(message.joinMeetingResponse, writer.uint32(90).fork()).join();
    }
    if (message.getActiveSessionResponse !== undefined) {
      GetActiveSessionResponse.encode(message.getActiveSessionResponse, writer.uint32(114).fork()).join();
    }
    if (message.getHistoryChunksResponse !== undefined) {
      GetHistoryChunksResponse.encode(message.getHistoryChunksResponse, writer.uint32(122).fork()).join();
    }
    if (message.getPendingHistoryRequestsResponse !== undefined) {
      GetPendingHistoryRequestsResponse.encode(message.getPendingHistoryRequestsResponse, writer.uint32(130).fork())
        .join();
    }
    if (message.historySuccess !== undefined) {
      HistoryOperationSuccess.encode(message.historySuccess, writer.uint32(138).fork()).join();
    }
    if (message.claimHistoryResponse !== undefined) {
      ClaimHistoryResponse.encode(message.claimHistoryResponse, writer.uint32(146).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Response {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.error = Error.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.createdUserMessage = UserMessage.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.userMessageUploaded = UserMessageUploaded.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.groupMessageUploaded = GroupMessage.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.groupReAddRequestSuccess = GroupReAddRequestSuccess.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.userOnlineStatus = UserOnlineStatusResponse.decode(reader, reader.uint32());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.createJoinLink = CreateJoinLinkResponse.decode(reader, reader.uint32());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.joinViaLinkSuccess = JoinViaLinkSuccess.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.createMeetingResponse = CreateMeetingResponse.decode(reader, reader.uint32());
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.joinMeetingResponse = JoinMeetingResponse.decode(reader, reader.uint32());
            continue;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.getActiveSessionResponse = GetActiveSessionResponse.decode(reader, reader.uint32());
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.getHistoryChunksResponse = GetHistoryChunksResponse.decode(reader, reader.uint32());
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.getPendingHistoryRequestsResponse = GetPendingHistoryRequestsResponse.decode(
              reader,
              reader.uint32(),
            );
            continue;
          }
          case 17: {
            if (tag !== 138) {
              break;
            }

            message.historySuccess = HistoryOperationSuccess.decode(reader, reader.uint32());
            continue;
          }
          case 18: {
            if (tag !== 146) {
              break;
            }

            message.claimHistoryResponse = ClaimHistoryResponse.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Response {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      error: isSet(object.error) ? Error.fromJSON(object.error) : undefined,
      createdUserMessage: isSet(object.createdUserMessage)
        ? UserMessage.fromJSON(object.createdUserMessage)
        : undefined,
      userMessageUploaded: isSet(object.userMessageUploaded)
        ? UserMessageUploaded.fromJSON(object.userMessageUploaded)
        : undefined,
      groupMessageUploaded: isSet(object.groupMessageUploaded)
        ? GroupMessage.fromJSON(object.groupMessageUploaded)
        : undefined,
      groupReAddRequestSuccess: isSet(object.groupReAddRequestSuccess)
        ? GroupReAddRequestSuccess.fromJSON(object.groupReAddRequestSuccess)
        : undefined,
      userOnlineStatus: isSet(object.userOnlineStatus)
        ? UserOnlineStatusResponse.fromJSON(object.userOnlineStatus)
        : undefined,
      createJoinLink: isSet(object.createJoinLink) ? CreateJoinLinkResponse.fromJSON(object.createJoinLink) : undefined,
      joinViaLinkSuccess: isSet(object.joinViaLinkSuccess)
        ? JoinViaLinkSuccess.fromJSON(object.joinViaLinkSuccess)
        : undefined,
      createMeetingResponse: isSet(object.createMeetingResponse)
        ? CreateMeetingResponse.fromJSON(object.createMeetingResponse)
        : undefined,
      joinMeetingResponse: isSet(object.joinMeetingResponse)
        ? JoinMeetingResponse.fromJSON(object.joinMeetingResponse)
        : undefined,
      getActiveSessionResponse: isSet(object.getActiveSessionResponse)
        ? GetActiveSessionResponse.fromJSON(object.getActiveSessionResponse)
        : undefined,
      getHistoryChunksResponse: isSet(object.getHistoryChunksResponse)
        ? GetHistoryChunksResponse.fromJSON(object.getHistoryChunksResponse)
        : undefined,
      getPendingHistoryRequestsResponse: isSet(object.getPendingHistoryRequestsResponse)
        ? GetPendingHistoryRequestsResponse.fromJSON(object.getPendingHistoryRequestsResponse)
        : undefined,
      historySuccess: isSet(object.historySuccess)
        ? HistoryOperationSuccess.fromJSON(object.historySuccess)
        : undefined,
      claimHistoryResponse: isSet(object.claimHistoryResponse)
        ? ClaimHistoryResponse.fromJSON(object.claimHistoryResponse)
        : undefined,
    };
  },

  toJSON(message: Response): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.error !== undefined) {
      obj.error = Error.toJSON(message.error);
    }
    if (message.createdUserMessage !== undefined) {
      obj.createdUserMessage = UserMessage.toJSON(message.createdUserMessage);
    }
    if (message.userMessageUploaded !== undefined) {
      obj.userMessageUploaded = UserMessageUploaded.toJSON(message.userMessageUploaded);
    }
    if (message.groupMessageUploaded !== undefined) {
      obj.groupMessageUploaded = GroupMessage.toJSON(message.groupMessageUploaded);
    }
    if (message.groupReAddRequestSuccess !== undefined) {
      obj.groupReAddRequestSuccess = GroupReAddRequestSuccess.toJSON(message.groupReAddRequestSuccess);
    }
    if (message.userOnlineStatus !== undefined) {
      obj.userOnlineStatus = UserOnlineStatusResponse.toJSON(message.userOnlineStatus);
    }
    if (message.createJoinLink !== undefined) {
      obj.createJoinLink = CreateJoinLinkResponse.toJSON(message.createJoinLink);
    }
    if (message.joinViaLinkSuccess !== undefined) {
      obj.joinViaLinkSuccess = JoinViaLinkSuccess.toJSON(message.joinViaLinkSuccess);
    }
    if (message.createMeetingResponse !== undefined) {
      obj.createMeetingResponse = CreateMeetingResponse.toJSON(message.createMeetingResponse);
    }
    if (message.joinMeetingResponse !== undefined) {
      obj.joinMeetingResponse = JoinMeetingResponse.toJSON(message.joinMeetingResponse);
    }
    if (message.getActiveSessionResponse !== undefined) {
      obj.getActiveSessionResponse = GetActiveSessionResponse.toJSON(message.getActiveSessionResponse);
    }
    if (message.getHistoryChunksResponse !== undefined) {
      obj.getHistoryChunksResponse = GetHistoryChunksResponse.toJSON(message.getHistoryChunksResponse);
    }
    if (message.getPendingHistoryRequestsResponse !== undefined) {
      obj.getPendingHistoryRequestsResponse = GetPendingHistoryRequestsResponse.toJSON(
        message.getPendingHistoryRequestsResponse,
      );
    }
    if (message.historySuccess !== undefined) {
      obj.historySuccess = HistoryOperationSuccess.toJSON(message.historySuccess);
    }
    if (message.claimHistoryResponse !== undefined) {
      obj.claimHistoryResponse = ClaimHistoryResponse.toJSON(message.claimHistoryResponse);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Response>, I>>(base?: I): Response {
    return Response.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Response>, I>>(object: I): Response {
    const message = createBaseResponse();
    message.id = object.id ?? 0;
    message.error = (object.error !== undefined && object.error !== null) ? Error.fromPartial(object.error) : undefined;
    message.createdUserMessage = (object.createdUserMessage !== undefined && object.createdUserMessage !== null)
      ? UserMessage.fromPartial(object.createdUserMessage)
      : undefined;
    message.userMessageUploaded = (object.userMessageUploaded !== undefined && object.userMessageUploaded !== null)
      ? UserMessageUploaded.fromPartial(object.userMessageUploaded)
      : undefined;
    message.groupMessageUploaded = (object.groupMessageUploaded !== undefined && object.groupMessageUploaded !== null)
      ? GroupMessage.fromPartial(object.groupMessageUploaded)
      : undefined;
    message.groupReAddRequestSuccess =
      (object.groupReAddRequestSuccess !== undefined && object.groupReAddRequestSuccess !== null)
        ? GroupReAddRequestSuccess.fromPartial(object.groupReAddRequestSuccess)
        : undefined;
    message.userOnlineStatus = (object.userOnlineStatus !== undefined && object.userOnlineStatus !== null)
      ? UserOnlineStatusResponse.fromPartial(object.userOnlineStatus)
      : undefined;
    message.createJoinLink = (object.createJoinLink !== undefined && object.createJoinLink !== null)
      ? CreateJoinLinkResponse.fromPartial(object.createJoinLink)
      : undefined;
    message.joinViaLinkSuccess = (object.joinViaLinkSuccess !== undefined && object.joinViaLinkSuccess !== null)
      ? JoinViaLinkSuccess.fromPartial(object.joinViaLinkSuccess)
      : undefined;
    message.createMeetingResponse =
      (object.createMeetingResponse !== undefined && object.createMeetingResponse !== null)
        ? CreateMeetingResponse.fromPartial(object.createMeetingResponse)
        : undefined;
    message.joinMeetingResponse = (object.joinMeetingResponse !== undefined && object.joinMeetingResponse !== null)
      ? JoinMeetingResponse.fromPartial(object.joinMeetingResponse)
      : undefined;
    message.getActiveSessionResponse =
      (object.getActiveSessionResponse !== undefined && object.getActiveSessionResponse !== null)
        ? GetActiveSessionResponse.fromPartial(object.getActiveSessionResponse)
        : undefined;
    message.getHistoryChunksResponse =
      (object.getHistoryChunksResponse !== undefined && object.getHistoryChunksResponse !== null)
        ? GetHistoryChunksResponse.fromPartial(object.getHistoryChunksResponse)
        : undefined;
    message.getPendingHistoryRequestsResponse =
      (object.getPendingHistoryRequestsResponse !== undefined && object.getPendingHistoryRequestsResponse !== null)
        ? GetPendingHistoryRequestsResponse.fromPartial(object.getPendingHistoryRequestsResponse)
        : undefined;
    message.historySuccess = (object.historySuccess !== undefined && object.historySuccess !== null)
      ? HistoryOperationSuccess.fromPartial(object.historySuccess)
      : undefined;
    message.claimHistoryResponse = (object.claimHistoryResponse !== undefined && object.claimHistoryResponse !== null)
      ? ClaimHistoryResponse.fromPartial(object.claimHistoryResponse)
      : undefined;
    return message;
  },
};

function createBaseServerMessage(): ServerMessage {
  return {
    userMessage: undefined,
    groupMessage: undefined,
    userMessages: undefined,
    groupMessages: undefined,
    response: undefined,
    ping: undefined,
    pong: undefined,
    readUserMessagesUpto: undefined,
    groupInvite: undefined,
    groupCommits: undefined,
    groupReAddRequests: undefined,
    groupJoinRequests: undefined,
    callSignal: undefined,
    groupMeetingSignal: undefined,
    groupHistorySignal: undefined,
  };
}

export const ServerMessage: MessageFns<ServerMessage> = {
  encode(message: ServerMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userMessage !== undefined) {
      UserMessage.encode(message.userMessage, writer.uint32(10).fork()).join();
    }
    if (message.groupMessage !== undefined) {
      GroupMessage.encode(message.groupMessage, writer.uint32(18).fork()).join();
    }
    if (message.userMessages !== undefined) {
      UserMessages.encode(message.userMessages, writer.uint32(26).fork()).join();
    }
    if (message.groupMessages !== undefined) {
      GroupMessages.encode(message.groupMessages, writer.uint32(34).fork()).join();
    }
    if (message.response !== undefined) {
      Response.encode(message.response, writer.uint32(82).fork()).join();
    }
    if (message.ping !== undefined) {
      writer.uint32(90).bytes(message.ping);
    }
    if (message.pong !== undefined) {
      writer.uint32(98).bytes(message.pong);
    }
    if (message.readUserMessagesUpto !== undefined) {
      ReadUserMessagesUpto.encode(message.readUserMessagesUpto, writer.uint32(106).fork()).join();
    }
    if (message.groupInvite !== undefined) {
      GroupInvite.encode(message.groupInvite, writer.uint32(122).fork()).join();
    }
    if (message.groupCommits !== undefined) {
      GroupCommits.encode(message.groupCommits, writer.uint32(130).fork()).join();
    }
    if (message.groupReAddRequests !== undefined) {
      GroupReAddRequests.encode(message.groupReAddRequests, writer.uint32(138).fork()).join();
    }
    if (message.groupJoinRequests !== undefined) {
      GroupJoinRequests.encode(message.groupJoinRequests, writer.uint32(146).fork()).join();
    }
    if (message.callSignal !== undefined) {
      CallSignal.encode(message.callSignal, writer.uint32(162).fork()).join();
    }
    if (message.groupMeetingSignal !== undefined) {
      GroupMeetingSignal.encode(message.groupMeetingSignal, writer.uint32(170).fork()).join();
    }
    if (message.groupHistorySignal !== undefined) {
      GroupHistorySignal.encode(message.groupHistorySignal, writer.uint32(178).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ServerMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseServerMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.userMessage = UserMessage.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.groupMessage = GroupMessage.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.userMessages = UserMessages.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.groupMessages = GroupMessages.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.response = Response.decode(reader, reader.uint32());
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.ping = Buffer.from(reader.bytes());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.pong = Buffer.from(reader.bytes());
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.readUserMessagesUpto = ReadUserMessagesUpto.decode(reader, reader.uint32());
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.groupInvite = GroupInvite.decode(reader, reader.uint32());
            continue;
          }
          case 16: {
            if (tag !== 130) {
              break;
            }

            message.groupCommits = GroupCommits.decode(reader, reader.uint32());
            continue;
          }
          case 17: {
            if (tag !== 138) {
              break;
            }

            message.groupReAddRequests = GroupReAddRequests.decode(reader, reader.uint32());
            continue;
          }
          case 18: {
            if (tag !== 146) {
              break;
            }

            message.groupJoinRequests = GroupJoinRequests.decode(reader, reader.uint32());
            continue;
          }
          case 20: {
            if (tag !== 162) {
              break;
            }

            message.callSignal = CallSignal.decode(reader, reader.uint32());
            continue;
          }
          case 21: {
            if (tag !== 170) {
              break;
            }

            message.groupMeetingSignal = GroupMeetingSignal.decode(reader, reader.uint32());
            continue;
          }
          case 22: {
            if (tag !== 178) {
              break;
            }

            message.groupHistorySignal = GroupHistorySignal.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ServerMessage {
    return {
      userMessage: isSet(object.userMessage) ? UserMessage.fromJSON(object.userMessage) : undefined,
      groupMessage: isSet(object.groupMessage) ? GroupMessage.fromJSON(object.groupMessage) : undefined,
      userMessages: isSet(object.userMessages) ? UserMessages.fromJSON(object.userMessages) : undefined,
      groupMessages: isSet(object.groupMessages) ? GroupMessages.fromJSON(object.groupMessages) : undefined,
      response: isSet(object.response) ? Response.fromJSON(object.response) : undefined,
      ping: isSet(object.ping) ? Buffer.from(bytesFromBase64(object.ping)) : undefined,
      pong: isSet(object.pong) ? Buffer.from(bytesFromBase64(object.pong)) : undefined,
      readUserMessagesUpto: isSet(object.readUserMessagesUpto)
        ? ReadUserMessagesUpto.fromJSON(object.readUserMessagesUpto)
        : undefined,
      groupInvite: isSet(object.groupInvite) ? GroupInvite.fromJSON(object.groupInvite) : undefined,
      groupCommits: isSet(object.groupCommits) ? GroupCommits.fromJSON(object.groupCommits) : undefined,
      groupReAddRequests: isSet(object.groupReAddRequests)
        ? GroupReAddRequests.fromJSON(object.groupReAddRequests)
        : undefined,
      groupJoinRequests: isSet(object.groupJoinRequests)
        ? GroupJoinRequests.fromJSON(object.groupJoinRequests)
        : undefined,
      callSignal: isSet(object.callSignal) ? CallSignal.fromJSON(object.callSignal) : undefined,
      groupMeetingSignal: isSet(object.groupMeetingSignal)
        ? GroupMeetingSignal.fromJSON(object.groupMeetingSignal)
        : undefined,
      groupHistorySignal: isSet(object.groupHistorySignal)
        ? GroupHistorySignal.fromJSON(object.groupHistorySignal)
        : undefined,
    };
  },

  toJSON(message: ServerMessage): unknown {
    const obj: any = {};
    if (message.userMessage !== undefined) {
      obj.userMessage = UserMessage.toJSON(message.userMessage);
    }
    if (message.groupMessage !== undefined) {
      obj.groupMessage = GroupMessage.toJSON(message.groupMessage);
    }
    if (message.userMessages !== undefined) {
      obj.userMessages = UserMessages.toJSON(message.userMessages);
    }
    if (message.groupMessages !== undefined) {
      obj.groupMessages = GroupMessages.toJSON(message.groupMessages);
    }
    if (message.response !== undefined) {
      obj.response = Response.toJSON(message.response);
    }
    if (message.ping !== undefined) {
      obj.ping = base64FromBytes(message.ping);
    }
    if (message.pong !== undefined) {
      obj.pong = base64FromBytes(message.pong);
    }
    if (message.readUserMessagesUpto !== undefined) {
      obj.readUserMessagesUpto = ReadUserMessagesUpto.toJSON(message.readUserMessagesUpto);
    }
    if (message.groupInvite !== undefined) {
      obj.groupInvite = GroupInvite.toJSON(message.groupInvite);
    }
    if (message.groupCommits !== undefined) {
      obj.groupCommits = GroupCommits.toJSON(message.groupCommits);
    }
    if (message.groupReAddRequests !== undefined) {
      obj.groupReAddRequests = GroupReAddRequests.toJSON(message.groupReAddRequests);
    }
    if (message.groupJoinRequests !== undefined) {
      obj.groupJoinRequests = GroupJoinRequests.toJSON(message.groupJoinRequests);
    }
    if (message.callSignal !== undefined) {
      obj.callSignal = CallSignal.toJSON(message.callSignal);
    }
    if (message.groupMeetingSignal !== undefined) {
      obj.groupMeetingSignal = GroupMeetingSignal.toJSON(message.groupMeetingSignal);
    }
    if (message.groupHistorySignal !== undefined) {
      obj.groupHistorySignal = GroupHistorySignal.toJSON(message.groupHistorySignal);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ServerMessage>, I>>(base?: I): ServerMessage {
    return ServerMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ServerMessage>, I>>(object: I): ServerMessage {
    const message = createBaseServerMessage();
    message.userMessage = (object.userMessage !== undefined && object.userMessage !== null)
      ? UserMessage.fromPartial(object.userMessage)
      : undefined;
    message.groupMessage = (object.groupMessage !== undefined && object.groupMessage !== null)
      ? GroupMessage.fromPartial(object.groupMessage)
      : undefined;
    message.userMessages = (object.userMessages !== undefined && object.userMessages !== null)
      ? UserMessages.fromPartial(object.userMessages)
      : undefined;
    message.groupMessages = (object.groupMessages !== undefined && object.groupMessages !== null)
      ? GroupMessages.fromPartial(object.groupMessages)
      : undefined;
    message.response = (object.response !== undefined && object.response !== null)
      ? Response.fromPartial(object.response)
      : undefined;
    message.ping = object.ping ?? undefined;
    message.pong = object.pong ?? undefined;
    message.readUserMessagesUpto = (object.readUserMessagesUpto !== undefined && object.readUserMessagesUpto !== null)
      ? ReadUserMessagesUpto.fromPartial(object.readUserMessagesUpto)
      : undefined;
    message.groupInvite = (object.groupInvite !== undefined && object.groupInvite !== null)
      ? GroupInvite.fromPartial(object.groupInvite)
      : undefined;
    message.groupCommits = (object.groupCommits !== undefined && object.groupCommits !== null)
      ? GroupCommits.fromPartial(object.groupCommits)
      : undefined;
    message.groupReAddRequests = (object.groupReAddRequests !== undefined && object.groupReAddRequests !== null)
      ? GroupReAddRequests.fromPartial(object.groupReAddRequests)
      : undefined;
    message.groupJoinRequests = (object.groupJoinRequests !== undefined && object.groupJoinRequests !== null)
      ? GroupJoinRequests.fromPartial(object.groupJoinRequests)
      : undefined;
    message.callSignal = (object.callSignal !== undefined && object.callSignal !== null)
      ? CallSignal.fromPartial(object.callSignal)
      : undefined;
    message.groupMeetingSignal = (object.groupMeetingSignal !== undefined && object.groupMeetingSignal !== null)
      ? GroupMeetingSignal.fromPartial(object.groupMeetingSignal)
      : undefined;
    message.groupHistorySignal = (object.groupHistorySignal !== undefined && object.groupHistorySignal !== null)
      ? GroupHistorySignal.fromPartial(object.groupHistorySignal)
      : undefined;
    return message;
  },
};

function createBaseClientMessage(): ClientMessage {
  return {
    userMessage: undefined,
    groupMessage: undefined,
    verifiedUserMessage: undefined,
    request: undefined,
    ping: undefined,
    pong: undefined,
    readUserMessagesUpto: undefined,
    callSignal: undefined,
    groupMeetingSignal: undefined,
  };
}

export const ClientMessage: MessageFns<ClientMessage> = {
  encode(message: ClientMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userMessage !== undefined) {
      UserMessage.encode(message.userMessage, writer.uint32(10).fork()).join();
    }
    if (message.groupMessage !== undefined) {
      GroupMessage.encode(message.groupMessage, writer.uint32(18).fork()).join();
    }
    if (message.verifiedUserMessage !== undefined) {
      UserMessage.encode(message.verifiedUserMessage, writer.uint32(26).fork()).join();
    }
    if (message.request !== undefined) {
      Request.encode(message.request, writer.uint32(82).fork()).join();
    }
    if (message.ping !== undefined) {
      writer.uint32(90).bytes(message.ping);
    }
    if (message.pong !== undefined) {
      writer.uint32(98).bytes(message.pong);
    }
    if (message.readUserMessagesUpto !== undefined) {
      ReadUserMessagesUpto.encode(message.readUserMessagesUpto, writer.uint32(106).fork()).join();
    }
    if (message.callSignal !== undefined) {
      CallSignal.encode(message.callSignal, writer.uint32(162).fork()).join();
    }
    if (message.groupMeetingSignal !== undefined) {
      GroupMeetingSignal.encode(message.groupMeetingSignal, writer.uint32(170).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClientMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseClientMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.userMessage = UserMessage.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.groupMessage = GroupMessage.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.verifiedUserMessage = UserMessage.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.request = Request.decode(reader, reader.uint32());
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.ping = Buffer.from(reader.bytes());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.pong = Buffer.from(reader.bytes());
            continue;
          }
          case 13: {
            if (tag !== 106) {
              break;
            }

            message.readUserMessagesUpto = ReadUserMessagesUpto.decode(reader, reader.uint32());
            continue;
          }
          case 20: {
            if (tag !== 162) {
              break;
            }

            message.callSignal = CallSignal.decode(reader, reader.uint32());
            continue;
          }
          case 21: {
            if (tag !== 170) {
              break;
            }

            message.groupMeetingSignal = GroupMeetingSignal.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ClientMessage {
    return {
      userMessage: isSet(object.userMessage) ? UserMessage.fromJSON(object.userMessage) : undefined,
      groupMessage: isSet(object.groupMessage) ? GroupMessage.fromJSON(object.groupMessage) : undefined,
      verifiedUserMessage: isSet(object.verifiedUserMessage)
        ? UserMessage.fromJSON(object.verifiedUserMessage)
        : undefined,
      request: isSet(object.request) ? Request.fromJSON(object.request) : undefined,
      ping: isSet(object.ping) ? Buffer.from(bytesFromBase64(object.ping)) : undefined,
      pong: isSet(object.pong) ? Buffer.from(bytesFromBase64(object.pong)) : undefined,
      readUserMessagesUpto: isSet(object.readUserMessagesUpto)
        ? ReadUserMessagesUpto.fromJSON(object.readUserMessagesUpto)
        : undefined,
      callSignal: isSet(object.callSignal) ? CallSignal.fromJSON(object.callSignal) : undefined,
      groupMeetingSignal: isSet(object.groupMeetingSignal)
        ? GroupMeetingSignal.fromJSON(object.groupMeetingSignal)
        : undefined,
    };
  },

  toJSON(message: ClientMessage): unknown {
    const obj: any = {};
    if (message.userMessage !== undefined) {
      obj.userMessage = UserMessage.toJSON(message.userMessage);
    }
    if (message.groupMessage !== undefined) {
      obj.groupMessage = GroupMessage.toJSON(message.groupMessage);
    }
    if (message.verifiedUserMessage !== undefined) {
      obj.verifiedUserMessage = UserMessage.toJSON(message.verifiedUserMessage);
    }
    if (message.request !== undefined) {
      obj.request = Request.toJSON(message.request);
    }
    if (message.ping !== undefined) {
      obj.ping = base64FromBytes(message.ping);
    }
    if (message.pong !== undefined) {
      obj.pong = base64FromBytes(message.pong);
    }
    if (message.readUserMessagesUpto !== undefined) {
      obj.readUserMessagesUpto = ReadUserMessagesUpto.toJSON(message.readUserMessagesUpto);
    }
    if (message.callSignal !== undefined) {
      obj.callSignal = CallSignal.toJSON(message.callSignal);
    }
    if (message.groupMeetingSignal !== undefined) {
      obj.groupMeetingSignal = GroupMeetingSignal.toJSON(message.groupMeetingSignal);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClientMessage>, I>>(base?: I): ClientMessage {
    return ClientMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClientMessage>, I>>(object: I): ClientMessage {
    const message = createBaseClientMessage();
    message.userMessage = (object.userMessage !== undefined && object.userMessage !== null)
      ? UserMessage.fromPartial(object.userMessage)
      : undefined;
    message.groupMessage = (object.groupMessage !== undefined && object.groupMessage !== null)
      ? GroupMessage.fromPartial(object.groupMessage)
      : undefined;
    message.verifiedUserMessage = (object.verifiedUserMessage !== undefined && object.verifiedUserMessage !== null)
      ? UserMessage.fromPartial(object.verifiedUserMessage)
      : undefined;
    message.request = (object.request !== undefined && object.request !== null)
      ? Request.fromPartial(object.request)
      : undefined;
    message.ping = object.ping ?? undefined;
    message.pong = object.pong ?? undefined;
    message.readUserMessagesUpto = (object.readUserMessagesUpto !== undefined && object.readUserMessagesUpto !== null)
      ? ReadUserMessagesUpto.fromPartial(object.readUserMessagesUpto)
      : undefined;
    message.callSignal = (object.callSignal !== undefined && object.callSignal !== null)
      ? CallSignal.fromPartial(object.callSignal)
      : undefined;
    message.groupMeetingSignal = (object.groupMeetingSignal !== undefined && object.groupMeetingSignal !== null)
      ? GroupMeetingSignal.fromPartial(object.groupMeetingSignal)
      : undefined;
    return message;
  },
};

function createBaseReadUserMessagesUpto(): ReadUserMessagesUpto {
  return { other: "", uptoMessageId: 0n };
}

export const ReadUserMessagesUpto: MessageFns<ReadUserMessagesUpto> = {
  encode(message: ReadUserMessagesUpto, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.other !== "") {
      writer.uint32(10).string(message.other);
    }
    if (message.uptoMessageId !== 0n) {
      if (BigInt.asUintN(64, message.uptoMessageId) !== message.uptoMessageId) {
        throw new globalThis.Error("value provided for field message.uptoMessageId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.uptoMessageId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ReadUserMessagesUpto {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseReadUserMessagesUpto();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.other = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.uptoMessageId = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ReadUserMessagesUpto {
    return {
      other: isSet(object.other) ? globalThis.String(object.other) : "",
      uptoMessageId: isSet(object.uptoMessageId) ? BigInt(object.uptoMessageId) : 0n,
    };
  },

  toJSON(message: ReadUserMessagesUpto): unknown {
    const obj: any = {};
    if (message.other !== "") {
      obj.other = message.other;
    }
    if (message.uptoMessageId !== 0n) {
      obj.uptoMessageId = message.uptoMessageId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ReadUserMessagesUpto>, I>>(base?: I): ReadUserMessagesUpto {
    return ReadUserMessagesUpto.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ReadUserMessagesUpto>, I>>(object: I): ReadUserMessagesUpto {
    const message = createBaseReadUserMessagesUpto();
    message.other = object.other ?? "";
    message.uptoMessageId = (object.uptoMessageId !== undefined && object.uptoMessageId !== null)
      ? BigInt(object.uptoMessageId)
      : 0n;
    return message;
  },
};

function createBaseCallSignal(): CallSignal {
  return {
    callId: 0n,
    senderUsername: "",
    receiverUsername: "",
    type: 0,
    sdp: "",
    candidate: "",
    sdpMLineIndex: 0,
    sdpMid: "",
    senderDeviceId: 0,
  };
}

export const CallSignal: MessageFns<CallSignal> = {
  encode(message: CallSignal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.callId !== 0n) {
      if (BigInt.asUintN(64, message.callId) !== message.callId) {
        throw new globalThis.Error("value provided for field message.callId of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.callId);
    }
    if (message.senderUsername !== "") {
      writer.uint32(18).string(message.senderUsername);
    }
    if (message.receiverUsername !== "") {
      writer.uint32(26).string(message.receiverUsername);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    if (message.sdp !== "") {
      writer.uint32(42).string(message.sdp);
    }
    if (message.candidate !== "") {
      writer.uint32(50).string(message.candidate);
    }
    if (message.sdpMLineIndex !== 0) {
      writer.uint32(56).int32(message.sdpMLineIndex);
    }
    if (message.sdpMid !== "") {
      writer.uint32(66).string(message.sdpMid);
    }
    if (message.senderDeviceId !== 0) {
      writer.uint32(72).uint32(message.senderDeviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallSignal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCallSignal();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.callId = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.senderUsername = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.receiverUsername = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.type = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.sdp = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.candidate = reader.string();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.sdpMLineIndex = reader.int32();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.sdpMid = reader.string();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.senderDeviceId = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CallSignal {
    return {
      callId: isSet(object.callId) ? BigInt(object.callId) : isSet(object.call_id) ? BigInt(object.call_id) : 0n,
      senderUsername: isSet(object.senderUsername)
        ? globalThis.String(object.senderUsername)
        : isSet(object.sender_username)
        ? globalThis.String(object.sender_username)
        : "",
      receiverUsername: isSet(object.receiverUsername)
        ? globalThis.String(object.receiverUsername)
        : isSet(object.receiver_username)
        ? globalThis.String(object.receiver_username)
        : "",
      type: isSet(object.type) ? callSignalTypeFromJSON(object.type) : 0,
      sdp: isSet(object.sdp) ? globalThis.String(object.sdp) : "",
      candidate: isSet(object.candidate) ? globalThis.String(object.candidate) : "",
      sdpMLineIndex: isSet(object.sdpMLineIndex)
        ? globalThis.Number(object.sdpMLineIndex)
        : isSet(object.sdp_m_line_index)
        ? globalThis.Number(object.sdp_m_line_index)
        : 0,
      sdpMid: isSet(object.sdpMid)
        ? globalThis.String(object.sdpMid)
        : isSet(object.sdp_mid)
        ? globalThis.String(object.sdp_mid)
        : "",
      senderDeviceId: isSet(object.senderDeviceId)
        ? globalThis.Number(object.senderDeviceId)
        : isSet(object.sender_device_id)
        ? globalThis.Number(object.sender_device_id)
        : 0,
    };
  },

  toJSON(message: CallSignal): unknown {
    const obj: any = {};
    if (message.callId !== 0n) {
      obj.callId = message.callId.toString();
    }
    if (message.senderUsername !== "") {
      obj.senderUsername = message.senderUsername;
    }
    if (message.receiverUsername !== "") {
      obj.receiverUsername = message.receiverUsername;
    }
    if (message.type !== 0) {
      obj.type = callSignalTypeToJSON(message.type);
    }
    if (message.sdp !== "") {
      obj.sdp = message.sdp;
    }
    if (message.candidate !== "") {
      obj.candidate = message.candidate;
    }
    if (message.sdpMLineIndex !== 0) {
      obj.sdpMLineIndex = Math.round(message.sdpMLineIndex);
    }
    if (message.sdpMid !== "") {
      obj.sdpMid = message.sdpMid;
    }
    if (message.senderDeviceId !== 0) {
      obj.senderDeviceId = Math.round(message.senderDeviceId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallSignal>, I>>(base?: I): CallSignal {
    return CallSignal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallSignal>, I>>(object: I): CallSignal {
    const message = createBaseCallSignal();
    message.callId = (object.callId !== undefined && object.callId !== null) ? BigInt(object.callId) : 0n;
    message.senderUsername = object.senderUsername ?? "";
    message.receiverUsername = object.receiverUsername ?? "";
    message.type = object.type ?? 0;
    message.sdp = object.sdp ?? "";
    message.candidate = object.candidate ?? "";
    message.sdpMLineIndex = object.sdpMLineIndex ?? 0;
    message.sdpMid = object.sdpMid ?? "";
    message.senderDeviceId = object.senderDeviceId ?? 0;
    return message;
  },
};

function createBaseGroupId(): GroupId {
  return { id: 0n };
}

export const GroupId: MessageFns<GroupId> = {
  encode(message: GroupId, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(16).uint64(message.id);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupId {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupId();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.id = reader.uint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupId {
    return { id: isSet(object.id) ? BigInt(object.id) : 0n };
  },

  toJSON(message: GroupId): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupId>, I>>(base?: I): GroupId {
    return GroupId.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupId>, I>>(object: I): GroupId {
    const message = createBaseGroupId();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    return message;
  },
};

function createBaseAuthToken(): AuthToken {
  return { username: "", validUntil: 0n, credential: Buffer.alloc(0), addressId: 0n, deviceId: 0 };
}

export const AuthToken: MessageFns<AuthToken> = {
  encode(message: AuthToken, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.validUntil !== 0n) {
      if (BigInt.asUintN(64, message.validUntil) !== message.validUntil) {
        throw new globalThis.Error("value provided for field message.validUntil of type uint64 too large");
      }
      writer.uint32(16).uint64(message.validUntil);
    }
    if (message.credential.length !== 0) {
      writer.uint32(34).bytes(message.credential);
    }
    if (message.addressId !== 0n) {
      if (BigInt.asUintN(64, message.addressId) !== message.addressId) {
        throw new globalThis.Error("value provided for field message.addressId of type uint64 too large");
      }
      writer.uint32(48).uint64(message.addressId);
    }
    if (message.deviceId !== 0) {
      writer.uint32(40).uint32(message.deviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AuthToken {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseAuthToken();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.validUntil = reader.uint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.credential = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.addressId = reader.uint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.deviceId = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): AuthToken {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      validUntil: isSet(object.validUntil)
        ? BigInt(object.validUntil)
        : isSet(object.valid_until)
        ? BigInt(object.valid_until)
        : 0n,
      credential: isSet(object.credential) ? Buffer.from(bytesFromBase64(object.credential)) : Buffer.alloc(0),
      addressId: isSet(object.addressId)
        ? BigInt(object.addressId)
        : isSet(object.address_id)
        ? BigInt(object.address_id)
        : 0n,
      deviceId: isSet(object.deviceId)
        ? globalThis.Number(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.Number(object.device_id)
        : 0,
    };
  },

  toJSON(message: AuthToken): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.validUntil !== 0n) {
      obj.validUntil = message.validUntil.toString();
    }
    if (message.credential.length !== 0) {
      obj.credential = base64FromBytes(message.credential);
    }
    if (message.addressId !== 0n) {
      obj.addressId = message.addressId.toString();
    }
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AuthToken>, I>>(base?: I): AuthToken {
    return AuthToken.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AuthToken>, I>>(object: I): AuthToken {
    const message = createBaseAuthToken();
    message.username = object.username ?? "";
    message.validUntil = (object.validUntil !== undefined && object.validUntil !== null)
      ? BigInt(object.validUntil)
      : 0n;
    message.credential = object.credential ?? Buffer.alloc(0);
    message.addressId = (object.addressId !== undefined && object.addressId !== null) ? BigInt(object.addressId) : 0n;
    message.deviceId = object.deviceId ?? 0;
    return message;
  },
};

function createBaseSignedToken(): SignedToken {
  return { kid: "", payload: Buffer.alloc(0), signature: Buffer.alloc(0) };
}

export const SignedToken: MessageFns<SignedToken> = {
  encode(message: SignedToken, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kid !== "") {
      writer.uint32(10).string(message.kid);
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    if (message.signature.length !== 0) {
      writer.uint32(26).bytes(message.signature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SignedToken {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSignedToken();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.kid = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.payload = Buffer.from(reader.bytes());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.signature = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SignedToken {
    return {
      kid: isSet(object.kid) ? globalThis.String(object.kid) : "",
      payload: isSet(object.payload) ? Buffer.from(bytesFromBase64(object.payload)) : Buffer.alloc(0),
      signature: isSet(object.signature) ? Buffer.from(bytesFromBase64(object.signature)) : Buffer.alloc(0),
    };
  },

  toJSON(message: SignedToken): unknown {
    const obj: any = {};
    if (message.kid !== "") {
      obj.kid = message.kid;
    }
    if (message.payload.length !== 0) {
      obj.payload = base64FromBytes(message.payload);
    }
    if (message.signature.length !== 0) {
      obj.signature = base64FromBytes(message.signature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SignedToken>, I>>(base?: I): SignedToken {
    return SignedToken.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SignedToken>, I>>(object: I): SignedToken {
    const message = createBaseSignedToken();
    message.kid = object.kid ?? "";
    message.payload = object.payload ?? Buffer.alloc(0);
    message.signature = object.signature ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseFireflyIdentity(): FireflyIdentity {
  return { secret: Buffer.alloc(0), public: Buffer.alloc(0), credential: Buffer.alloc(0) };
}

export const FireflyIdentity: MessageFns<FireflyIdentity> = {
  encode(message: FireflyIdentity, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.secret.length !== 0) {
      writer.uint32(18).bytes(message.secret);
    }
    if (message.public.length !== 0) {
      writer.uint32(26).bytes(message.public);
    }
    if (message.credential.length !== 0) {
      writer.uint32(34).bytes(message.credential);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FireflyIdentity {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFireflyIdentity();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.secret = Buffer.from(reader.bytes());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.public = Buffer.from(reader.bytes());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.credential = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): FireflyIdentity {
    return {
      secret: isSet(object.secret) ? Buffer.from(bytesFromBase64(object.secret)) : Buffer.alloc(0),
      public: isSet(object.public) ? Buffer.from(bytesFromBase64(object.public)) : Buffer.alloc(0),
      credential: isSet(object.credential) ? Buffer.from(bytesFromBase64(object.credential)) : Buffer.alloc(0),
    };
  },

  toJSON(message: FireflyIdentity): unknown {
    const obj: any = {};
    if (message.secret.length !== 0) {
      obj.secret = base64FromBytes(message.secret);
    }
    if (message.public.length !== 0) {
      obj.public = base64FromBytes(message.public);
    }
    if (message.credential.length !== 0) {
      obj.credential = base64FromBytes(message.credential);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FireflyIdentity>, I>>(base?: I): FireflyIdentity {
    return FireflyIdentity.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FireflyIdentity>, I>>(object: I): FireflyIdentity {
    const message = createBaseFireflyIdentity();
    message.secret = object.secret ?? Buffer.alloc(0);
    message.public = object.public ?? Buffer.alloc(0);
    message.credential = object.credential ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseFireflyGroupExtension(): FireflyGroupExtension {
  return { name: "", roles: [], channels: [], members: [], defaultPermissions: 0 };
}

export const FireflyGroupExtension: MessageFns<FireflyGroupExtension> = {
  encode(message: FireflyGroupExtension, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.name !== "") {
      writer.uint32(10).string(message.name);
    }
    for (const v of message.roles) {
      FireflyGroupRole.encode(v!, writer.uint32(18).fork()).join();
    }
    for (const v of message.channels) {
      FireflyGroupChannel.encode(v!, writer.uint32(26).fork()).join();
    }
    for (const v of message.members) {
      FireflyGroupMember.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.defaultPermissions !== 0) {
      writer.uint32(45).fixed32(message.defaultPermissions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FireflyGroupExtension {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFireflyGroupExtension();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.roles.push(FireflyGroupRole.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.channels.push(FireflyGroupChannel.decode(reader, reader.uint32()));
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.members.push(FireflyGroupMember.decode(reader, reader.uint32()));
            continue;
          }
          case 5: {
            if (tag !== 45) {
              break;
            }

            message.defaultPermissions = reader.fixed32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): FireflyGroupExtension {
    return {
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => FireflyGroupRole.fromJSON(e)) : [],
      channels: globalThis.Array.isArray(object?.channels)
        ? object.channels.map((e: any) => FireflyGroupChannel.fromJSON(e))
        : [],
      members: globalThis.Array.isArray(object?.members)
        ? object.members.map((e: any) => FireflyGroupMember.fromJSON(e))
        : [],
      defaultPermissions: isSet(object.defaultPermissions)
        ? globalThis.Number(object.defaultPermissions)
        : isSet(object.default_permissions)
        ? globalThis.Number(object.default_permissions)
        : 0,
    };
  },

  toJSON(message: FireflyGroupExtension): unknown {
    const obj: any = {};
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.roles?.length) {
      obj.roles = message.roles.map((e) => FireflyGroupRole.toJSON(e));
    }
    if (message.channels?.length) {
      obj.channels = message.channels.map((e) => FireflyGroupChannel.toJSON(e));
    }
    if (message.members?.length) {
      obj.members = message.members.map((e) => FireflyGroupMember.toJSON(e));
    }
    if (message.defaultPermissions !== 0) {
      obj.defaultPermissions = Math.round(message.defaultPermissions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FireflyGroupExtension>, I>>(base?: I): FireflyGroupExtension {
    return FireflyGroupExtension.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FireflyGroupExtension>, I>>(object: I): FireflyGroupExtension {
    const message = createBaseFireflyGroupExtension();
    message.name = object.name ?? "";
    message.roles = object.roles?.map((e) => FireflyGroupRole.fromPartial(e)) || [];
    message.channels = object.channels?.map((e) => FireflyGroupChannel.fromPartial(e)) || [];
    message.members = object.members?.map((e) => FireflyGroupMember.fromPartial(e)) || [];
    message.defaultPermissions = object.defaultPermissions ?? 0;
    return message;
  },
};

function createBaseFireflyGroupRole(): FireflyGroupRole {
  return { id: 0, name: "", permissions: 0, color: 0 };
}

export const FireflyGroupRole: MessageFns<FireflyGroupRole> = {
  encode(message: FireflyGroupRole, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.permissions !== 0) {
      writer.uint32(29).fixed32(message.permissions);
    }
    if (message.color !== 0) {
      writer.uint32(32).uint32(message.color);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FireflyGroupRole {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFireflyGroupRole();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 29) {
              break;
            }

            message.permissions = reader.fixed32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.color = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): FireflyGroupRole {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      permissions: isSet(object.permissions) ? globalThis.Number(object.permissions) : 0,
      color: isSet(object.color) ? globalThis.Number(object.color) : 0,
    };
  },

  toJSON(message: FireflyGroupRole): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.permissions !== 0) {
      obj.permissions = Math.round(message.permissions);
    }
    if (message.color !== 0) {
      obj.color = Math.round(message.color);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FireflyGroupRole>, I>>(base?: I): FireflyGroupRole {
    return FireflyGroupRole.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FireflyGroupRole>, I>>(object: I): FireflyGroupRole {
    const message = createBaseFireflyGroupRole();
    message.id = object.id ?? 0;
    message.name = object.name ?? "";
    message.permissions = object.permissions ?? 0;
    message.color = object.color ?? 0;
    return message;
  },
};

function createBaseFireflyGroupMember(): FireflyGroupMember {
  return { username: "", role: 0 };
}

export const FireflyGroupMember: MessageFns<FireflyGroupMember> = {
  encode(message: FireflyGroupMember, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.username !== "") {
      writer.uint32(10).string(message.username);
    }
    if (message.role !== 0) {
      writer.uint32(16).uint32(message.role);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FireflyGroupMember {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFireflyGroupMember();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.role = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): FireflyGroupMember {
    return {
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      role: isSet(object.role) ? globalThis.Number(object.role) : 0,
    };
  },

  toJSON(message: FireflyGroupMember): unknown {
    const obj: any = {};
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.role !== 0) {
      obj.role = Math.round(message.role);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FireflyGroupMember>, I>>(base?: I): FireflyGroupMember {
    return FireflyGroupMember.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FireflyGroupMember>, I>>(object: I): FireflyGroupMember {
    const message = createBaseFireflyGroupMember();
    message.username = object.username ?? "";
    message.role = object.role ?? 0;
    return message;
  },
};

function createBaseFireflyGroupChannel(): FireflyGroupChannel {
  return { id: 0, name: "", type: 0, roles: [], defaultPermissions: 0 };
}

export const FireflyGroupChannel: MessageFns<FireflyGroupChannel> = {
  encode(message: FireflyGroupChannel, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.type !== 0) {
      writer.uint32(24).uint32(message.type);
    }
    for (const v of message.roles) {
      FireflyGroupRole.encode(v!, writer.uint32(34).fork()).join();
    }
    if (message.defaultPermissions !== 0) {
      writer.uint32(45).fixed32(message.defaultPermissions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FireflyGroupChannel {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFireflyGroupChannel();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.name = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.type = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.roles.push(FireflyGroupRole.decode(reader, reader.uint32()));
            continue;
          }
          case 5: {
            if (tag !== 45) {
              break;
            }

            message.defaultPermissions = reader.fixed32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): FireflyGroupChannel {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      type: isSet(object.type) ? globalThis.Number(object.type) : 0,
      roles: globalThis.Array.isArray(object?.roles) ? object.roles.map((e: any) => FireflyGroupRole.fromJSON(e)) : [],
      defaultPermissions: isSet(object.defaultPermissions)
        ? globalThis.Number(object.defaultPermissions)
        : isSet(object.default_permissions)
        ? globalThis.Number(object.default_permissions)
        : 0,
    };
  },

  toJSON(message: FireflyGroupChannel): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.type !== 0) {
      obj.type = Math.round(message.type);
    }
    if (message.roles?.length) {
      obj.roles = message.roles.map((e) => FireflyGroupRole.toJSON(e));
    }
    if (message.defaultPermissions !== 0) {
      obj.defaultPermissions = Math.round(message.defaultPermissions);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<FireflyGroupChannel>, I>>(base?: I): FireflyGroupChannel {
    return FireflyGroupChannel.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FireflyGroupChannel>, I>>(object: I): FireflyGroupChannel {
    const message = createBaseFireflyGroupChannel();
    message.id = object.id ?? 0;
    message.name = object.name ?? "";
    message.type = object.type ?? 0;
    message.roles = object.roles?.map((e) => FireflyGroupRole.fromPartial(e)) || [];
    message.defaultPermissions = object.defaultPermissions ?? 0;
    return message;
  },
};

function createBasePreKeyBundle(): PreKeyBundle {
  return {
    registrationId: 0,
    deviceId: 0,
    preKeyId: 0,
    prePublicKey: Buffer.alloc(0),
    signedPreKeyId: 0,
    signedPrePublicKey: Buffer.alloc(0),
    signedPreKeySignature: Buffer.alloc(0),
    identityPublicKey: Buffer.alloc(0),
    KEMPreKeyId: 0,
    KEMPrePublicKey: Buffer.alloc(0),
    KEMPreKeySignature: Buffer.alloc(0),
  };
}

export const PreKeyBundle: MessageFns<PreKeyBundle> = {
  encode(message: PreKeyBundle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.registrationId !== 0) {
      writer.uint32(8).uint32(message.registrationId);
    }
    if (message.deviceId !== 0) {
      writer.uint32(16).uint32(message.deviceId);
    }
    if (message.preKeyId !== 0) {
      writer.uint32(24).uint32(message.preKeyId);
    }
    if (message.prePublicKey.length !== 0) {
      writer.uint32(34).bytes(message.prePublicKey);
    }
    if (message.signedPreKeyId !== 0) {
      writer.uint32(40).uint32(message.signedPreKeyId);
    }
    if (message.signedPrePublicKey.length !== 0) {
      writer.uint32(50).bytes(message.signedPrePublicKey);
    }
    if (message.signedPreKeySignature.length !== 0) {
      writer.uint32(58).bytes(message.signedPreKeySignature);
    }
    if (message.identityPublicKey.length !== 0) {
      writer.uint32(66).bytes(message.identityPublicKey);
    }
    if (message.KEMPreKeyId !== 0) {
      writer.uint32(72).uint32(message.KEMPreKeyId);
    }
    if (message.KEMPrePublicKey.length !== 0) {
      writer.uint32(82).bytes(message.KEMPrePublicKey);
    }
    if (message.KEMPreKeySignature.length !== 0) {
      writer.uint32(90).bytes(message.KEMPreKeySignature);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreKeyBundle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePreKeyBundle();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.registrationId = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.deviceId = reader.uint32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.preKeyId = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.prePublicKey = Buffer.from(reader.bytes());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.signedPreKeyId = reader.uint32();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.signedPrePublicKey = Buffer.from(reader.bytes());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.signedPreKeySignature = Buffer.from(reader.bytes());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.identityPublicKey = Buffer.from(reader.bytes());
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.KEMPreKeyId = reader.uint32();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.KEMPrePublicKey = Buffer.from(reader.bytes());
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.KEMPreKeySignature = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PreKeyBundle {
    return {
      registrationId: isSet(object.registrationId) ? globalThis.Number(object.registrationId) : 0,
      deviceId: isSet(object.deviceId) ? globalThis.Number(object.deviceId) : 0,
      preKeyId: isSet(object.preKeyId) ? globalThis.Number(object.preKeyId) : 0,
      prePublicKey: isSet(object.prePublicKey) ? Buffer.from(bytesFromBase64(object.prePublicKey)) : Buffer.alloc(0),
      signedPreKeyId: isSet(object.signedPreKeyId) ? globalThis.Number(object.signedPreKeyId) : 0,
      signedPrePublicKey: isSet(object.signedPrePublicKey)
        ? Buffer.from(bytesFromBase64(object.signedPrePublicKey))
        : Buffer.alloc(0),
      signedPreKeySignature: isSet(object.signedPreKeySignature)
        ? Buffer.from(bytesFromBase64(object.signedPreKeySignature))
        : Buffer.alloc(0),
      identityPublicKey: isSet(object.identityPublicKey)
        ? Buffer.from(bytesFromBase64(object.identityPublicKey))
        : Buffer.alloc(0),
      KEMPreKeyId: isSet(object.KEMPreKeyId) ? globalThis.Number(object.KEMPreKeyId) : 0,
      KEMPrePublicKey: isSet(object.KEMPrePublicKey)
        ? Buffer.from(bytesFromBase64(object.KEMPrePublicKey))
        : Buffer.alloc(0),
      KEMPreKeySignature: isSet(object.KEMPreKeySignature)
        ? Buffer.from(bytesFromBase64(object.KEMPreKeySignature))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: PreKeyBundle): unknown {
    const obj: any = {};
    if (message.registrationId !== 0) {
      obj.registrationId = Math.round(message.registrationId);
    }
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    if (message.preKeyId !== 0) {
      obj.preKeyId = Math.round(message.preKeyId);
    }
    if (message.prePublicKey.length !== 0) {
      obj.prePublicKey = base64FromBytes(message.prePublicKey);
    }
    if (message.signedPreKeyId !== 0) {
      obj.signedPreKeyId = Math.round(message.signedPreKeyId);
    }
    if (message.signedPrePublicKey.length !== 0) {
      obj.signedPrePublicKey = base64FromBytes(message.signedPrePublicKey);
    }
    if (message.signedPreKeySignature.length !== 0) {
      obj.signedPreKeySignature = base64FromBytes(message.signedPreKeySignature);
    }
    if (message.identityPublicKey.length !== 0) {
      obj.identityPublicKey = base64FromBytes(message.identityPublicKey);
    }
    if (message.KEMPreKeyId !== 0) {
      obj.KEMPreKeyId = Math.round(message.KEMPreKeyId);
    }
    if (message.KEMPrePublicKey.length !== 0) {
      obj.KEMPrePublicKey = base64FromBytes(message.KEMPrePublicKey);
    }
    if (message.KEMPreKeySignature.length !== 0) {
      obj.KEMPreKeySignature = base64FromBytes(message.KEMPreKeySignature);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreKeyBundle>, I>>(base?: I): PreKeyBundle {
    return PreKeyBundle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreKeyBundle>, I>>(object: I): PreKeyBundle {
    const message = createBasePreKeyBundle();
    message.registrationId = object.registrationId ?? 0;
    message.deviceId = object.deviceId ?? 0;
    message.preKeyId = object.preKeyId ?? 0;
    message.prePublicKey = object.prePublicKey ?? Buffer.alloc(0);
    message.signedPreKeyId = object.signedPreKeyId ?? 0;
    message.signedPrePublicKey = object.signedPrePublicKey ?? Buffer.alloc(0);
    message.signedPreKeySignature = object.signedPreKeySignature ?? Buffer.alloc(0);
    message.identityPublicKey = object.identityPublicKey ?? Buffer.alloc(0);
    message.KEMPreKeyId = object.KEMPreKeyId ?? 0;
    message.KEMPrePublicKey = object.KEMPrePublicKey ?? Buffer.alloc(0);
    message.KEMPreKeySignature = object.KEMPreKeySignature ?? Buffer.alloc(0);
    return message;
  },
};

function createBasePreKeyBundleEntry(): PreKeyBundleEntry {
  return { id: 0, address: 0n, bundle: undefined, username: "", deviceId: 0 };
}

export const PreKeyBundleEntry: MessageFns<PreKeyBundleEntry> = {
  encode(message: PreKeyBundleEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0) {
      writer.uint32(8).uint32(message.id);
    }
    if (message.address !== 0n) {
      if (BigInt.asUintN(64, message.address) !== message.address) {
        throw new globalThis.Error("value provided for field message.address of type uint64 too large");
      }
      writer.uint32(16).uint64(message.address);
    }
    if (message.bundle !== undefined) {
      PreKeyBundle.encode(message.bundle, writer.uint32(26).fork()).join();
    }
    if (message.username !== "") {
      writer.uint32(34).string(message.username);
    }
    if (message.deviceId !== 0) {
      writer.uint32(40).uint32(message.deviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreKeyBundleEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePreKeyBundleEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.address = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.bundle = PreKeyBundle.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.deviceId = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PreKeyBundleEntry {
    return {
      id: isSet(object.id) ? globalThis.Number(object.id) : 0,
      address: isSet(object.address) ? BigInt(object.address) : 0n,
      bundle: isSet(object.bundle) ? PreKeyBundle.fromJSON(object.bundle) : undefined,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      deviceId: isSet(object.deviceId)
        ? globalThis.Number(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.Number(object.device_id)
        : 0,
    };
  },

  toJSON(message: PreKeyBundleEntry): unknown {
    const obj: any = {};
    if (message.id !== 0) {
      obj.id = Math.round(message.id);
    }
    if (message.address !== 0n) {
      obj.address = message.address.toString();
    }
    if (message.bundle !== undefined) {
      obj.bundle = PreKeyBundle.toJSON(message.bundle);
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.deviceId !== 0) {
      obj.deviceId = Math.round(message.deviceId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreKeyBundleEntry>, I>>(base?: I): PreKeyBundleEntry {
    return PreKeyBundleEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreKeyBundleEntry>, I>>(object: I): PreKeyBundleEntry {
    const message = createBasePreKeyBundleEntry();
    message.id = object.id ?? 0;
    message.address = (object.address !== undefined && object.address !== null) ? BigInt(object.address) : 0n;
    message.bundle = (object.bundle !== undefined && object.bundle !== null)
      ? PreKeyBundle.fromPartial(object.bundle)
      : undefined;
    message.username = object.username ?? "";
    message.deviceId = object.deviceId ?? 0;
    return message;
  },
};

function createBasePreKeyBundleEntries(): PreKeyBundleEntries {
  return { entries: [] };
}

export const PreKeyBundleEntries: MessageFns<PreKeyBundleEntries> = {
  encode(message: PreKeyBundleEntries, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      PreKeyBundleEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreKeyBundleEntries {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePreKeyBundleEntries();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.entries.push(PreKeyBundleEntry.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PreKeyBundleEntries {
    return {
      entries: globalThis.Array.isArray(object?.entries)
        ? object.entries.map((e: any) => PreKeyBundleEntry.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PreKeyBundleEntries): unknown {
    const obj: any = {};
    if (message.entries?.length) {
      obj.entries = message.entries.map((e) => PreKeyBundleEntry.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreKeyBundleEntries>, I>>(base?: I): PreKeyBundleEntries {
    return PreKeyBundleEntries.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreKeyBundleEntries>, I>>(object: I): PreKeyBundleEntries {
    const message = createBasePreKeyBundleEntries();
    message.entries = object.entries?.map((e) => PreKeyBundleEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseConversationStart(): ConversationStart {
  return { conversationId: 0n, startedBy: "", other: "", bundle: undefined };
}

export const ConversationStart: MessageFns<ConversationStart> = {
  encode(message: ConversationStart, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.conversationId !== 0n) {
      if (BigInt.asUintN(64, message.conversationId) !== message.conversationId) {
        throw new globalThis.Error("value provided for field message.conversationId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.conversationId);
    }
    if (message.startedBy !== "") {
      writer.uint32(18).string(message.startedBy);
    }
    if (message.other !== "") {
      writer.uint32(26).string(message.other);
    }
    if (message.bundle !== undefined) {
      PreKeyBundle.encode(message.bundle, writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ConversationStart {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConversationStart();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.conversationId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.startedBy = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.other = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.bundle = PreKeyBundle.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ConversationStart {
    return {
      conversationId: isSet(object.conversationId) ? BigInt(object.conversationId) : 0n,
      startedBy: isSet(object.startedBy)
        ? globalThis.String(object.startedBy)
        : isSet(object.started_by)
        ? globalThis.String(object.started_by)
        : "",
      other: isSet(object.other) ? globalThis.String(object.other) : "",
      bundle: isSet(object.bundle) ? PreKeyBundle.fromJSON(object.bundle) : undefined,
    };
  },

  toJSON(message: ConversationStart): unknown {
    const obj: any = {};
    if (message.conversationId !== 0n) {
      obj.conversationId = message.conversationId.toString();
    }
    if (message.startedBy !== "") {
      obj.startedBy = message.startedBy;
    }
    if (message.other !== "") {
      obj.other = message.other;
    }
    if (message.bundle !== undefined) {
      obj.bundle = PreKeyBundle.toJSON(message.bundle);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ConversationStart>, I>>(base?: I): ConversationStart {
    return ConversationStart.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ConversationStart>, I>>(object: I): ConversationStart {
    const message = createBaseConversationStart();
    message.conversationId = (object.conversationId !== undefined && object.conversationId !== null)
      ? BigInt(object.conversationId)
      : 0n;
    message.startedBy = object.startedBy ?? "";
    message.other = object.other ?? "";
    message.bundle = (object.bundle !== undefined && object.bundle !== null)
      ? PreKeyBundle.fromPartial(object.bundle)
      : undefined;
    return message;
  },
};

function createBasePreKeyBundles(): PreKeyBundles {
  return { bundles: [] };
}

export const PreKeyBundles: MessageFns<PreKeyBundles> = {
  encode(message: PreKeyBundles, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.bundles) {
      PreKeyBundle.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PreKeyBundles {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePreKeyBundles();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.bundles.push(PreKeyBundle.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PreKeyBundles {
    return {
      bundles: globalThis.Array.isArray(object?.bundles)
        ? object.bundles.map((e: any) => PreKeyBundle.fromJSON(e))
        : [],
    };
  },

  toJSON(message: PreKeyBundles): unknown {
    const obj: any = {};
    if (message.bundles?.length) {
      obj.bundles = message.bundles.map((e) => PreKeyBundle.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PreKeyBundles>, I>>(base?: I): PreKeyBundles {
    return PreKeyBundles.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PreKeyBundles>, I>>(object: I): PreKeyBundles {
    const message = createBasePreKeyBundles();
    message.bundles = object.bundles?.map((e) => PreKeyBundle.fromPartial(e)) || [];
    return message;
  },
};

function createBaseConversation(): Conversation {
  return { user1: "", user2: "", settings: 0n };
}

export const Conversation: MessageFns<Conversation> = {
  encode(message: Conversation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.user1 !== "") {
      writer.uint32(10).string(message.user1);
    }
    if (message.user2 !== "") {
      writer.uint32(18).string(message.user2);
    }
    if (message.settings !== 0n) {
      if (BigInt.asUintN(64, message.settings) !== message.settings) {
        throw new globalThis.Error("value provided for field message.settings of type uint64 too large");
      }
      writer.uint32(24).uint64(message.settings);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Conversation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConversation();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.user1 = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.user2 = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.settings = reader.uint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Conversation {
    return {
      user1: isSet(object.user1) ? globalThis.String(object.user1) : "",
      user2: isSet(object.user2) ? globalThis.String(object.user2) : "",
      settings: isSet(object.settings) ? BigInt(object.settings) : 0n,
    };
  },

  toJSON(message: Conversation): unknown {
    const obj: any = {};
    if (message.user1 !== "") {
      obj.user1 = message.user1;
    }
    if (message.user2 !== "") {
      obj.user2 = message.user2;
    }
    if (message.settings !== 0n) {
      obj.settings = message.settings.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Conversation>, I>>(base?: I): Conversation {
    return Conversation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Conversation>, I>>(object: I): Conversation {
    const message = createBaseConversation();
    message.user1 = object.user1 ?? "";
    message.user2 = object.user2 ?? "";
    message.settings = (object.settings !== undefined && object.settings !== null) ? BigInt(object.settings) : 0n;
    return message;
  },
};

function createBaseConversations(): Conversations {
  return { conversations: [] };
}

export const Conversations: MessageFns<Conversations> = {
  encode(message: Conversations, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.conversations) {
      Conversation.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Conversations {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseConversations();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.conversations.push(Conversation.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Conversations {
    return {
      conversations: globalThis.Array.isArray(object?.conversations)
        ? object.conversations.map((e: any) => Conversation.fromJSON(e))
        : [],
    };
  },

  toJSON(message: Conversations): unknown {
    const obj: any = {};
    if (message.conversations?.length) {
      obj.conversations = message.conversations.map((e) => Conversation.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Conversations>, I>>(base?: I): Conversations {
    return Conversations.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Conversations>, I>>(object: I): Conversations {
    const message = createBaseConversations();
    message.conversations = object.conversations?.map((e) => Conversation.fromPartial(e)) || [];
    return message;
  },
};

function createBaseEncryptedFile(): EncryptedFile {
  return { url: "", secretKey: Buffer.alloc(0), contentType: 0, contentLength: 0 };
}

export const EncryptedFile: MessageFns<EncryptedFile> = {
  encode(message: EncryptedFile, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.url !== "") {
      writer.uint32(10).string(message.url);
    }
    if (message.secretKey.length !== 0) {
      writer.uint32(26).bytes(message.secretKey);
    }
    if (message.contentType !== 0) {
      writer.uint32(16).uint32(message.contentType);
    }
    if (message.contentLength !== 0) {
      writer.uint32(32).uint32(message.contentLength);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EncryptedFile {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEncryptedFile();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.url = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.secretKey = Buffer.from(reader.bytes());
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.contentType = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.contentLength = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EncryptedFile {
    return {
      url: isSet(object.url) ? globalThis.String(object.url) : "",
      secretKey: isSet(object.secretKey) ? Buffer.from(bytesFromBase64(object.secretKey)) : Buffer.alloc(0),
      contentType: isSet(object.contentType) ? globalThis.Number(object.contentType) : 0,
      contentLength: isSet(object.contentLength) ? globalThis.Number(object.contentLength) : 0,
    };
  },

  toJSON(message: EncryptedFile): unknown {
    const obj: any = {};
    if (message.url !== "") {
      obj.url = message.url;
    }
    if (message.secretKey.length !== 0) {
      obj.secretKey = base64FromBytes(message.secretKey);
    }
    if (message.contentType !== 0) {
      obj.contentType = Math.round(message.contentType);
    }
    if (message.contentLength !== 0) {
      obj.contentLength = Math.round(message.contentLength);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EncryptedFile>, I>>(base?: I): EncryptedFile {
    return EncryptedFile.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EncryptedFile>, I>>(object: I): EncryptedFile {
    const message = createBaseEncryptedFile();
    message.url = object.url ?? "";
    message.secretKey = object.secretKey ?? Buffer.alloc(0);
    message.contentType = object.contentType ?? 0;
    message.contentLength = object.contentLength ?? 0;
    return message;
  },
};

function createBaseEncryptedFiles(): EncryptedFiles {
  return { files: [] };
}

export const EncryptedFiles: MessageFns<EncryptedFiles> = {
  encode(message: EncryptedFiles, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.files) {
      EncryptedFile.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EncryptedFiles {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEncryptedFiles();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.files.push(EncryptedFile.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EncryptedFiles {
    return {
      files: globalThis.Array.isArray(object?.files) ? object.files.map((e: any) => EncryptedFile.fromJSON(e)) : [],
    };
  },

  toJSON(message: EncryptedFiles): unknown {
    const obj: any = {};
    if (message.files?.length) {
      obj.files = message.files.map((e) => EncryptedFile.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EncryptedFiles>, I>>(base?: I): EncryptedFiles {
    return EncryptedFiles.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EncryptedFiles>, I>>(object: I): EncryptedFiles {
    const message = createBaseEncryptedFiles();
    message.files = object.files?.map((e) => EncryptedFile.fromPartial(e)) || [];
    return message;
  },
};

function createBaseMessagePayload(): MessagePayload {
  return { text: "", files: undefined, editedOf: undefined, replyingTo: undefined, deleted: undefined, messageType: 0 };
}

export const MessagePayload: MessageFns<MessagePayload> = {
  encode(message: MessagePayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.text !== "") {
      writer.uint32(10).string(message.text);
    }
    if (message.files !== undefined) {
      EncryptedFiles.encode(message.files, writer.uint32(26).fork()).join();
    }
    if (message.editedOf !== undefined) {
      if (BigInt.asUintN(64, message.editedOf) !== message.editedOf) {
        throw new globalThis.Error("value provided for field message.editedOf of type fixed64 too large");
      }
      writer.uint32(33).fixed64(message.editedOf);
    }
    if (message.replyingTo !== undefined) {
      if (BigInt.asUintN(64, message.replyingTo) !== message.replyingTo) {
        throw new globalThis.Error("value provided for field message.replyingTo of type fixed64 too large");
      }
      writer.uint32(41).fixed64(message.replyingTo);
    }
    if (message.deleted !== undefined) {
      if (BigInt.asUintN(64, message.deleted) !== message.deleted) {
        throw new globalThis.Error("value provided for field message.deleted of type fixed64 too large");
      }
      writer.uint32(49).fixed64(message.deleted);
    }
    if (message.messageType !== 0) {
      writer.uint32(56).uint32(message.messageType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MessagePayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMessagePayload();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.text = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.files = EncryptedFiles.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.editedOf = reader.fixed64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 41) {
              break;
            }

            message.replyingTo = reader.fixed64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 49) {
              break;
            }

            message.deleted = reader.fixed64() as bigint;
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.messageType = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): MessagePayload {
    return {
      text: isSet(object.text) ? globalThis.String(object.text) : "",
      files: isSet(object.files) ? EncryptedFiles.fromJSON(object.files) : undefined,
      editedOf: isSet(object.editedOf) ? BigInt(object.editedOf) : undefined,
      replyingTo: isSet(object.replyingTo) ? BigInt(object.replyingTo) : undefined,
      deleted: isSet(object.deleted) ? BigInt(object.deleted) : undefined,
      messageType: isSet(object.messageType)
        ? globalThis.Number(object.messageType)
        : isSet(object.message_type)
        ? globalThis.Number(object.message_type)
        : 0,
    };
  },

  toJSON(message: MessagePayload): unknown {
    const obj: any = {};
    if (message.text !== "") {
      obj.text = message.text;
    }
    if (message.files !== undefined) {
      obj.files = EncryptedFiles.toJSON(message.files);
    }
    if (message.editedOf !== undefined) {
      obj.editedOf = message.editedOf.toString();
    }
    if (message.replyingTo !== undefined) {
      obj.replyingTo = message.replyingTo.toString();
    }
    if (message.deleted !== undefined) {
      obj.deleted = message.deleted.toString();
    }
    if (message.messageType !== 0) {
      obj.messageType = Math.round(message.messageType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<MessagePayload>, I>>(base?: I): MessagePayload {
    return MessagePayload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MessagePayload>, I>>(object: I): MessagePayload {
    const message = createBaseMessagePayload();
    message.text = object.text ?? "";
    message.files = (object.files !== undefined && object.files !== null)
      ? EncryptedFiles.fromPartial(object.files)
      : undefined;
    message.editedOf = (object.editedOf !== undefined && object.editedOf !== null)
      ? BigInt(object.editedOf)
      : undefined;
    message.replyingTo = (object.replyingTo !== undefined && object.replyingTo !== null)
      ? BigInt(object.replyingTo)
      : undefined;
    message.deleted = (object.deleted !== undefined && object.deleted !== null) ? BigInt(object.deleted) : undefined;
    message.messageType = object.messageType ?? 0;
    return message;
  },
};

function createBaseCallMessage(): CallMessage {
  return { message: Buffer.alloc(0), type: 0, jsonBody: "", sessionId: 0 };
}

export const CallMessage: MessageFns<CallMessage> = {
  encode(message: CallMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.message.length !== 0) {
      writer.uint32(10).bytes(message.message);
    }
    if (message.type !== 0) {
      writer.uint32(24).int32(message.type);
    }
    if (message.jsonBody !== "") {
      writer.uint32(34).string(message.jsonBody);
    }
    if (message.sessionId !== 0) {
      writer.uint32(16).uint32(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCallMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.message = Buffer.from(reader.bytes());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.type = reader.int32() as any;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.jsonBody = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.sessionId = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CallMessage {
    return {
      message: isSet(object.message) ? Buffer.from(bytesFromBase64(object.message)) : Buffer.alloc(0),
      type: isSet(object.type) ? callMessageTypeFromJSON(object.type) : 0,
      jsonBody: isSet(object.jsonBody) ? globalThis.String(object.jsonBody) : "",
      sessionId: isSet(object.sessionId) ? globalThis.Number(object.sessionId) : 0,
    };
  },

  toJSON(message: CallMessage): unknown {
    const obj: any = {};
    if (message.message.length !== 0) {
      obj.message = base64FromBytes(message.message);
    }
    if (message.type !== 0) {
      obj.type = callMessageTypeToJSON(message.type);
    }
    if (message.jsonBody !== "") {
      obj.jsonBody = message.jsonBody;
    }
    if (message.sessionId !== 0) {
      obj.sessionId = Math.round(message.sessionId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CallMessage>, I>>(base?: I): CallMessage {
    return CallMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallMessage>, I>>(object: I): CallMessage {
    const message = createBaseCallMessage();
    message.message = object.message ?? Buffer.alloc(0);
    message.type = object.type ?? 0;
    message.jsonBody = object.jsonBody ?? "";
    message.sessionId = object.sessionId ?? 0;
    return message;
  },
};

function createBaseSelfUserMessage(): SelfUserMessage {
  return { to: "", inner: Buffer.alloc(0) };
}

export const SelfUserMessage: MessageFns<SelfUserMessage> = {
  encode(message: SelfUserMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.to !== "") {
      writer.uint32(10).string(message.to);
    }
    if (message.inner.length !== 0) {
      writer.uint32(18).bytes(message.inner);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SelfUserMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSelfUserMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.to = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.inner = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): SelfUserMessage {
    return {
      to: isSet(object.to) ? globalThis.String(object.to) : "",
      inner: isSet(object.inner) ? Buffer.from(bytesFromBase64(object.inner)) : Buffer.alloc(0),
    };
  },

  toJSON(message: SelfUserMessage): unknown {
    const obj: any = {};
    if (message.to !== "") {
      obj.to = message.to;
    }
    if (message.inner.length !== 0) {
      obj.inner = base64FromBytes(message.inner);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<SelfUserMessage>, I>>(base?: I): SelfUserMessage {
    return SelfUserMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SelfUserMessage>, I>>(object: I): SelfUserMessage {
    const message = createBaseSelfUserMessage();
    message.to = object.to ?? "";
    message.inner = object.inner ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseReaction(): Reaction {
  return { reactingTo: 0n, reaction: 0 };
}

export const Reaction: MessageFns<Reaction> = {
  encode(message: Reaction, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reactingTo !== 0n) {
      if (BigInt.asUintN(64, message.reactingTo) !== message.reactingTo) {
        throw new globalThis.Error("value provided for field message.reactingTo of type uint64 too large");
      }
      writer.uint32(8).uint64(message.reactingTo);
    }
    if (message.reaction !== 0) {
      writer.uint32(16).uint32(message.reaction);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Reaction {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseReaction();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.reactingTo = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.reaction = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): Reaction {
    return {
      reactingTo: isSet(object.reactingTo)
        ? BigInt(object.reactingTo)
        : isSet(object.reacting_to)
        ? BigInt(object.reacting_to)
        : 0n,
      reaction: isSet(object.reaction) ? globalThis.Number(object.reaction) : 0,
    };
  },

  toJSON(message: Reaction): unknown {
    const obj: any = {};
    if (message.reactingTo !== 0n) {
      obj.reactingTo = message.reactingTo.toString();
    }
    if (message.reaction !== 0) {
      obj.reaction = Math.round(message.reaction);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Reaction>, I>>(base?: I): Reaction {
    return Reaction.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Reaction>, I>>(object: I): Reaction {
    const message = createBaseReaction();
    message.reactingTo = (object.reactingTo !== undefined && object.reactingTo !== null)
      ? BigInt(object.reactingTo)
      : 0n;
    message.reaction = object.reaction ?? 0;
    return message;
  },
};

function createBaseUserMessageInner(): UserMessageInner {
  return {
    plainText: undefined,
    callMessage: undefined,
    messagePayload: undefined,
    selfMessage: undefined,
    reaction: undefined,
    backupKeySync: undefined,
    directPinSnapshot: undefined,
    nonce: 0,
    messageType: 0,
  };
}

export const UserMessageInner: MessageFns<UserMessageInner> = {
  encode(message: UserMessageInner, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.plainText !== undefined) {
      writer.uint32(10).bytes(message.plainText);
    }
    if (message.callMessage !== undefined) {
      CallMessage.encode(message.callMessage, writer.uint32(18).fork()).join();
    }
    if (message.messagePayload !== undefined) {
      MessagePayload.encode(message.messagePayload, writer.uint32(26).fork()).join();
    }
    if (message.selfMessage !== undefined) {
      SelfUserMessage.encode(message.selfMessage, writer.uint32(34).fork()).join();
    }
    if (message.reaction !== undefined) {
      Reaction.encode(message.reaction, writer.uint32(50).fork()).join();
    }
    if (message.backupKeySync !== undefined) {
      DirectBackupKeySync.encode(message.backupKeySync, writer.uint32(58).fork()).join();
    }
    if (message.directPinSnapshot !== undefined) {
      DirectPinSnapshotSecret.encode(message.directPinSnapshot, writer.uint32(66).fork()).join();
    }
    if (message.nonce !== 0) {
      writer.uint32(85).fixed32(message.nonce);
    }
    if (message.messageType !== 0) {
      writer.uint32(40).uint32(message.messageType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserMessageInner {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseUserMessageInner();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.plainText = Buffer.from(reader.bytes());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.callMessage = CallMessage.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.messagePayload = MessagePayload.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.selfMessage = SelfUserMessage.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.reaction = Reaction.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.backupKeySync = DirectBackupKeySync.decode(reader, reader.uint32());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.directPinSnapshot = DirectPinSnapshotSecret.decode(reader, reader.uint32());
            continue;
          }
          case 10: {
            if (tag !== 85) {
              break;
            }

            message.nonce = reader.fixed32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.messageType = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): UserMessageInner {
    return {
      plainText: isSet(object.plainText) ? Buffer.from(bytesFromBase64(object.plainText)) : undefined,
      callMessage: isSet(object.callMessage) ? CallMessage.fromJSON(object.callMessage) : undefined,
      messagePayload: isSet(object.messagePayload) ? MessagePayload.fromJSON(object.messagePayload) : undefined,
      selfMessage: isSet(object.selfMessage) ? SelfUserMessage.fromJSON(object.selfMessage) : undefined,
      reaction: isSet(object.reaction) ? Reaction.fromJSON(object.reaction) : undefined,
      backupKeySync: isSet(object.backupKeySync) ? DirectBackupKeySync.fromJSON(object.backupKeySync) : undefined,
      directPinSnapshot: isSet(object.directPinSnapshot)
        ? DirectPinSnapshotSecret.fromJSON(object.directPinSnapshot)
        : undefined,
      nonce: isSet(object.nonce) ? globalThis.Number(object.nonce) : 0,
      messageType: isSet(object.messageType)
        ? globalThis.Number(object.messageType)
        : isSet(object.message_type)
        ? globalThis.Number(object.message_type)
        : 0,
    };
  },

  toJSON(message: UserMessageInner): unknown {
    const obj: any = {};
    if (message.plainText !== undefined) {
      obj.plainText = base64FromBytes(message.plainText);
    }
    if (message.callMessage !== undefined) {
      obj.callMessage = CallMessage.toJSON(message.callMessage);
    }
    if (message.messagePayload !== undefined) {
      obj.messagePayload = MessagePayload.toJSON(message.messagePayload);
    }
    if (message.selfMessage !== undefined) {
      obj.selfMessage = SelfUserMessage.toJSON(message.selfMessage);
    }
    if (message.reaction !== undefined) {
      obj.reaction = Reaction.toJSON(message.reaction);
    }
    if (message.backupKeySync !== undefined) {
      obj.backupKeySync = DirectBackupKeySync.toJSON(message.backupKeySync);
    }
    if (message.directPinSnapshot !== undefined) {
      obj.directPinSnapshot = DirectPinSnapshotSecret.toJSON(message.directPinSnapshot);
    }
    if (message.nonce !== 0) {
      obj.nonce = Math.round(message.nonce);
    }
    if (message.messageType !== 0) {
      obj.messageType = Math.round(message.messageType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserMessageInner>, I>>(base?: I): UserMessageInner {
    return UserMessageInner.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserMessageInner>, I>>(object: I): UserMessageInner {
    const message = createBaseUserMessageInner();
    message.plainText = object.plainText ?? undefined;
    message.callMessage = (object.callMessage !== undefined && object.callMessage !== null)
      ? CallMessage.fromPartial(object.callMessage)
      : undefined;
    message.messagePayload = (object.messagePayload !== undefined && object.messagePayload !== null)
      ? MessagePayload.fromPartial(object.messagePayload)
      : undefined;
    message.selfMessage = (object.selfMessage !== undefined && object.selfMessage !== null)
      ? SelfUserMessage.fromPartial(object.selfMessage)
      : undefined;
    message.reaction = (object.reaction !== undefined && object.reaction !== null)
      ? Reaction.fromPartial(object.reaction)
      : undefined;
    message.backupKeySync = (object.backupKeySync !== undefined && object.backupKeySync !== null)
      ? DirectBackupKeySync.fromPartial(object.backupKeySync)
      : undefined;
    message.directPinSnapshot = (object.directPinSnapshot !== undefined && object.directPinSnapshot !== null)
      ? DirectPinSnapshotSecret.fromPartial(object.directPinSnapshot)
      : undefined;
    message.nonce = object.nonce ?? 0;
    message.messageType = object.messageType ?? 0;
    return message;
  },
};

function createBaseGroupPinUpdate(): GroupPinUpdate {
  return { messageId: 0n, pinned: false };
}

export const GroupPinUpdate: MessageFns<GroupPinUpdate> = {
  encode(message: GroupPinUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.messageId !== 0n) {
      if (BigInt.asUintN(64, message.messageId) !== message.messageId) {
        throw new globalThis.Error("value provided for field message.messageId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.messageId);
    }
    if (message.pinned !== false) {
      writer.uint32(16).bool(message.pinned);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupPinUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupPinUpdate();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.messageId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.pinned = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupPinUpdate {
    return {
      messageId: isSet(object.messageId)
        ? BigInt(object.messageId)
        : isSet(object.message_id)
        ? BigInt(object.message_id)
        : 0n,
      pinned: isSet(object.pinned) ? globalThis.Boolean(object.pinned) : false,
    };
  },

  toJSON(message: GroupPinUpdate): unknown {
    const obj: any = {};
    if (message.messageId !== 0n) {
      obj.messageId = message.messageId.toString();
    }
    if (message.pinned !== false) {
      obj.pinned = message.pinned;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupPinUpdate>, I>>(base?: I): GroupPinUpdate {
    return GroupPinUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupPinUpdate>, I>>(object: I): GroupPinUpdate {
    const message = createBaseGroupPinUpdate();
    message.messageId = (object.messageId !== undefined && object.messageId !== null) ? BigInt(object.messageId) : 0n;
    message.pinned = object.pinned ?? false;
    return message;
  },
};

function createBaseGroupPinSnapshot(): GroupPinSnapshot {
  return { messages: [] };
}

export const GroupPinSnapshot: MessageFns<GroupPinSnapshot> = {
  encode(message: GroupPinSnapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      GroupHistoryRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupPinSnapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupPinSnapshot();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.messages.push(GroupHistoryRecord.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupPinSnapshot {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => GroupHistoryRecord.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupPinSnapshot): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => GroupHistoryRecord.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupPinSnapshot>, I>>(base?: I): GroupPinSnapshot {
    return GroupPinSnapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupPinSnapshot>, I>>(object: I): GroupPinSnapshot {
    const message = createBaseGroupPinSnapshot();
    message.messages = object.messages?.map((e) => GroupHistoryRecord.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupMessageInner(): GroupMessageInner {
  return {
    channelId: 0,
    messagePayload: undefined,
    pinUpdate: undefined,
    pinSnapshot: undefined,
    syncBundle: undefined,
    reaction: undefined,
    messageType: 0,
  };
}

export const GroupMessageInner: MessageFns<GroupMessageInner> = {
  encode(message: GroupMessageInner, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.channelId !== 0) {
      writer.uint32(8).uint32(message.channelId);
    }
    if (message.messagePayload !== undefined) {
      MessagePayload.encode(message.messagePayload, writer.uint32(18).fork()).join();
    }
    if (message.pinUpdate !== undefined) {
      GroupPinUpdate.encode(message.pinUpdate, writer.uint32(34).fork()).join();
    }
    if (message.pinSnapshot !== undefined) {
      GroupPinSnapshot.encode(message.pinSnapshot, writer.uint32(42).fork()).join();
    }
    if (message.syncBundle !== undefined) {
      GroupSyncBundle.encode(message.syncBundle, writer.uint32(50).fork()).join();
    }
    if (message.reaction !== undefined) {
      Reaction.encode(message.reaction, writer.uint32(58).fork()).join();
    }
    if (message.messageType !== 0) {
      writer.uint32(24).uint32(message.messageType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMessageInner {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMessageInner();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.channelId = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.messagePayload = MessagePayload.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.pinUpdate = GroupPinUpdate.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.pinSnapshot = GroupPinSnapshot.decode(reader, reader.uint32());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.syncBundle = GroupSyncBundle.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.reaction = Reaction.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.messageType = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMessageInner {
    return {
      channelId: isSet(object.channelId) ? globalThis.Number(object.channelId) : 0,
      messagePayload: isSet(object.messagePayload) ? MessagePayload.fromJSON(object.messagePayload) : undefined,
      pinUpdate: isSet(object.pinUpdate) ? GroupPinUpdate.fromJSON(object.pinUpdate) : undefined,
      pinSnapshot: isSet(object.pinSnapshot) ? GroupPinSnapshot.fromJSON(object.pinSnapshot) : undefined,
      syncBundle: isSet(object.syncBundle) ? GroupSyncBundle.fromJSON(object.syncBundle) : undefined,
      reaction: isSet(object.reaction) ? Reaction.fromJSON(object.reaction) : undefined,
      messageType: isSet(object.messageType)
        ? globalThis.Number(object.messageType)
        : isSet(object.message_type)
        ? globalThis.Number(object.message_type)
        : 0,
    };
  },

  toJSON(message: GroupMessageInner): unknown {
    const obj: any = {};
    if (message.channelId !== 0) {
      obj.channelId = Math.round(message.channelId);
    }
    if (message.messagePayload !== undefined) {
      obj.messagePayload = MessagePayload.toJSON(message.messagePayload);
    }
    if (message.pinUpdate !== undefined) {
      obj.pinUpdate = GroupPinUpdate.toJSON(message.pinUpdate);
    }
    if (message.pinSnapshot !== undefined) {
      obj.pinSnapshot = GroupPinSnapshot.toJSON(message.pinSnapshot);
    }
    if (message.syncBundle !== undefined) {
      obj.syncBundle = GroupSyncBundle.toJSON(message.syncBundle);
    }
    if (message.reaction !== undefined) {
      obj.reaction = Reaction.toJSON(message.reaction);
    }
    if (message.messageType !== 0) {
      obj.messageType = Math.round(message.messageType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMessageInner>, I>>(base?: I): GroupMessageInner {
    return GroupMessageInner.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMessageInner>, I>>(object: I): GroupMessageInner {
    const message = createBaseGroupMessageInner();
    message.channelId = object.channelId ?? 0;
    message.messagePayload = (object.messagePayload !== undefined && object.messagePayload !== null)
      ? MessagePayload.fromPartial(object.messagePayload)
      : undefined;
    message.pinUpdate = (object.pinUpdate !== undefined && object.pinUpdate !== null)
      ? GroupPinUpdate.fromPartial(object.pinUpdate)
      : undefined;
    message.pinSnapshot = (object.pinSnapshot !== undefined && object.pinSnapshot !== null)
      ? GroupPinSnapshot.fromPartial(object.pinSnapshot)
      : undefined;
    message.syncBundle = (object.syncBundle !== undefined && object.syncBundle !== null)
      ? GroupSyncBundle.fromPartial(object.syncBundle)
      : undefined;
    message.reaction = (object.reaction !== undefined && object.reaction !== null)
      ? Reaction.fromPartial(object.reaction)
      : undefined;
    message.messageType = object.messageType ?? 0;
    return message;
  },
};

function createBaseRequestGroupReAdds(): RequestGroupReAdds {
  return { groupIds: [] };
}

export const RequestGroupReAdds: MessageFns<RequestGroupReAdds> = {
  encode(message: RequestGroupReAdds, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.groupIds) {
      if (BigInt.asUintN(64, v) !== v) {
        throw new globalThis.Error("a value provided in array field groupIds of type uint64 is too large");
      }
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestGroupReAdds {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestGroupReAdds();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag === 8) {
              message.groupIds.push(reader.uint64() as bigint);

              continue;
            }

            if (tag === 10) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.groupIds.push(reader.uint64() as bigint);
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestGroupReAdds {
    return {
      groupIds: globalThis.Array.isArray(object?.groupIds)
        ? object.groupIds.map((e: any) => BigInt(e))
        : globalThis.Array.isArray(object?.group_ids)
        ? object.group_ids.map((e: any) => BigInt(e))
        : [],
    };
  },

  toJSON(message: RequestGroupReAdds): unknown {
    const obj: any = {};
    if (message.groupIds?.length) {
      obj.groupIds = message.groupIds.map((e) => e.toString());
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestGroupReAdds>, I>>(base?: I): RequestGroupReAdds {
    return RequestGroupReAdds.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestGroupReAdds>, I>>(object: I): RequestGroupReAdds {
    const message = createBaseRequestGroupReAdds();
    message.groupIds = object.groupIds?.map((e) => BigInt(e)) || [];
    return message;
  },
};

function createBaseRequestGroupSync(): RequestGroupSync {
  return { groupId: 0n, epoch: 0 };
}

export const RequestGroupSync: MessageFns<RequestGroupSync> = {
  encode(message: RequestGroupSync, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.epoch !== 0) {
      writer.uint32(16).uint32(message.epoch);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RequestGroupSync {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRequestGroupSync();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.epoch = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): RequestGroupSync {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
    };
  },

  toJSON(message: RequestGroupSync): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RequestGroupSync>, I>>(base?: I): RequestGroupSync {
    return RequestGroupSync.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RequestGroupSync>, I>>(object: I): RequestGroupSync {
    const message = createBaseRequestGroupSync();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.epoch = object.epoch ?? 0;
    return message;
  },
};

function createBaseGroupReAddRequestSuccess(): GroupReAddRequestSuccess {
  return {};
}

export const GroupReAddRequestSuccess: MessageFns<GroupReAddRequestSuccess> = {
  encode(_: GroupReAddRequestSuccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupReAddRequestSuccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupReAddRequestSuccess();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): GroupReAddRequestSuccess {
    return {};
  },

  toJSON(_: GroupReAddRequestSuccess): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupReAddRequestSuccess>, I>>(base?: I): GroupReAddRequestSuccess {
    return GroupReAddRequestSuccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupReAddRequestSuccess>, I>>(_: I): GroupReAddRequestSuccess {
    const message = createBaseGroupReAddRequestSuccess();
    return message;
  },
};

function createBaseCreateJoinLinkRequest(): CreateJoinLinkRequest {
  return { groupId: 0n, expiresInSeconds: 0n, maxUses: 0 };
}

export const CreateJoinLinkRequest: MessageFns<CreateJoinLinkRequest> = {
  encode(message: CreateJoinLinkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.expiresInSeconds !== 0n) {
      if (BigInt.asUintN(64, message.expiresInSeconds) !== message.expiresInSeconds) {
        throw new globalThis.Error("value provided for field message.expiresInSeconds of type uint64 too large");
      }
      writer.uint32(16).uint64(message.expiresInSeconds);
    }
    if (message.maxUses !== 0) {
      writer.uint32(24).uint32(message.maxUses);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateJoinLinkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateJoinLinkRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.expiresInSeconds = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.maxUses = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateJoinLinkRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      expiresInSeconds: isSet(object.expiresInSeconds)
        ? BigInt(object.expiresInSeconds)
        : isSet(object.expires_in_seconds)
        ? BigInt(object.expires_in_seconds)
        : 0n,
      maxUses: isSet(object.maxUses)
        ? globalThis.Number(object.maxUses)
        : isSet(object.max_uses)
        ? globalThis.Number(object.max_uses)
        : 0,
    };
  },

  toJSON(message: CreateJoinLinkRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.expiresInSeconds !== 0n) {
      obj.expiresInSeconds = message.expiresInSeconds.toString();
    }
    if (message.maxUses !== 0) {
      obj.maxUses = Math.round(message.maxUses);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateJoinLinkRequest>, I>>(base?: I): CreateJoinLinkRequest {
    return CreateJoinLinkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateJoinLinkRequest>, I>>(object: I): CreateJoinLinkRequest {
    const message = createBaseCreateJoinLinkRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.expiresInSeconds = (object.expiresInSeconds !== undefined && object.expiresInSeconds !== null)
      ? BigInt(object.expiresInSeconds)
      : 0n;
    message.maxUses = object.maxUses ?? 0;
    return message;
  },
};

function createBaseCreateJoinLinkResponse(): CreateJoinLinkResponse {
  return { token: "" };
}

export const CreateJoinLinkResponse: MessageFns<CreateJoinLinkResponse> = {
  encode(message: CreateJoinLinkResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateJoinLinkResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateJoinLinkResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.token = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateJoinLinkResponse {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: CreateJoinLinkResponse): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateJoinLinkResponse>, I>>(base?: I): CreateJoinLinkResponse {
    return CreateJoinLinkResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateJoinLinkResponse>, I>>(object: I): CreateJoinLinkResponse {
    const message = createBaseCreateJoinLinkResponse();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseJoinViaLinkRequest(): JoinViaLinkRequest {
  return { token: "" };
}

export const JoinViaLinkRequest: MessageFns<JoinViaLinkRequest> = {
  encode(message: JoinViaLinkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.token !== "") {
      writer.uint32(10).string(message.token);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinViaLinkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseJoinViaLinkRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.token = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): JoinViaLinkRequest {
    return { token: isSet(object.token) ? globalThis.String(object.token) : "" };
  },

  toJSON(message: JoinViaLinkRequest): unknown {
    const obj: any = {};
    if (message.token !== "") {
      obj.token = message.token;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinViaLinkRequest>, I>>(base?: I): JoinViaLinkRequest {
    return JoinViaLinkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinViaLinkRequest>, I>>(object: I): JoinViaLinkRequest {
    const message = createBaseJoinViaLinkRequest();
    message.token = object.token ?? "";
    return message;
  },
};

function createBaseJoinViaLinkSuccess(): JoinViaLinkSuccess {
  return {};
}

export const JoinViaLinkSuccess: MessageFns<JoinViaLinkSuccess> = {
  encode(_: JoinViaLinkSuccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinViaLinkSuccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseJoinViaLinkSuccess();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): JoinViaLinkSuccess {
    return {};
  },

  toJSON(_: JoinViaLinkSuccess): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinViaLinkSuccess>, I>>(base?: I): JoinViaLinkSuccess {
    return JoinViaLinkSuccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinViaLinkSuccess>, I>>(_: I): JoinViaLinkSuccess {
    const message = createBaseJoinViaLinkSuccess();
    return message;
  },
};

function createBaseGroupJoinRequest(): GroupJoinRequest {
  return { groupId: 0n, addressId: 0n, username: "" };
}

export const GroupJoinRequest: MessageFns<GroupJoinRequest> = {
  encode(message: GroupJoinRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.addressId !== 0n) {
      if (BigInt.asUintN(64, message.addressId) !== message.addressId) {
        throw new globalThis.Error("value provided for field message.addressId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.addressId);
    }
    if (message.username !== "") {
      writer.uint32(26).string(message.username);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupJoinRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupJoinRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.addressId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.username = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupJoinRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      addressId: isSet(object.addressId)
        ? BigInt(object.addressId)
        : isSet(object.address_id)
        ? BigInt(object.address_id)
        : 0n,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
    };
  },

  toJSON(message: GroupJoinRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.addressId !== 0n) {
      obj.addressId = message.addressId.toString();
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupJoinRequest>, I>>(base?: I): GroupJoinRequest {
    return GroupJoinRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupJoinRequest>, I>>(object: I): GroupJoinRequest {
    const message = createBaseGroupJoinRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.addressId = (object.addressId !== undefined && object.addressId !== null) ? BigInt(object.addressId) : 0n;
    message.username = object.username ?? "";
    return message;
  },
};

function createBaseGroupJoinRequests(): GroupJoinRequests {
  return { requests: [] };
}

export const GroupJoinRequests: MessageFns<GroupJoinRequests> = {
  encode(message: GroupJoinRequests, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.requests) {
      GroupJoinRequest.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupJoinRequests {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupJoinRequests();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requests.push(GroupJoinRequest.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupJoinRequests {
    return {
      requests: globalThis.Array.isArray(object?.requests)
        ? object.requests.map((e: any) => GroupJoinRequest.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupJoinRequests): unknown {
    const obj: any = {};
    if (message.requests?.length) {
      obj.requests = message.requests.map((e) => GroupJoinRequest.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupJoinRequests>, I>>(base?: I): GroupJoinRequests {
    return GroupJoinRequests.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupJoinRequests>, I>>(object: I): GroupJoinRequests {
    const message = createBaseGroupJoinRequests();
    message.requests = object.requests?.map((e) => GroupJoinRequest.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGroupMeetingSession(): GroupMeetingSession {
  return {
    sessionId: 0n,
    groupId: 0n,
    channelId: 0,
    creatorUsername: "",
    status: 0,
    createdAt: 0n,
    endedAt: 0n,
    participants: [],
    cfMeetingId: "",
    e2eeEnabled: false,
  };
}

export const GroupMeetingSession: MessageFns<GroupMeetingSession> = {
  encode(message: GroupMeetingSession, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sessionId !== 0n) {
      if (BigInt.asUintN(64, message.sessionId) !== message.sessionId) {
        throw new globalThis.Error("value provided for field message.sessionId of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.sessionId);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.channelId !== 0) {
      writer.uint32(24).uint32(message.channelId);
    }
    if (message.creatorUsername !== "") {
      writer.uint32(34).string(message.creatorUsername);
    }
    if (message.status !== 0) {
      writer.uint32(40).int32(message.status);
    }
    if (message.createdAt !== 0n) {
      if (BigInt.asUintN(64, message.createdAt) !== message.createdAt) {
        throw new globalThis.Error("value provided for field message.createdAt of type fixed64 too large");
      }
      writer.uint32(49).fixed64(message.createdAt);
    }
    if (message.endedAt !== 0n) {
      if (BigInt.asUintN(64, message.endedAt) !== message.endedAt) {
        throw new globalThis.Error("value provided for field message.endedAt of type fixed64 too large");
      }
      writer.uint32(57).fixed64(message.endedAt);
    }
    for (const v of message.participants) {
      writer.uint32(66).string(v!);
    }
    if (message.cfMeetingId !== "") {
      writer.uint32(74).string(message.cfMeetingId);
    }
    if (message.e2eeEnabled !== false) {
      writer.uint32(80).bool(message.e2eeEnabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMeetingSession {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMeetingSession();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.sessionId = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.channelId = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.creatorUsername = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.status = reader.int32() as any;
            continue;
          }
          case 6: {
            if (tag !== 49) {
              break;
            }

            message.createdAt = reader.fixed64() as bigint;
            continue;
          }
          case 7: {
            if (tag !== 57) {
              break;
            }

            message.endedAt = reader.fixed64() as bigint;
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.participants.push(reader.string());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.cfMeetingId = reader.string();
            continue;
          }
          case 10: {
            if (tag !== 80) {
              break;
            }

            message.e2eeEnabled = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMeetingSession {
    return {
      sessionId: isSet(object.sessionId)
        ? BigInt(object.sessionId)
        : isSet(object.session_id)
        ? BigInt(object.session_id)
        : 0n,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      channelId: isSet(object.channelId)
        ? globalThis.Number(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.Number(object.channel_id)
        : 0,
      creatorUsername: isSet(object.creatorUsername)
        ? globalThis.String(object.creatorUsername)
        : isSet(object.creator_username)
        ? globalThis.String(object.creator_username)
        : "",
      status: isSet(object.status) ? meetingSessionStatusFromJSON(object.status) : 0,
      createdAt: isSet(object.createdAt)
        ? BigInt(object.createdAt)
        : isSet(object.created_at)
        ? BigInt(object.created_at)
        : 0n,
      endedAt: isSet(object.endedAt) ? BigInt(object.endedAt) : isSet(object.ended_at) ? BigInt(object.ended_at) : 0n,
      participants: globalThis.Array.isArray(object?.participants)
        ? object.participants.map((e: any) => globalThis.String(e))
        : [],
      cfMeetingId: isSet(object.cfMeetingId)
        ? globalThis.String(object.cfMeetingId)
        : isSet(object.cf_meeting_id)
        ? globalThis.String(object.cf_meeting_id)
        : "",
      e2eeEnabled: isSet(object.e2eeEnabled)
        ? globalThis.Boolean(object.e2eeEnabled)
        : isSet(object.e2ee_enabled)
        ? globalThis.Boolean(object.e2ee_enabled)
        : false,
    };
  },

  toJSON(message: GroupMeetingSession): unknown {
    const obj: any = {};
    if (message.sessionId !== 0n) {
      obj.sessionId = message.sessionId.toString();
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.channelId !== 0) {
      obj.channelId = Math.round(message.channelId);
    }
    if (message.creatorUsername !== "") {
      obj.creatorUsername = message.creatorUsername;
    }
    if (message.status !== 0) {
      obj.status = meetingSessionStatusToJSON(message.status);
    }
    if (message.createdAt !== 0n) {
      obj.createdAt = message.createdAt.toString();
    }
    if (message.endedAt !== 0n) {
      obj.endedAt = message.endedAt.toString();
    }
    if (message.participants?.length) {
      obj.participants = message.participants;
    }
    if (message.cfMeetingId !== "") {
      obj.cfMeetingId = message.cfMeetingId;
    }
    if (message.e2eeEnabled !== false) {
      obj.e2eeEnabled = message.e2eeEnabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMeetingSession>, I>>(base?: I): GroupMeetingSession {
    return GroupMeetingSession.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMeetingSession>, I>>(object: I): GroupMeetingSession {
    const message = createBaseGroupMeetingSession();
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null) ? BigInt(object.sessionId) : 0n;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.channelId = object.channelId ?? 0;
    message.creatorUsername = object.creatorUsername ?? "";
    message.status = object.status ?? 0;
    message.createdAt = (object.createdAt !== undefined && object.createdAt !== null) ? BigInt(object.createdAt) : 0n;
    message.endedAt = (object.endedAt !== undefined && object.endedAt !== null) ? BigInt(object.endedAt) : 0n;
    message.participants = object.participants?.map((e) => e) || [];
    message.cfMeetingId = object.cfMeetingId ?? "";
    message.e2eeEnabled = object.e2eeEnabled ?? false;
    return message;
  },
};

function createBaseCreateMeetingRequest(): CreateMeetingRequest {
  return { groupId: 0n, channelId: 0, e2eeEnabled: false };
}

export const CreateMeetingRequest: MessageFns<CreateMeetingRequest> = {
  encode(message: CreateMeetingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.channelId !== 0) {
      writer.uint32(16).uint32(message.channelId);
    }
    if (message.e2eeEnabled !== false) {
      writer.uint32(24).bool(message.e2eeEnabled);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateMeetingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateMeetingRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.channelId = reader.uint32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.e2eeEnabled = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateMeetingRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      channelId: isSet(object.channelId)
        ? globalThis.Number(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.Number(object.channel_id)
        : 0,
      e2eeEnabled: isSet(object.e2eeEnabled)
        ? globalThis.Boolean(object.e2eeEnabled)
        : isSet(object.e2ee_enabled)
        ? globalThis.Boolean(object.e2ee_enabled)
        : false,
    };
  },

  toJSON(message: CreateMeetingRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.channelId !== 0) {
      obj.channelId = Math.round(message.channelId);
    }
    if (message.e2eeEnabled !== false) {
      obj.e2eeEnabled = message.e2eeEnabled;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateMeetingRequest>, I>>(base?: I): CreateMeetingRequest {
    return CreateMeetingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateMeetingRequest>, I>>(object: I): CreateMeetingRequest {
    const message = createBaseCreateMeetingRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.channelId = object.channelId ?? 0;
    message.e2eeEnabled = object.e2eeEnabled ?? false;
    return message;
  },
};

function createBaseCreateMeetingResponse(): CreateMeetingResponse {
  return { session: undefined, participantToken: "" };
}

export const CreateMeetingResponse: MessageFns<CreateMeetingResponse> = {
  encode(message: CreateMeetingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      GroupMeetingSession.encode(message.session, writer.uint32(10).fork()).join();
    }
    if (message.participantToken !== "") {
      writer.uint32(18).string(message.participantToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateMeetingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateMeetingResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.session = GroupMeetingSession.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.participantToken = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateMeetingResponse {
    return {
      session: isSet(object.session) ? GroupMeetingSession.fromJSON(object.session) : undefined,
      participantToken: isSet(object.participantToken)
        ? globalThis.String(object.participantToken)
        : isSet(object.participant_token)
        ? globalThis.String(object.participant_token)
        : "",
    };
  },

  toJSON(message: CreateMeetingResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = GroupMeetingSession.toJSON(message.session);
    }
    if (message.participantToken !== "") {
      obj.participantToken = message.participantToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateMeetingResponse>, I>>(base?: I): CreateMeetingResponse {
    return CreateMeetingResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateMeetingResponse>, I>>(object: I): CreateMeetingResponse {
    const message = createBaseCreateMeetingResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? GroupMeetingSession.fromPartial(object.session)
      : undefined;
    message.participantToken = object.participantToken ?? "";
    return message;
  },
};

function createBaseJoinMeetingRequest(): JoinMeetingRequest {
  return { groupId: 0n, sessionId: 0n };
}

export const JoinMeetingRequest: MessageFns<JoinMeetingRequest> = {
  encode(message: JoinMeetingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.sessionId !== 0n) {
      if (BigInt.asUintN(64, message.sessionId) !== message.sessionId) {
        throw new globalThis.Error("value provided for field message.sessionId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinMeetingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseJoinMeetingRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.sessionId = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): JoinMeetingRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      sessionId: isSet(object.sessionId)
        ? BigInt(object.sessionId)
        : isSet(object.session_id)
        ? BigInt(object.session_id)
        : 0n,
    };
  },

  toJSON(message: JoinMeetingRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.sessionId !== 0n) {
      obj.sessionId = message.sessionId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinMeetingRequest>, I>>(base?: I): JoinMeetingRequest {
    return JoinMeetingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinMeetingRequest>, I>>(object: I): JoinMeetingRequest {
    const message = createBaseJoinMeetingRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null) ? BigInt(object.sessionId) : 0n;
    return message;
  },
};

function createBaseJoinMeetingResponse(): JoinMeetingResponse {
  return { session: undefined, participantToken: "" };
}

export const JoinMeetingResponse: MessageFns<JoinMeetingResponse> = {
  encode(message: JoinMeetingResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      GroupMeetingSession.encode(message.session, writer.uint32(10).fork()).join();
    }
    if (message.participantToken !== "") {
      writer.uint32(18).string(message.participantToken);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): JoinMeetingResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseJoinMeetingResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.session = GroupMeetingSession.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.participantToken = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): JoinMeetingResponse {
    return {
      session: isSet(object.session) ? GroupMeetingSession.fromJSON(object.session) : undefined,
      participantToken: isSet(object.participantToken)
        ? globalThis.String(object.participantToken)
        : isSet(object.participant_token)
        ? globalThis.String(object.participant_token)
        : "",
    };
  },

  toJSON(message: JoinMeetingResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = GroupMeetingSession.toJSON(message.session);
    }
    if (message.participantToken !== "") {
      obj.participantToken = message.participantToken;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<JoinMeetingResponse>, I>>(base?: I): JoinMeetingResponse {
    return JoinMeetingResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<JoinMeetingResponse>, I>>(object: I): JoinMeetingResponse {
    const message = createBaseJoinMeetingResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? GroupMeetingSession.fromPartial(object.session)
      : undefined;
    message.participantToken = object.participantToken ?? "";
    return message;
  },
};

function createBaseLeaveMeetingRequest(): LeaveMeetingRequest {
  return { groupId: 0n, sessionId: 0n };
}

export const LeaveMeetingRequest: MessageFns<LeaveMeetingRequest> = {
  encode(message: LeaveMeetingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.sessionId !== 0n) {
      if (BigInt.asUintN(64, message.sessionId) !== message.sessionId) {
        throw new globalThis.Error("value provided for field message.sessionId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): LeaveMeetingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLeaveMeetingRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.sessionId = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): LeaveMeetingRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      sessionId: isSet(object.sessionId)
        ? BigInt(object.sessionId)
        : isSet(object.session_id)
        ? BigInt(object.session_id)
        : 0n,
    };
  },

  toJSON(message: LeaveMeetingRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.sessionId !== 0n) {
      obj.sessionId = message.sessionId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<LeaveMeetingRequest>, I>>(base?: I): LeaveMeetingRequest {
    return LeaveMeetingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<LeaveMeetingRequest>, I>>(object: I): LeaveMeetingRequest {
    const message = createBaseLeaveMeetingRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null) ? BigInt(object.sessionId) : 0n;
    return message;
  },
};

function createBaseEndMeetingRequest(): EndMeetingRequest {
  return { groupId: 0n, sessionId: 0n };
}

export const EndMeetingRequest: MessageFns<EndMeetingRequest> = {
  encode(message: EndMeetingRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.sessionId !== 0n) {
      if (BigInt.asUintN(64, message.sessionId) !== message.sessionId) {
        throw new globalThis.Error("value provided for field message.sessionId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.sessionId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EndMeetingRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEndMeetingRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.sessionId = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): EndMeetingRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      sessionId: isSet(object.sessionId)
        ? BigInt(object.sessionId)
        : isSet(object.session_id)
        ? BigInt(object.session_id)
        : 0n,
    };
  },

  toJSON(message: EndMeetingRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.sessionId !== 0n) {
      obj.sessionId = message.sessionId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<EndMeetingRequest>, I>>(base?: I): EndMeetingRequest {
    return EndMeetingRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EndMeetingRequest>, I>>(object: I): EndMeetingRequest {
    const message = createBaseEndMeetingRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null) ? BigInt(object.sessionId) : 0n;
    return message;
  },
};

function createBaseGetActiveSessionRequest(): GetActiveSessionRequest {
  return { groupId: 0n, channelId: 0 };
}

export const GetActiveSessionRequest: MessageFns<GetActiveSessionRequest> = {
  encode(message: GetActiveSessionRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.channelId !== 0) {
      writer.uint32(16).uint32(message.channelId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActiveSessionRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetActiveSessionRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.channelId = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetActiveSessionRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      channelId: isSet(object.channelId)
        ? globalThis.Number(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.Number(object.channel_id)
        : 0,
    };
  },

  toJSON(message: GetActiveSessionRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.channelId !== 0) {
      obj.channelId = Math.round(message.channelId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetActiveSessionRequest>, I>>(base?: I): GetActiveSessionRequest {
    return GetActiveSessionRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetActiveSessionRequest>, I>>(object: I): GetActiveSessionRequest {
    const message = createBaseGetActiveSessionRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.channelId = object.channelId ?? 0;
    return message;
  },
};

function createBaseGetActiveSessionResponse(): GetActiveSessionResponse {
  return { session: undefined };
}

export const GetActiveSessionResponse: MessageFns<GetActiveSessionResponse> = {
  encode(message: GetActiveSessionResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.session !== undefined) {
      GroupMeetingSession.encode(message.session, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetActiveSessionResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetActiveSessionResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.session = GroupMeetingSession.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetActiveSessionResponse {
    return { session: isSet(object.session) ? GroupMeetingSession.fromJSON(object.session) : undefined };
  },

  toJSON(message: GetActiveSessionResponse): unknown {
    const obj: any = {};
    if (message.session !== undefined) {
      obj.session = GroupMeetingSession.toJSON(message.session);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetActiveSessionResponse>, I>>(base?: I): GetActiveSessionResponse {
    return GetActiveSessionResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetActiveSessionResponse>, I>>(object: I): GetActiveSessionResponse {
    const message = createBaseGetActiveSessionResponse();
    message.session = (object.session !== undefined && object.session !== null)
      ? GroupMeetingSession.fromPartial(object.session)
      : undefined;
    return message;
  },
};

function createBaseGroupMeetingSignal(): GroupMeetingSignal {
  return { groupId: 0n, channelId: 0, sessionId: 0n, type: 0, username: "", cfMeetingId: "" };
}

export const GroupMeetingSignal: MessageFns<GroupMeetingSignal> = {
  encode(message: GroupMeetingSignal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.channelId !== 0) {
      writer.uint32(16).uint32(message.channelId);
    }
    if (message.sessionId !== 0n) {
      if (BigInt.asUintN(64, message.sessionId) !== message.sessionId) {
        throw new globalThis.Error("value provided for field message.sessionId of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.sessionId);
    }
    if (message.type !== 0) {
      writer.uint32(32).int32(message.type);
    }
    if (message.username !== "") {
      writer.uint32(42).string(message.username);
    }
    if (message.cfMeetingId !== "") {
      writer.uint32(50).string(message.cfMeetingId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupMeetingSignal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupMeetingSignal();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.channelId = reader.uint32();
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.sessionId = reader.fixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.type = reader.int32() as any;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.cfMeetingId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupMeetingSignal {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      channelId: isSet(object.channelId)
        ? globalThis.Number(object.channelId)
        : isSet(object.channel_id)
        ? globalThis.Number(object.channel_id)
        : 0,
      sessionId: isSet(object.sessionId)
        ? BigInt(object.sessionId)
        : isSet(object.session_id)
        ? BigInt(object.session_id)
        : 0n,
      type: isSet(object.type) ? meetingSignalTypeFromJSON(object.type) : 0,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      cfMeetingId: isSet(object.cfMeetingId)
        ? globalThis.String(object.cfMeetingId)
        : isSet(object.cf_meeting_id)
        ? globalThis.String(object.cf_meeting_id)
        : "",
    };
  },

  toJSON(message: GroupMeetingSignal): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.channelId !== 0) {
      obj.channelId = Math.round(message.channelId);
    }
    if (message.sessionId !== 0n) {
      obj.sessionId = message.sessionId.toString();
    }
    if (message.type !== 0) {
      obj.type = meetingSignalTypeToJSON(message.type);
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.cfMeetingId !== "") {
      obj.cfMeetingId = message.cfMeetingId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupMeetingSignal>, I>>(base?: I): GroupMeetingSignal {
    return GroupMeetingSignal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupMeetingSignal>, I>>(object: I): GroupMeetingSignal {
    const message = createBaseGroupMeetingSignal();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.channelId = object.channelId ?? 0;
    message.sessionId = (object.sessionId !== undefined && object.sessionId !== null) ? BigInt(object.sessionId) : 0n;
    message.type = object.type ?? 0;
    message.username = object.username ?? "";
    message.cfMeetingId = object.cfMeetingId ?? "";
    return message;
  },
};

function createBaseGroupHistoryRequestItem(): GroupHistoryRequestItem {
  return {
    id: 0n,
    groupId: 0n,
    requesterAddress: 0n,
    requesterUsername: "",
    startMsgId: 0n,
    endMsgId: 0n,
    status: 0,
    claimedBy: 0n,
    createdAt: 0n,
  };
}

export const GroupHistoryRequestItem: MessageFns<GroupHistoryRequestItem> = {
  encode(message: GroupHistoryRequestItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.id);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.requesterAddress !== 0n) {
      if (BigInt.asUintN(64, message.requesterAddress) !== message.requesterAddress) {
        throw new globalThis.Error("value provided for field message.requesterAddress of type uint64 too large");
      }
      writer.uint32(24).uint64(message.requesterAddress);
    }
    if (message.requesterUsername !== "") {
      writer.uint32(34).string(message.requesterUsername);
    }
    if (message.startMsgId !== 0n) {
      if (BigInt.asUintN(64, message.startMsgId) !== message.startMsgId) {
        throw new globalThis.Error("value provided for field message.startMsgId of type fixed64 too large");
      }
      writer.uint32(41).fixed64(message.startMsgId);
    }
    if (message.endMsgId !== 0n) {
      if (BigInt.asUintN(64, message.endMsgId) !== message.endMsgId) {
        throw new globalThis.Error("value provided for field message.endMsgId of type fixed64 too large");
      }
      writer.uint32(49).fixed64(message.endMsgId);
    }
    if (message.status !== 0) {
      writer.uint32(56).uint32(message.status);
    }
    if (message.claimedBy !== 0n) {
      if (BigInt.asUintN(64, message.claimedBy) !== message.claimedBy) {
        throw new globalThis.Error("value provided for field message.claimedBy of type uint64 too large");
      }
      writer.uint32(64).uint64(message.claimedBy);
    }
    if (message.createdAt !== 0n) {
      if (BigInt.asUintN(64, message.createdAt) !== message.createdAt) {
        throw new globalThis.Error("value provided for field message.createdAt of type fixed64 too large");
      }
      writer.uint32(73).fixed64(message.createdAt);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupHistoryRequestItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupHistoryRequestItem();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.id = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.requesterAddress = reader.uint64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.requesterUsername = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 41) {
              break;
            }

            message.startMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 49) {
              break;
            }

            message.endMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.status = reader.uint32();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.claimedBy = reader.uint64() as bigint;
            continue;
          }
          case 9: {
            if (tag !== 73) {
              break;
            }

            message.createdAt = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupHistoryRequestItem {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      requesterAddress: isSet(object.requesterAddress)
        ? BigInt(object.requesterAddress)
        : isSet(object.requester_address)
        ? BigInt(object.requester_address)
        : 0n,
      requesterUsername: isSet(object.requesterUsername)
        ? globalThis.String(object.requesterUsername)
        : isSet(object.requester_username)
        ? globalThis.String(object.requester_username)
        : "",
      startMsgId: isSet(object.startMsgId)
        ? BigInt(object.startMsgId)
        : isSet(object.start_msg_id)
        ? BigInt(object.start_msg_id)
        : 0n,
      endMsgId: isSet(object.endMsgId)
        ? BigInt(object.endMsgId)
        : isSet(object.end_msg_id)
        ? BigInt(object.end_msg_id)
        : 0n,
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      claimedBy: isSet(object.claimedBy)
        ? BigInt(object.claimedBy)
        : isSet(object.claimed_by)
        ? BigInt(object.claimed_by)
        : 0n,
      createdAt: isSet(object.createdAt)
        ? BigInt(object.createdAt)
        : isSet(object.created_at)
        ? BigInt(object.created_at)
        : 0n,
    };
  },

  toJSON(message: GroupHistoryRequestItem): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.requesterAddress !== 0n) {
      obj.requesterAddress = message.requesterAddress.toString();
    }
    if (message.requesterUsername !== "") {
      obj.requesterUsername = message.requesterUsername;
    }
    if (message.startMsgId !== 0n) {
      obj.startMsgId = message.startMsgId.toString();
    }
    if (message.endMsgId !== 0n) {
      obj.endMsgId = message.endMsgId.toString();
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.claimedBy !== 0n) {
      obj.claimedBy = message.claimedBy.toString();
    }
    if (message.createdAt !== 0n) {
      obj.createdAt = message.createdAt.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupHistoryRequestItem>, I>>(base?: I): GroupHistoryRequestItem {
    return GroupHistoryRequestItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupHistoryRequestItem>, I>>(object: I): GroupHistoryRequestItem {
    const message = createBaseGroupHistoryRequestItem();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.requesterAddress = (object.requesterAddress !== undefined && object.requesterAddress !== null)
      ? BigInt(object.requesterAddress)
      : 0n;
    message.requesterUsername = object.requesterUsername ?? "";
    message.startMsgId = (object.startMsgId !== undefined && object.startMsgId !== null)
      ? BigInt(object.startMsgId)
      : 0n;
    message.endMsgId = (object.endMsgId !== undefined && object.endMsgId !== null) ? BigInt(object.endMsgId) : 0n;
    message.status = object.status ?? 0;
    message.claimedBy = (object.claimedBy !== undefined && object.claimedBy !== null) ? BigInt(object.claimedBy) : 0n;
    message.createdAt = (object.createdAt !== undefined && object.createdAt !== null) ? BigInt(object.createdAt) : 0n;
    return message;
  },
};

function createBaseGroupHistoryChunkItem(): GroupHistoryChunkItem {
  return {
    id: 0n,
    groupId: 0n,
    startMsgId: 0n,
    endMsgId: 0n,
    msgCount: 0,
    unencryptedHash: Buffer.alloc(0),
    chunkUrl: "",
    uploadedBy: "",
    status: 0,
    disapprovedBy: "",
    disapprovedReason: "",
    createdAt: 0n,
    verified: false,
  };
}

export const GroupHistoryChunkItem: MessageFns<GroupHistoryChunkItem> = {
  encode(message: GroupHistoryChunkItem, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type uint64 too large");
      }
      writer.uint32(8).uint64(message.id);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.startMsgId !== 0n) {
      if (BigInt.asUintN(64, message.startMsgId) !== message.startMsgId) {
        throw new globalThis.Error("value provided for field message.startMsgId of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.startMsgId);
    }
    if (message.endMsgId !== 0n) {
      if (BigInt.asUintN(64, message.endMsgId) !== message.endMsgId) {
        throw new globalThis.Error("value provided for field message.endMsgId of type fixed64 too large");
      }
      writer.uint32(33).fixed64(message.endMsgId);
    }
    if (message.msgCount !== 0) {
      writer.uint32(40).uint32(message.msgCount);
    }
    if (message.unencryptedHash.length !== 0) {
      writer.uint32(50).bytes(message.unencryptedHash);
    }
    if (message.chunkUrl !== "") {
      writer.uint32(58).string(message.chunkUrl);
    }
    if (message.uploadedBy !== "") {
      writer.uint32(66).string(message.uploadedBy);
    }
    if (message.status !== 0) {
      writer.uint32(72).uint32(message.status);
    }
    if (message.disapprovedBy !== "") {
      writer.uint32(82).string(message.disapprovedBy);
    }
    if (message.disapprovedReason !== "") {
      writer.uint32(90).string(message.disapprovedReason);
    }
    if (message.createdAt !== 0n) {
      if (BigInt.asUintN(64, message.createdAt) !== message.createdAt) {
        throw new globalThis.Error("value provided for field message.createdAt of type fixed64 too large");
      }
      writer.uint32(97).fixed64(message.createdAt);
    }
    if (message.verified !== false) {
      writer.uint32(104).bool(message.verified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupHistoryChunkItem {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupHistoryChunkItem();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.id = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.startMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.endMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.msgCount = reader.uint32();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.unencryptedHash = Buffer.from(reader.bytes());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.chunkUrl = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.uploadedBy = reader.string();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.status = reader.uint32();
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.disapprovedBy = reader.string();
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.disapprovedReason = reader.string();
            continue;
          }
          case 12: {
            if (tag !== 97) {
              break;
            }

            message.createdAt = reader.fixed64() as bigint;
            continue;
          }
          case 13: {
            if (tag !== 104) {
              break;
            }

            message.verified = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupHistoryChunkItem {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      startMsgId: isSet(object.startMsgId)
        ? BigInt(object.startMsgId)
        : isSet(object.start_msg_id)
        ? BigInt(object.start_msg_id)
        : 0n,
      endMsgId: isSet(object.endMsgId)
        ? BigInt(object.endMsgId)
        : isSet(object.end_msg_id)
        ? BigInt(object.end_msg_id)
        : 0n,
      msgCount: isSet(object.msgCount)
        ? globalThis.Number(object.msgCount)
        : isSet(object.msg_count)
        ? globalThis.Number(object.msg_count)
        : 0,
      unencryptedHash: isSet(object.unencryptedHash)
        ? Buffer.from(bytesFromBase64(object.unencryptedHash))
        : isSet(object.unencrypted_hash)
        ? Buffer.from(bytesFromBase64(object.unencrypted_hash))
        : Buffer.alloc(0),
      chunkUrl: isSet(object.chunkUrl)
        ? globalThis.String(object.chunkUrl)
        : isSet(object.chunk_url)
        ? globalThis.String(object.chunk_url)
        : "",
      uploadedBy: isSet(object.uploadedBy)
        ? globalThis.String(object.uploadedBy)
        : isSet(object.uploaded_by)
        ? globalThis.String(object.uploaded_by)
        : "",
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      disapprovedBy: isSet(object.disapprovedBy)
        ? globalThis.String(object.disapprovedBy)
        : isSet(object.disapproved_by)
        ? globalThis.String(object.disapproved_by)
        : "",
      disapprovedReason: isSet(object.disapprovedReason)
        ? globalThis.String(object.disapprovedReason)
        : isSet(object.disapproved_reason)
        ? globalThis.String(object.disapproved_reason)
        : "",
      createdAt: isSet(object.createdAt)
        ? BigInt(object.createdAt)
        : isSet(object.created_at)
        ? BigInt(object.created_at)
        : 0n,
      verified: isSet(object.verified) ? globalThis.Boolean(object.verified) : false,
    };
  },

  toJSON(message: GroupHistoryChunkItem): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.startMsgId !== 0n) {
      obj.startMsgId = message.startMsgId.toString();
    }
    if (message.endMsgId !== 0n) {
      obj.endMsgId = message.endMsgId.toString();
    }
    if (message.msgCount !== 0) {
      obj.msgCount = Math.round(message.msgCount);
    }
    if (message.unencryptedHash.length !== 0) {
      obj.unencryptedHash = base64FromBytes(message.unencryptedHash);
    }
    if (message.chunkUrl !== "") {
      obj.chunkUrl = message.chunkUrl;
    }
    if (message.uploadedBy !== "") {
      obj.uploadedBy = message.uploadedBy;
    }
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.disapprovedBy !== "") {
      obj.disapprovedBy = message.disapprovedBy;
    }
    if (message.disapprovedReason !== "") {
      obj.disapprovedReason = message.disapprovedReason;
    }
    if (message.createdAt !== 0n) {
      obj.createdAt = message.createdAt.toString();
    }
    if (message.verified !== false) {
      obj.verified = message.verified;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupHistoryChunkItem>, I>>(base?: I): GroupHistoryChunkItem {
    return GroupHistoryChunkItem.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupHistoryChunkItem>, I>>(object: I): GroupHistoryChunkItem {
    const message = createBaseGroupHistoryChunkItem();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.startMsgId = (object.startMsgId !== undefined && object.startMsgId !== null)
      ? BigInt(object.startMsgId)
      : 0n;
    message.endMsgId = (object.endMsgId !== undefined && object.endMsgId !== null) ? BigInt(object.endMsgId) : 0n;
    message.msgCount = object.msgCount ?? 0;
    message.unencryptedHash = object.unencryptedHash ?? Buffer.alloc(0);
    message.chunkUrl = object.chunkUrl ?? "";
    message.uploadedBy = object.uploadedBy ?? "";
    message.status = object.status ?? 0;
    message.disapprovedBy = object.disapprovedBy ?? "";
    message.disapprovedReason = object.disapprovedReason ?? "";
    message.createdAt = (object.createdAt !== undefined && object.createdAt !== null) ? BigInt(object.createdAt) : 0n;
    message.verified = object.verified ?? false;
    return message;
  },
};

function createBaseGroupHistoryChunkKey(): GroupHistoryChunkKey {
  return {
    groupId: 0n,
    startMsgId: 0n,
    endMsgId: 0n,
    key: Buffer.alloc(0),
    nonce: Buffer.alloc(0),
    unencryptedHash: Buffer.alloc(0),
  };
}

export const GroupHistoryChunkKey: MessageFns<GroupHistoryChunkKey> = {
  encode(message: GroupHistoryChunkKey, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.startMsgId !== 0n) {
      if (BigInt.asUintN(64, message.startMsgId) !== message.startMsgId) {
        throw new globalThis.Error("value provided for field message.startMsgId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.startMsgId);
    }
    if (message.endMsgId !== 0n) {
      if (BigInt.asUintN(64, message.endMsgId) !== message.endMsgId) {
        throw new globalThis.Error("value provided for field message.endMsgId of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.endMsgId);
    }
    if (message.key.length !== 0) {
      writer.uint32(34).bytes(message.key);
    }
    if (message.nonce.length !== 0) {
      writer.uint32(42).bytes(message.nonce);
    }
    if (message.unencryptedHash.length !== 0) {
      writer.uint32(50).bytes(message.unencryptedHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupHistoryChunkKey {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupHistoryChunkKey();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.startMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.endMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.key = Buffer.from(reader.bytes());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.nonce = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.unencryptedHash = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupHistoryChunkKey {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      startMsgId: isSet(object.startMsgId)
        ? BigInt(object.startMsgId)
        : isSet(object.start_msg_id)
        ? BigInt(object.start_msg_id)
        : 0n,
      endMsgId: isSet(object.endMsgId)
        ? BigInt(object.endMsgId)
        : isSet(object.end_msg_id)
        ? BigInt(object.end_msg_id)
        : 0n,
      key: isSet(object.key) ? Buffer.from(bytesFromBase64(object.key)) : Buffer.alloc(0),
      nonce: isSet(object.nonce) ? Buffer.from(bytesFromBase64(object.nonce)) : Buffer.alloc(0),
      unencryptedHash: isSet(object.unencryptedHash)
        ? Buffer.from(bytesFromBase64(object.unencryptedHash))
        : isSet(object.unencrypted_hash)
        ? Buffer.from(bytesFromBase64(object.unencrypted_hash))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: GroupHistoryChunkKey): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.startMsgId !== 0n) {
      obj.startMsgId = message.startMsgId.toString();
    }
    if (message.endMsgId !== 0n) {
      obj.endMsgId = message.endMsgId.toString();
    }
    if (message.key.length !== 0) {
      obj.key = base64FromBytes(message.key);
    }
    if (message.nonce.length !== 0) {
      obj.nonce = base64FromBytes(message.nonce);
    }
    if (message.unencryptedHash.length !== 0) {
      obj.unencryptedHash = base64FromBytes(message.unencryptedHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupHistoryChunkKey>, I>>(base?: I): GroupHistoryChunkKey {
    return GroupHistoryChunkKey.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupHistoryChunkKey>, I>>(object: I): GroupHistoryChunkKey {
    const message = createBaseGroupHistoryChunkKey();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.startMsgId = (object.startMsgId !== undefined && object.startMsgId !== null)
      ? BigInt(object.startMsgId)
      : 0n;
    message.endMsgId = (object.endMsgId !== undefined && object.endMsgId !== null) ? BigInt(object.endMsgId) : 0n;
    message.key = object.key ?? Buffer.alloc(0);
    message.nonce = object.nonce ?? Buffer.alloc(0);
    message.unencryptedHash = object.unencryptedHash ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseGroupHistoryKeysPayload(): GroupHistoryKeysPayload {
  return { keys: [], chunks: [] };
}

export const GroupHistoryKeysPayload: MessageFns<GroupHistoryKeysPayload> = {
  encode(message: GroupHistoryKeysPayload, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.keys) {
      GroupHistoryChunkKey.encode(v!, writer.uint32(10).fork()).join();
    }
    for (const v of message.chunks) {
      GroupHistoryChunkItem.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupHistoryKeysPayload {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupHistoryKeysPayload();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.keys.push(GroupHistoryChunkKey.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.chunks.push(GroupHistoryChunkItem.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupHistoryKeysPayload {
    return {
      keys: globalThis.Array.isArray(object?.keys) ? object.keys.map((e: any) => GroupHistoryChunkKey.fromJSON(e)) : [],
      chunks: globalThis.Array.isArray(object?.chunks)
        ? object.chunks.map((e: any) => GroupHistoryChunkItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GroupHistoryKeysPayload): unknown {
    const obj: any = {};
    if (message.keys?.length) {
      obj.keys = message.keys.map((e) => GroupHistoryChunkKey.toJSON(e));
    }
    if (message.chunks?.length) {
      obj.chunks = message.chunks.map((e) => GroupHistoryChunkItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupHistoryKeysPayload>, I>>(base?: I): GroupHistoryKeysPayload {
    return GroupHistoryKeysPayload.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupHistoryKeysPayload>, I>>(object: I): GroupHistoryKeysPayload {
    const message = createBaseGroupHistoryKeysPayload();
    message.keys = object.keys?.map((e) => GroupHistoryChunkKey.fromPartial(e)) || [];
    message.chunks = object.chunks?.map((e) => GroupHistoryChunkItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateHistoryRequest(): CreateHistoryRequest {
  return { groupId: 0n, startMsgId: 0n, endMsgId: 0n };
}

export const CreateHistoryRequest: MessageFns<CreateHistoryRequest> = {
  encode(message: CreateHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.startMsgId !== 0n) {
      if (BigInt.asUintN(64, message.startMsgId) !== message.startMsgId) {
        throw new globalThis.Error("value provided for field message.startMsgId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.startMsgId);
    }
    if (message.endMsgId !== 0n) {
      if (BigInt.asUintN(64, message.endMsgId) !== message.endMsgId) {
        throw new globalThis.Error("value provided for field message.endMsgId of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.endMsgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCreateHistoryRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.startMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.endMsgId = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CreateHistoryRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      startMsgId: isSet(object.startMsgId)
        ? BigInt(object.startMsgId)
        : isSet(object.start_msg_id)
        ? BigInt(object.start_msg_id)
        : 0n,
      endMsgId: isSet(object.endMsgId)
        ? BigInt(object.endMsgId)
        : isSet(object.end_msg_id)
        ? BigInt(object.end_msg_id)
        : 0n,
    };
  },

  toJSON(message: CreateHistoryRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.startMsgId !== 0n) {
      obj.startMsgId = message.startMsgId.toString();
    }
    if (message.endMsgId !== 0n) {
      obj.endMsgId = message.endMsgId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateHistoryRequest>, I>>(base?: I): CreateHistoryRequest {
    return CreateHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateHistoryRequest>, I>>(object: I): CreateHistoryRequest {
    const message = createBaseCreateHistoryRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.startMsgId = (object.startMsgId !== undefined && object.startMsgId !== null)
      ? BigInt(object.startMsgId)
      : 0n;
    message.endMsgId = (object.endMsgId !== undefined && object.endMsgId !== null) ? BigInt(object.endMsgId) : 0n;
    return message;
  },
};

function createBaseClaimHistoryRequest(): ClaimHistoryRequest {
  return { groupId: 0n, requestId: 0n, startMsgId: 0n, endMsgId: 0n };
}

export const ClaimHistoryRequest: MessageFns<ClaimHistoryRequest> = {
  encode(message: ClaimHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.requestId !== 0n) {
      if (BigInt.asUintN(64, message.requestId) !== message.requestId) {
        throw new globalThis.Error("value provided for field message.requestId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.requestId);
    }
    if (message.startMsgId !== 0n) {
      if (BigInt.asUintN(64, message.startMsgId) !== message.startMsgId) {
        throw new globalThis.Error("value provided for field message.startMsgId of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.startMsgId);
    }
    if (message.endMsgId !== 0n) {
      if (BigInt.asUintN(64, message.endMsgId) !== message.endMsgId) {
        throw new globalThis.Error("value provided for field message.endMsgId of type fixed64 too large");
      }
      writer.uint32(33).fixed64(message.endMsgId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaimHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseClaimHistoryRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.requestId = reader.fixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.startMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.endMsgId = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ClaimHistoryRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      requestId: isSet(object.requestId)
        ? BigInt(object.requestId)
        : isSet(object.request_id)
        ? BigInt(object.request_id)
        : 0n,
      startMsgId: isSet(object.startMsgId)
        ? BigInt(object.startMsgId)
        : isSet(object.start_msg_id)
        ? BigInt(object.start_msg_id)
        : 0n,
      endMsgId: isSet(object.endMsgId)
        ? BigInt(object.endMsgId)
        : isSet(object.end_msg_id)
        ? BigInt(object.end_msg_id)
        : 0n,
    };
  },

  toJSON(message: ClaimHistoryRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.requestId !== 0n) {
      obj.requestId = message.requestId.toString();
    }
    if (message.startMsgId !== 0n) {
      obj.startMsgId = message.startMsgId.toString();
    }
    if (message.endMsgId !== 0n) {
      obj.endMsgId = message.endMsgId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClaimHistoryRequest>, I>>(base?: I): ClaimHistoryRequest {
    return ClaimHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClaimHistoryRequest>, I>>(object: I): ClaimHistoryRequest {
    const message = createBaseClaimHistoryRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.requestId = (object.requestId !== undefined && object.requestId !== null) ? BigInt(object.requestId) : 0n;
    message.startMsgId = (object.startMsgId !== undefined && object.startMsgId !== null)
      ? BigInt(object.startMsgId)
      : 0n;
    message.endMsgId = (object.endMsgId !== undefined && object.endMsgId !== null) ? BigInt(object.endMsgId) : 0n;
    return message;
  },
};

function createBaseClaimHistoryResponse(): ClaimHistoryResponse {
  return { granted: false, message: "" };
}

export const ClaimHistoryResponse: MessageFns<ClaimHistoryResponse> = {
  encode(message: ClaimHistoryResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.granted !== false) {
      writer.uint32(8).bool(message.granted);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ClaimHistoryResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseClaimHistoryResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.granted = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.message = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ClaimHistoryResponse {
    return {
      granted: isSet(object.granted) ? globalThis.Boolean(object.granted) : false,
      message: isSet(object.message) ? globalThis.String(object.message) : "",
    };
  },

  toJSON(message: ClaimHistoryResponse): unknown {
    const obj: any = {};
    if (message.granted !== false) {
      obj.granted = message.granted;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ClaimHistoryResponse>, I>>(base?: I): ClaimHistoryResponse {
    return ClaimHistoryResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ClaimHistoryResponse>, I>>(object: I): ClaimHistoryResponse {
    const message = createBaseClaimHistoryResponse();
    message.granted = object.granted ?? false;
    message.message = object.message ?? "";
    return message;
  },
};

function createBasePublishHistoryChunkRequest(): PublishHistoryChunkRequest {
  return {
    reserveOnly: false,
    groupId: 0n,
    startMsgId: 0n,
    endMsgId: 0n,
    msgCount: 0,
    unencryptedHash: Buffer.alloc(0),
    chunkUrl: "",
  };
}

export const PublishHistoryChunkRequest: MessageFns<PublishHistoryChunkRequest> = {
  encode(message: PublishHistoryChunkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reserveOnly !== false) {
      writer.uint32(56).bool(message.reserveOnly);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.startMsgId !== 0n) {
      if (BigInt.asUintN(64, message.startMsgId) !== message.startMsgId) {
        throw new globalThis.Error("value provided for field message.startMsgId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.startMsgId);
    }
    if (message.endMsgId !== 0n) {
      if (BigInt.asUintN(64, message.endMsgId) !== message.endMsgId) {
        throw new globalThis.Error("value provided for field message.endMsgId of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.endMsgId);
    }
    if (message.msgCount !== 0) {
      writer.uint32(32).uint32(message.msgCount);
    }
    if (message.unencryptedHash.length !== 0) {
      writer.uint32(42).bytes(message.unencryptedHash);
    }
    if (message.chunkUrl !== "") {
      writer.uint32(50).string(message.chunkUrl);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PublishHistoryChunkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBasePublishHistoryChunkRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.reserveOnly = reader.bool();
            continue;
          }
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.startMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.endMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.msgCount = reader.uint32();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.unencryptedHash = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.chunkUrl = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): PublishHistoryChunkRequest {
    return {
      reserveOnly: isSet(object.reserveOnly)
        ? globalThis.Boolean(object.reserveOnly)
        : isSet(object.reserve_only)
        ? globalThis.Boolean(object.reserve_only)
        : false,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      startMsgId: isSet(object.startMsgId)
        ? BigInt(object.startMsgId)
        : isSet(object.start_msg_id)
        ? BigInt(object.start_msg_id)
        : 0n,
      endMsgId: isSet(object.endMsgId)
        ? BigInt(object.endMsgId)
        : isSet(object.end_msg_id)
        ? BigInt(object.end_msg_id)
        : 0n,
      msgCount: isSet(object.msgCount)
        ? globalThis.Number(object.msgCount)
        : isSet(object.msg_count)
        ? globalThis.Number(object.msg_count)
        : 0,
      unencryptedHash: isSet(object.unencryptedHash)
        ? Buffer.from(bytesFromBase64(object.unencryptedHash))
        : isSet(object.unencrypted_hash)
        ? Buffer.from(bytesFromBase64(object.unencrypted_hash))
        : Buffer.alloc(0),
      chunkUrl: isSet(object.chunkUrl)
        ? globalThis.String(object.chunkUrl)
        : isSet(object.chunk_url)
        ? globalThis.String(object.chunk_url)
        : "",
    };
  },

  toJSON(message: PublishHistoryChunkRequest): unknown {
    const obj: any = {};
    if (message.reserveOnly !== false) {
      obj.reserveOnly = message.reserveOnly;
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.startMsgId !== 0n) {
      obj.startMsgId = message.startMsgId.toString();
    }
    if (message.endMsgId !== 0n) {
      obj.endMsgId = message.endMsgId.toString();
    }
    if (message.msgCount !== 0) {
      obj.msgCount = Math.round(message.msgCount);
    }
    if (message.unencryptedHash.length !== 0) {
      obj.unencryptedHash = base64FromBytes(message.unencryptedHash);
    }
    if (message.chunkUrl !== "") {
      obj.chunkUrl = message.chunkUrl;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PublishHistoryChunkRequest>, I>>(base?: I): PublishHistoryChunkRequest {
    return PublishHistoryChunkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PublishHistoryChunkRequest>, I>>(object: I): PublishHistoryChunkRequest {
    const message = createBasePublishHistoryChunkRequest();
    message.reserveOnly = object.reserveOnly ?? false;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.startMsgId = (object.startMsgId !== undefined && object.startMsgId !== null)
      ? BigInt(object.startMsgId)
      : 0n;
    message.endMsgId = (object.endMsgId !== undefined && object.endMsgId !== null) ? BigInt(object.endMsgId) : 0n;
    message.msgCount = object.msgCount ?? 0;
    message.unencryptedHash = object.unencryptedHash ?? Buffer.alloc(0);
    message.chunkUrl = object.chunkUrl ?? "";
    return message;
  },
};

function createBaseDisapproveHistoryChunkRequest(): DisapproveHistoryChunkRequest {
  return { groupId: 0n, chunkId: 0n, reason: "" };
}

export const DisapproveHistoryChunkRequest: MessageFns<DisapproveHistoryChunkRequest> = {
  encode(message: DisapproveHistoryChunkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.chunkId !== 0n) {
      if (BigInt.asUintN(64, message.chunkId) !== message.chunkId) {
        throw new globalThis.Error("value provided for field message.chunkId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.chunkId);
    }
    if (message.reason !== "") {
      writer.uint32(26).string(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DisapproveHistoryChunkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDisapproveHistoryChunkRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.chunkId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.reason = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DisapproveHistoryChunkRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      chunkId: isSet(object.chunkId) ? BigInt(object.chunkId) : isSet(object.chunk_id) ? BigInt(object.chunk_id) : 0n,
      reason: isSet(object.reason) ? globalThis.String(object.reason) : "",
    };
  },

  toJSON(message: DisapproveHistoryChunkRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.chunkId !== 0n) {
      obj.chunkId = message.chunkId.toString();
    }
    if (message.reason !== "") {
      obj.reason = message.reason;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DisapproveHistoryChunkRequest>, I>>(base?: I): DisapproveHistoryChunkRequest {
    return DisapproveHistoryChunkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DisapproveHistoryChunkRequest>, I>>(
    object: I,
  ): DisapproveHistoryChunkRequest {
    const message = createBaseDisapproveHistoryChunkRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.chunkId = (object.chunkId !== undefined && object.chunkId !== null) ? BigInt(object.chunkId) : 0n;
    message.reason = object.reason ?? "";
    return message;
  },
};

function createBaseGetHistoryChunksRequest(): GetHistoryChunksRequest {
  return { groupId: 0n, sinceMsgId: 0n, untilMsgId: 0n, includeUnverified: false };
}

export const GetHistoryChunksRequest: MessageFns<GetHistoryChunksRequest> = {
  encode(message: GetHistoryChunksRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.sinceMsgId !== 0n) {
      if (BigInt.asUintN(64, message.sinceMsgId) !== message.sinceMsgId) {
        throw new globalThis.Error("value provided for field message.sinceMsgId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.sinceMsgId);
    }
    if (message.untilMsgId !== 0n) {
      if (BigInt.asUintN(64, message.untilMsgId) !== message.untilMsgId) {
        throw new globalThis.Error("value provided for field message.untilMsgId of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.untilMsgId);
    }
    if (message.includeUnverified !== false) {
      writer.uint32(32).bool(message.includeUnverified);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetHistoryChunksRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetHistoryChunksRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.sinceMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.untilMsgId = reader.fixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.includeUnverified = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetHistoryChunksRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      sinceMsgId: isSet(object.sinceMsgId)
        ? BigInt(object.sinceMsgId)
        : isSet(object.since_msg_id)
        ? BigInt(object.since_msg_id)
        : 0n,
      untilMsgId: isSet(object.untilMsgId)
        ? BigInt(object.untilMsgId)
        : isSet(object.until_msg_id)
        ? BigInt(object.until_msg_id)
        : 0n,
      includeUnverified: isSet(object.includeUnverified)
        ? globalThis.Boolean(object.includeUnverified)
        : isSet(object.include_unverified)
        ? globalThis.Boolean(object.include_unverified)
        : false,
    };
  },

  toJSON(message: GetHistoryChunksRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.sinceMsgId !== 0n) {
      obj.sinceMsgId = message.sinceMsgId.toString();
    }
    if (message.untilMsgId !== 0n) {
      obj.untilMsgId = message.untilMsgId.toString();
    }
    if (message.includeUnverified !== false) {
      obj.includeUnverified = message.includeUnverified;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetHistoryChunksRequest>, I>>(base?: I): GetHistoryChunksRequest {
    return GetHistoryChunksRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetHistoryChunksRequest>, I>>(object: I): GetHistoryChunksRequest {
    const message = createBaseGetHistoryChunksRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.sinceMsgId = (object.sinceMsgId !== undefined && object.sinceMsgId !== null)
      ? BigInt(object.sinceMsgId)
      : 0n;
    message.untilMsgId = (object.untilMsgId !== undefined && object.untilMsgId !== null)
      ? BigInt(object.untilMsgId)
      : 0n;
    message.includeUnverified = object.includeUnverified ?? false;
    return message;
  },
};

function createBaseGetHistoryChunksResponse(): GetHistoryChunksResponse {
  return { chunks: [] };
}

export const GetHistoryChunksResponse: MessageFns<GetHistoryChunksResponse> = {
  encode(message: GetHistoryChunksResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.chunks) {
      GroupHistoryChunkItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetHistoryChunksResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetHistoryChunksResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.chunks.push(GroupHistoryChunkItem.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetHistoryChunksResponse {
    return {
      chunks: globalThis.Array.isArray(object?.chunks)
        ? object.chunks.map((e: any) => GroupHistoryChunkItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetHistoryChunksResponse): unknown {
    const obj: any = {};
    if (message.chunks?.length) {
      obj.chunks = message.chunks.map((e) => GroupHistoryChunkItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetHistoryChunksResponse>, I>>(base?: I): GetHistoryChunksResponse {
    return GetHistoryChunksResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetHistoryChunksResponse>, I>>(object: I): GetHistoryChunksResponse {
    const message = createBaseGetHistoryChunksResponse();
    message.chunks = object.chunks?.map((e) => GroupHistoryChunkItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseGetPendingHistoryRequests(): GetPendingHistoryRequests {
  return { groupIds: [] };
}

export const GetPendingHistoryRequests: MessageFns<GetPendingHistoryRequests> = {
  encode(message: GetPendingHistoryRequests, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    writer.uint32(10).fork();
    for (const v of message.groupIds) {
      if (BigInt.asUintN(64, v) !== v) {
        throw new globalThis.Error("a value provided in array field groupIds of type uint64 is too large");
      }
      writer.uint64(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPendingHistoryRequests {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetPendingHistoryRequests();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag === 8) {
              message.groupIds.push(reader.uint64() as bigint);

              continue;
            }

            if (tag === 10) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.groupIds.push(reader.uint64() as bigint);
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetPendingHistoryRequests {
    return {
      groupIds: globalThis.Array.isArray(object?.groupIds)
        ? object.groupIds.map((e: any) => BigInt(e))
        : globalThis.Array.isArray(object?.group_ids)
        ? object.group_ids.map((e: any) => BigInt(e))
        : [],
    };
  },

  toJSON(message: GetPendingHistoryRequests): unknown {
    const obj: any = {};
    if (message.groupIds?.length) {
      obj.groupIds = message.groupIds.map((e) => e.toString());
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPendingHistoryRequests>, I>>(base?: I): GetPendingHistoryRequests {
    return GetPendingHistoryRequests.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPendingHistoryRequests>, I>>(object: I): GetPendingHistoryRequests {
    const message = createBaseGetPendingHistoryRequests();
    message.groupIds = object.groupIds?.map((e) => BigInt(e)) || [];
    return message;
  },
};

function createBaseGetPendingHistoryRequestsResponse(): GetPendingHistoryRequestsResponse {
  return { requests: [] };
}

export const GetPendingHistoryRequestsResponse: MessageFns<GetPendingHistoryRequestsResponse> = {
  encode(message: GetPendingHistoryRequestsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.requests) {
      GroupHistoryRequestItem.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPendingHistoryRequestsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGetPendingHistoryRequestsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.requests.push(GroupHistoryRequestItem.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GetPendingHistoryRequestsResponse {
    return {
      requests: globalThis.Array.isArray(object?.requests)
        ? object.requests.map((e: any) => GroupHistoryRequestItem.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPendingHistoryRequestsResponse): unknown {
    const obj: any = {};
    if (message.requests?.length) {
      obj.requests = message.requests.map((e) => GroupHistoryRequestItem.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPendingHistoryRequestsResponse>, I>>(
    base?: I,
  ): GetPendingHistoryRequestsResponse {
    return GetPendingHistoryRequestsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPendingHistoryRequestsResponse>, I>>(
    object: I,
  ): GetPendingHistoryRequestsResponse {
    const message = createBaseGetPendingHistoryRequestsResponse();
    message.requests = object.requests?.map((e) => GroupHistoryRequestItem.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCloseHistoryRequest(): CloseHistoryRequest {
  return { groupId: 0n, requestId: 0n };
}

export const CloseHistoryRequest: MessageFns<CloseHistoryRequest> = {
  encode(message: CloseHistoryRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.requestId !== 0n) {
      if (BigInt.asUintN(64, message.requestId) !== message.requestId) {
        throw new globalThis.Error("value provided for field message.requestId of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CloseHistoryRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCloseHistoryRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.requestId = reader.fixed64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): CloseHistoryRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      requestId: isSet(object.requestId)
        ? BigInt(object.requestId)
        : isSet(object.request_id)
        ? BigInt(object.request_id)
        : 0n,
    };
  },

  toJSON(message: CloseHistoryRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.requestId !== 0n) {
      obj.requestId = message.requestId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CloseHistoryRequest>, I>>(base?: I): CloseHistoryRequest {
    return CloseHistoryRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CloseHistoryRequest>, I>>(object: I): CloseHistoryRequest {
    const message = createBaseCloseHistoryRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.requestId = (object.requestId !== undefined && object.requestId !== null) ? BigInt(object.requestId) : 0n;
    return message;
  },
};

function createBaseHistoryOperationSuccess(): HistoryOperationSuccess {
  return {};
}

export const HistoryOperationSuccess: MessageFns<HistoryOperationSuccess> = {
  encode(_: HistoryOperationSuccess, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HistoryOperationSuccess {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHistoryOperationSuccess();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): HistoryOperationSuccess {
    return {};
  },

  toJSON(_: HistoryOperationSuccess): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<HistoryOperationSuccess>, I>>(base?: I): HistoryOperationSuccess {
    return HistoryOperationSuccess.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HistoryOperationSuccess>, I>>(_: I): HistoryOperationSuccess {
    const message = createBaseHistoryOperationSuccess();
    return message;
  },
};

function createBaseGroupHistorySignal(): GroupHistorySignal {
  return { groupId: 0n, type: 0, requestId: 0n, chunkId: 0n, username: "" };
}

export const GroupHistorySignal: MessageFns<GroupHistorySignal> = {
  encode(message: GroupHistorySignal, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.type !== 0) {
      writer.uint32(16).int32(message.type);
    }
    if (message.requestId !== 0n) {
      if (BigInt.asUintN(64, message.requestId) !== message.requestId) {
        throw new globalThis.Error("value provided for field message.requestId of type fixed64 too large");
      }
      writer.uint32(25).fixed64(message.requestId);
    }
    if (message.chunkId !== 0n) {
      if (BigInt.asUintN(64, message.chunkId) !== message.chunkId) {
        throw new globalThis.Error("value provided for field message.chunkId of type uint64 too large");
      }
      writer.uint32(32).uint64(message.chunkId);
    }
    if (message.username !== "") {
      writer.uint32(42).string(message.username);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupHistorySignal {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupHistorySignal();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.type = reader.int32() as any;
            continue;
          }
          case 3: {
            if (tag !== 25) {
              break;
            }

            message.requestId = reader.fixed64() as bigint;
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.chunkId = reader.uint64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.username = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupHistorySignal {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      type: isSet(object.type) ? historySignalTypeFromJSON(object.type) : 0,
      requestId: isSet(object.requestId)
        ? BigInt(object.requestId)
        : isSet(object.request_id)
        ? BigInt(object.request_id)
        : 0n,
      chunkId: isSet(object.chunkId) ? BigInt(object.chunkId) : isSet(object.chunk_id) ? BigInt(object.chunk_id) : 0n,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
    };
  },

  toJSON(message: GroupHistorySignal): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.type !== 0) {
      obj.type = historySignalTypeToJSON(message.type);
    }
    if (message.requestId !== 0n) {
      obj.requestId = message.requestId.toString();
    }
    if (message.chunkId !== 0n) {
      obj.chunkId = message.chunkId.toString();
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupHistorySignal>, I>>(base?: I): GroupHistorySignal {
    return GroupHistorySignal.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupHistorySignal>, I>>(object: I): GroupHistorySignal {
    const message = createBaseGroupHistorySignal();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.type = object.type ?? 0;
    message.requestId = (object.requestId !== undefined && object.requestId !== null) ? BigInt(object.requestId) : 0n;
    message.chunkId = (object.chunkId !== undefined && object.chunkId !== null) ? BigInt(object.chunkId) : 0n;
    message.username = object.username ?? "";
    return message;
  },
};

function createBaseGroupHistoryRecord(): GroupHistoryRecord {
  return { id: 0n, groupId: 0n, message: Buffer.alloc(0), epoch: 0, sender: "" };
}

export const GroupHistoryRecord: MessageFns<GroupHistoryRecord> = {
  encode(message: GroupHistoryRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== 0n) {
      if (BigInt.asUintN(64, message.id) !== message.id) {
        throw new globalThis.Error("value provided for field message.id of type fixed64 too large");
      }
      writer.uint32(9).fixed64(message.id);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.message.length !== 0) {
      writer.uint32(26).bytes(message.message);
    }
    if (message.epoch !== 0) {
      writer.uint32(32).uint32(message.epoch);
    }
    if (message.sender !== "") {
      writer.uint32(42).string(message.sender);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupHistoryRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupHistoryRecord();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 9) {
              break;
            }

            message.id = reader.fixed64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.message = Buffer.from(reader.bytes());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.epoch = reader.uint32();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.sender = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupHistoryRecord {
    return {
      id: isSet(object.id) ? BigInt(object.id) : 0n,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      message: isSet(object.message) ? Buffer.from(bytesFromBase64(object.message)) : Buffer.alloc(0),
      epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
      sender: isSet(object.sender) ? globalThis.String(object.sender) : "",
    };
  },

  toJSON(message: GroupHistoryRecord): unknown {
    const obj: any = {};
    if (message.id !== 0n) {
      obj.id = message.id.toString();
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.message.length !== 0) {
      obj.message = base64FromBytes(message.message);
    }
    if (message.epoch !== 0) {
      obj.epoch = Math.round(message.epoch);
    }
    if (message.sender !== "") {
      obj.sender = message.sender;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupHistoryRecord>, I>>(base?: I): GroupHistoryRecord {
    return GroupHistoryRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupHistoryRecord>, I>>(object: I): GroupHistoryRecord {
    const message = createBaseGroupHistoryRecord();
    message.id = (object.id !== undefined && object.id !== null) ? BigInt(object.id) : 0n;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.message = object.message ?? Buffer.alloc(0);
    message.epoch = object.epoch ?? 0;
    message.sender = object.sender ?? "";
    return message;
  },
};

function createBaseGroupHistoryRecords(): GroupHistoryRecords {
  return { messages: [], formatVersion: 0 };
}

export const GroupHistoryRecords: MessageFns<GroupHistoryRecords> = {
  encode(message: GroupHistoryRecords, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.messages) {
      GroupHistoryRecord.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.formatVersion !== 0) {
      writer.uint32(16).uint32(message.formatVersion);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupHistoryRecords {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupHistoryRecords();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.messages.push(GroupHistoryRecord.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.formatVersion = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupHistoryRecords {
    return {
      messages: globalThis.Array.isArray(object?.messages)
        ? object.messages.map((e: any) => GroupHistoryRecord.fromJSON(e))
        : [],
      formatVersion: isSet(object.formatVersion)
        ? globalThis.Number(object.formatVersion)
        : isSet(object.format_version)
        ? globalThis.Number(object.format_version)
        : 0,
    };
  },

  toJSON(message: GroupHistoryRecords): unknown {
    const obj: any = {};
    if (message.messages?.length) {
      obj.messages = message.messages.map((e) => GroupHistoryRecord.toJSON(e));
    }
    if (message.formatVersion !== 0) {
      obj.formatVersion = Math.round(message.formatVersion);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupHistoryRecords>, I>>(base?: I): GroupHistoryRecords {
    return GroupHistoryRecords.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupHistoryRecords>, I>>(object: I): GroupHistoryRecords {
    const message = createBaseGroupHistoryRecords();
    message.messages = object.messages?.map((e) => GroupHistoryRecord.fromPartial(e)) || [];
    message.formatVersion = object.formatVersion ?? 0;
    return message;
  },
};

function createBaseApproveHistoryChunkRequest(): ApproveHistoryChunkRequest {
  return { groupId: 0n, chunkId: 0n, unencryptedHash: Buffer.alloc(0) };
}

export const ApproveHistoryChunkRequest: MessageFns<ApproveHistoryChunkRequest> = {
  encode(message: ApproveHistoryChunkRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.groupId);
    }
    if (message.chunkId !== 0n) {
      if (BigInt.asUintN(64, message.chunkId) !== message.chunkId) {
        throw new globalThis.Error("value provided for field message.chunkId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.chunkId);
    }
    if (message.unencryptedHash.length !== 0) {
      writer.uint32(26).bytes(message.unencryptedHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ApproveHistoryChunkRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseApproveHistoryChunkRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.chunkId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.unencryptedHash = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): ApproveHistoryChunkRequest {
    return {
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      chunkId: isSet(object.chunkId) ? BigInt(object.chunkId) : isSet(object.chunk_id) ? BigInt(object.chunk_id) : 0n,
      unencryptedHash: isSet(object.unencryptedHash)
        ? Buffer.from(bytesFromBase64(object.unencryptedHash))
        : isSet(object.unencrypted_hash)
        ? Buffer.from(bytesFromBase64(object.unencrypted_hash))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: ApproveHistoryChunkRequest): unknown {
    const obj: any = {};
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.chunkId !== 0n) {
      obj.chunkId = message.chunkId.toString();
    }
    if (message.unencryptedHash.length !== 0) {
      obj.unencryptedHash = base64FromBytes(message.unencryptedHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ApproveHistoryChunkRequest>, I>>(base?: I): ApproveHistoryChunkRequest {
    return ApproveHistoryChunkRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ApproveHistoryChunkRequest>, I>>(object: I): ApproveHistoryChunkRequest {
    const message = createBaseApproveHistoryChunkRequest();
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.chunkId = (object.chunkId !== undefined && object.chunkId !== null) ? BigInt(object.chunkId) : 0n;
    message.unencryptedHash = object.unencryptedHash ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseGroupSnapshotSecret(): GroupSnapshotSecret {
  return {
    kind: 0,
    snapshotId: "",
    key: Buffer.alloc(0),
    plaintextHash: Buffer.alloc(0),
    ciphertextHash: Buffer.alloc(0),
    messageCount: 0,
    startId: 0n,
    endId: 0n,
  };
}

export const GroupSnapshotSecret: MessageFns<GroupSnapshotSecret> = {
  encode(message: GroupSnapshotSecret, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.kind !== 0) {
      writer.uint32(8).uint32(message.kind);
    }
    if (message.snapshotId !== "") {
      writer.uint32(18).string(message.snapshotId);
    }
    if (message.key.length !== 0) {
      writer.uint32(26).bytes(message.key);
    }
    if (message.plaintextHash.length !== 0) {
      writer.uint32(34).bytes(message.plaintextHash);
    }
    if (message.ciphertextHash.length !== 0) {
      writer.uint32(42).bytes(message.ciphertextHash);
    }
    if (message.messageCount !== 0) {
      writer.uint32(48).uint32(message.messageCount);
    }
    if (message.startId !== 0n) {
      if (BigInt.asUintN(64, message.startId) !== message.startId) {
        throw new globalThis.Error("value provided for field message.startId of type uint64 too large");
      }
      writer.uint32(56).uint64(message.startId);
    }
    if (message.endId !== 0n) {
      if (BigInt.asUintN(64, message.endId) !== message.endId) {
        throw new globalThis.Error("value provided for field message.endId of type uint64 too large");
      }
      writer.uint32(64).uint64(message.endId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupSnapshotSecret {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupSnapshotSecret();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.kind = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.snapshotId = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.key = Buffer.from(reader.bytes());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.plaintextHash = Buffer.from(reader.bytes());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.ciphertextHash = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.messageCount = reader.uint32();
            continue;
          }
          case 7: {
            if (tag !== 56) {
              break;
            }

            message.startId = reader.uint64() as bigint;
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.endId = reader.uint64() as bigint;
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupSnapshotSecret {
    return {
      kind: isSet(object.kind) ? globalThis.Number(object.kind) : 0,
      snapshotId: isSet(object.snapshotId)
        ? globalThis.String(object.snapshotId)
        : isSet(object.snapshot_id)
        ? globalThis.String(object.snapshot_id)
        : "",
      key: isSet(object.key) ? Buffer.from(bytesFromBase64(object.key)) : Buffer.alloc(0),
      plaintextHash: isSet(object.plaintextHash)
        ? Buffer.from(bytesFromBase64(object.plaintextHash))
        : isSet(object.plaintext_hash)
        ? Buffer.from(bytesFromBase64(object.plaintext_hash))
        : Buffer.alloc(0),
      ciphertextHash: isSet(object.ciphertextHash)
        ? Buffer.from(bytesFromBase64(object.ciphertextHash))
        : isSet(object.ciphertext_hash)
        ? Buffer.from(bytesFromBase64(object.ciphertext_hash))
        : Buffer.alloc(0),
      messageCount: isSet(object.messageCount)
        ? globalThis.Number(object.messageCount)
        : isSet(object.message_count)
        ? globalThis.Number(object.message_count)
        : 0,
      startId: isSet(object.startId) ? BigInt(object.startId) : isSet(object.start_id) ? BigInt(object.start_id) : 0n,
      endId: isSet(object.endId) ? BigInt(object.endId) : isSet(object.end_id) ? BigInt(object.end_id) : 0n,
    };
  },

  toJSON(message: GroupSnapshotSecret): unknown {
    const obj: any = {};
    if (message.kind !== 0) {
      obj.kind = Math.round(message.kind);
    }
    if (message.snapshotId !== "") {
      obj.snapshotId = message.snapshotId;
    }
    if (message.key.length !== 0) {
      obj.key = base64FromBytes(message.key);
    }
    if (message.plaintextHash.length !== 0) {
      obj.plaintextHash = base64FromBytes(message.plaintextHash);
    }
    if (message.ciphertextHash.length !== 0) {
      obj.ciphertextHash = base64FromBytes(message.ciphertextHash);
    }
    if (message.messageCount !== 0) {
      obj.messageCount = Math.round(message.messageCount);
    }
    if (message.startId !== 0n) {
      obj.startId = message.startId.toString();
    }
    if (message.endId !== 0n) {
      obj.endId = message.endId.toString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupSnapshotSecret>, I>>(base?: I): GroupSnapshotSecret {
    return GroupSnapshotSecret.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupSnapshotSecret>, I>>(object: I): GroupSnapshotSecret {
    const message = createBaseGroupSnapshotSecret();
    message.kind = object.kind ?? 0;
    message.snapshotId = object.snapshotId ?? "";
    message.key = object.key ?? Buffer.alloc(0);
    message.plaintextHash = object.plaintextHash ?? Buffer.alloc(0);
    message.ciphertextHash = object.ciphertextHash ?? Buffer.alloc(0);
    message.messageCount = object.messageCount ?? 0;
    message.startId = (object.startId !== undefined && object.startId !== null) ? BigInt(object.startId) : 0n;
    message.endId = (object.endId !== undefined && object.endId !== null) ? BigInt(object.endId) : 0n;
    return message;
  },
};

function createBaseGroupSyncBundle(): GroupSyncBundle {
  return {
    formatVersion: 0,
    groupId: 0n,
    pinned: undefined,
    recent: undefined,
    keys: [],
    chunks: [],
    bundleId: "",
    page: 0,
    lastPage: false,
  };
}

export const GroupSyncBundle: MessageFns<GroupSyncBundle> = {
  encode(message: GroupSyncBundle, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.formatVersion !== 0) {
      writer.uint32(8).uint32(message.formatVersion);
    }
    if (message.groupId !== 0n) {
      if (BigInt.asUintN(64, message.groupId) !== message.groupId) {
        throw new globalThis.Error("value provided for field message.groupId of type uint64 too large");
      }
      writer.uint32(16).uint64(message.groupId);
    }
    if (message.pinned !== undefined) {
      GroupSnapshotSecret.encode(message.pinned, writer.uint32(26).fork()).join();
    }
    if (message.recent !== undefined) {
      GroupSnapshotSecret.encode(message.recent, writer.uint32(34).fork()).join();
    }
    for (const v of message.keys) {
      GroupHistoryChunkKey.encode(v!, writer.uint32(42).fork()).join();
    }
    for (const v of message.chunks) {
      GroupHistoryChunkItem.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.bundleId !== "") {
      writer.uint32(58).string(message.bundleId);
    }
    if (message.page !== 0) {
      writer.uint32(64).uint32(message.page);
    }
    if (message.lastPage !== false) {
      writer.uint32(72).bool(message.lastPage);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GroupSyncBundle {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseGroupSyncBundle();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.formatVersion = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.groupId = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.pinned = GroupSnapshotSecret.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.recent = GroupSnapshotSecret.decode(reader, reader.uint32());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.keys.push(GroupHistoryChunkKey.decode(reader, reader.uint32()));
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.chunks.push(GroupHistoryChunkItem.decode(reader, reader.uint32()));
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.bundleId = reader.string();
            continue;
          }
          case 8: {
            if (tag !== 64) {
              break;
            }

            message.page = reader.uint32();
            continue;
          }
          case 9: {
            if (tag !== 72) {
              break;
            }

            message.lastPage = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): GroupSyncBundle {
    return {
      formatVersion: isSet(object.formatVersion)
        ? globalThis.Number(object.formatVersion)
        : isSet(object.format_version)
        ? globalThis.Number(object.format_version)
        : 0,
      groupId: isSet(object.groupId) ? BigInt(object.groupId) : isSet(object.group_id) ? BigInt(object.group_id) : 0n,
      pinned: isSet(object.pinned) ? GroupSnapshotSecret.fromJSON(object.pinned) : undefined,
      recent: isSet(object.recent) ? GroupSnapshotSecret.fromJSON(object.recent) : undefined,
      keys: globalThis.Array.isArray(object?.keys) ? object.keys.map((e: any) => GroupHistoryChunkKey.fromJSON(e)) : [],
      chunks: globalThis.Array.isArray(object?.chunks)
        ? object.chunks.map((e: any) => GroupHistoryChunkItem.fromJSON(e))
        : [],
      bundleId: isSet(object.bundleId)
        ? globalThis.String(object.bundleId)
        : isSet(object.bundle_id)
        ? globalThis.String(object.bundle_id)
        : "",
      page: isSet(object.page) ? globalThis.Number(object.page) : 0,
      lastPage: isSet(object.lastPage)
        ? globalThis.Boolean(object.lastPage)
        : isSet(object.last_page)
        ? globalThis.Boolean(object.last_page)
        : false,
    };
  },

  toJSON(message: GroupSyncBundle): unknown {
    const obj: any = {};
    if (message.formatVersion !== 0) {
      obj.formatVersion = Math.round(message.formatVersion);
    }
    if (message.groupId !== 0n) {
      obj.groupId = message.groupId.toString();
    }
    if (message.pinned !== undefined) {
      obj.pinned = GroupSnapshotSecret.toJSON(message.pinned);
    }
    if (message.recent !== undefined) {
      obj.recent = GroupSnapshotSecret.toJSON(message.recent);
    }
    if (message.keys?.length) {
      obj.keys = message.keys.map((e) => GroupHistoryChunkKey.toJSON(e));
    }
    if (message.chunks?.length) {
      obj.chunks = message.chunks.map((e) => GroupHistoryChunkItem.toJSON(e));
    }
    if (message.bundleId !== "") {
      obj.bundleId = message.bundleId;
    }
    if (message.page !== 0) {
      obj.page = Math.round(message.page);
    }
    if (message.lastPage !== false) {
      obj.lastPage = message.lastPage;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GroupSyncBundle>, I>>(base?: I): GroupSyncBundle {
    return GroupSyncBundle.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GroupSyncBundle>, I>>(object: I): GroupSyncBundle {
    const message = createBaseGroupSyncBundle();
    message.formatVersion = object.formatVersion ?? 0;
    message.groupId = (object.groupId !== undefined && object.groupId !== null) ? BigInt(object.groupId) : 0n;
    message.pinned = (object.pinned !== undefined && object.pinned !== null)
      ? GroupSnapshotSecret.fromPartial(object.pinned)
      : undefined;
    message.recent = (object.recent !== undefined && object.recent !== null)
      ? GroupSnapshotSecret.fromPartial(object.recent)
      : undefined;
    message.keys = object.keys?.map((e) => GroupHistoryChunkKey.fromPartial(e)) || [];
    message.chunks = object.chunks?.map((e) => GroupHistoryChunkItem.fromPartial(e)) || [];
    message.bundleId = object.bundleId ?? "";
    message.page = object.page ?? 0;
    message.lastPage = object.lastPage ?? false;
    return message;
  },
};

function createBaseDirectBackupChunk(): DirectBackupChunk {
  return { id: "", start: 0n, count: 0, blobPath: "", byteSize: 0n, ciphertextHash: Buffer.alloc(0) };
}

export const DirectBackupChunk: MessageFns<DirectBackupChunk> = {
  encode(message: DirectBackupChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.start !== 0n) {
      if (BigInt.asUintN(64, message.start) !== message.start) {
        throw new globalThis.Error("value provided for field message.start of type uint64 too large");
      }
      writer.uint32(16).uint64(message.start);
    }
    if (message.count !== 0) {
      writer.uint32(24).uint32(message.count);
    }
    if (message.blobPath !== "") {
      writer.uint32(34).string(message.blobPath);
    }
    if (message.byteSize !== 0n) {
      if (BigInt.asUintN(64, message.byteSize) !== message.byteSize) {
        throw new globalThis.Error("value provided for field message.byteSize of type uint64 too large");
      }
      writer.uint32(40).uint64(message.byteSize);
    }
    if (message.ciphertextHash.length !== 0) {
      writer.uint32(50).bytes(message.ciphertextHash);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectBackupChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectBackupChunk();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.id = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.start = reader.uint64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.count = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.blobPath = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.byteSize = reader.uint64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.ciphertextHash = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectBackupChunk {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      start: isSet(object.start) ? BigInt(object.start) : 0n,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
      blobPath: isSet(object.blobPath)
        ? globalThis.String(object.blobPath)
        : isSet(object.blob_path)
        ? globalThis.String(object.blob_path)
        : "",
      byteSize: isSet(object.byteSize)
        ? BigInt(object.byteSize)
        : isSet(object.byte_size)
        ? BigInt(object.byte_size)
        : 0n,
      ciphertextHash: isSet(object.ciphertextHash)
        ? Buffer.from(bytesFromBase64(object.ciphertextHash))
        : isSet(object.ciphertext_hash)
        ? Buffer.from(bytesFromBase64(object.ciphertext_hash))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: DirectBackupChunk): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.start !== 0n) {
      obj.start = message.start.toString();
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    if (message.blobPath !== "") {
      obj.blobPath = message.blobPath;
    }
    if (message.byteSize !== 0n) {
      obj.byteSize = message.byteSize.toString();
    }
    if (message.ciphertextHash.length !== 0) {
      obj.ciphertextHash = base64FromBytes(message.ciphertextHash);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectBackupChunk>, I>>(base?: I): DirectBackupChunk {
    return DirectBackupChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectBackupChunk>, I>>(object: I): DirectBackupChunk {
    const message = createBaseDirectBackupChunk();
    message.id = object.id ?? "";
    message.start = (object.start !== undefined && object.start !== null) ? BigInt(object.start) : 0n;
    message.count = object.count ?? 0;
    message.blobPath = object.blobPath ?? "";
    message.byteSize = (object.byteSize !== undefined && object.byteSize !== null) ? BigInt(object.byteSize) : 0n;
    message.ciphertextHash = object.ciphertextHash ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseDirectBackupManifest(): DirectBackupManifest {
  return {
    version: 0,
    epoch: "",
    salt: Buffer.alloc(0),
    verifier: Buffer.alloc(0),
    scheduleDays: 0,
    chunks: [],
    authentication: Buffer.alloc(0),
  };
}

export const DirectBackupManifest: MessageFns<DirectBackupManifest> = {
  encode(message: DirectBackupManifest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.epoch !== "") {
      writer.uint32(18).string(message.epoch);
    }
    if (message.salt.length !== 0) {
      writer.uint32(26).bytes(message.salt);
    }
    if (message.verifier.length !== 0) {
      writer.uint32(34).bytes(message.verifier);
    }
    if (message.scheduleDays !== 0) {
      writer.uint32(40).uint32(message.scheduleDays);
    }
    for (const v of message.chunks) {
      DirectBackupChunk.encode(v!, writer.uint32(50).fork()).join();
    }
    if (message.authentication.length !== 0) {
      writer.uint32(58).bytes(message.authentication);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectBackupManifest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectBackupManifest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.version = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.epoch = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.salt = Buffer.from(reader.bytes());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.verifier = Buffer.from(reader.bytes());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.scheduleDays = reader.uint32();
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.chunks.push(DirectBackupChunk.decode(reader, reader.uint32()));
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.authentication = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectBackupManifest {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "",
      salt: isSet(object.salt) ? Buffer.from(bytesFromBase64(object.salt)) : Buffer.alloc(0),
      verifier: isSet(object.verifier) ? Buffer.from(bytesFromBase64(object.verifier)) : Buffer.alloc(0),
      scheduleDays: isSet(object.scheduleDays)
        ? globalThis.Number(object.scheduleDays)
        : isSet(object.schedule_days)
        ? globalThis.Number(object.schedule_days)
        : 0,
      chunks: globalThis.Array.isArray(object?.chunks)
        ? object.chunks.map((e: any) => DirectBackupChunk.fromJSON(e))
        : [],
      authentication: isSet(object.authentication)
        ? Buffer.from(bytesFromBase64(object.authentication))
        : Buffer.alloc(0),
    };
  },

  toJSON(message: DirectBackupManifest): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.epoch !== "") {
      obj.epoch = message.epoch;
    }
    if (message.salt.length !== 0) {
      obj.salt = base64FromBytes(message.salt);
    }
    if (message.verifier.length !== 0) {
      obj.verifier = base64FromBytes(message.verifier);
    }
    if (message.scheduleDays !== 0) {
      obj.scheduleDays = Math.round(message.scheduleDays);
    }
    if (message.chunks?.length) {
      obj.chunks = message.chunks.map((e) => DirectBackupChunk.toJSON(e));
    }
    if (message.authentication.length !== 0) {
      obj.authentication = base64FromBytes(message.authentication);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectBackupManifest>, I>>(base?: I): DirectBackupManifest {
    return DirectBackupManifest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectBackupManifest>, I>>(object: I): DirectBackupManifest {
    const message = createBaseDirectBackupManifest();
    message.version = object.version ?? 0;
    message.epoch = object.epoch ?? "";
    message.salt = object.salt ?? Buffer.alloc(0);
    message.verifier = object.verifier ?? Buffer.alloc(0);
    message.scheduleDays = object.scheduleDays ?? 0;
    message.chunks = object.chunks?.map((e) => DirectBackupChunk.fromPartial(e)) || [];
    message.authentication = object.authentication ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseDirectBackupState(): DirectBackupState {
  return { revision: 0n, updatedAt: 0n, manifest: undefined };
}

export const DirectBackupState: MessageFns<DirectBackupState> = {
  encode(message: DirectBackupState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.revision !== 0n) {
      if (BigInt.asIntN(64, message.revision) !== message.revision) {
        throw new globalThis.Error("value provided for field message.revision of type int64 too large");
      }
      writer.uint32(8).int64(message.revision);
    }
    if (message.updatedAt !== 0n) {
      if (BigInt.asIntN(64, message.updatedAt) !== message.updatedAt) {
        throw new globalThis.Error("value provided for field message.updatedAt of type int64 too large");
      }
      writer.uint32(16).int64(message.updatedAt);
    }
    if (message.manifest !== undefined) {
      DirectBackupManifest.encode(message.manifest, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectBackupState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectBackupState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.revision = reader.int64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.updatedAt = reader.int64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.manifest = DirectBackupManifest.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectBackupState {
    return {
      revision: isSet(object.revision) ? BigInt(object.revision) : 0n,
      updatedAt: isSet(object.updatedAt)
        ? BigInt(object.updatedAt)
        : isSet(object.updated_at)
        ? BigInt(object.updated_at)
        : 0n,
      manifest: isSet(object.manifest) ? DirectBackupManifest.fromJSON(object.manifest) : undefined,
    };
  },

  toJSON(message: DirectBackupState): unknown {
    const obj: any = {};
    if (message.revision !== 0n) {
      obj.revision = message.revision.toString();
    }
    if (message.updatedAt !== 0n) {
      obj.updatedAt = message.updatedAt.toString();
    }
    if (message.manifest !== undefined) {
      obj.manifest = DirectBackupManifest.toJSON(message.manifest);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectBackupState>, I>>(base?: I): DirectBackupState {
    return DirectBackupState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectBackupState>, I>>(object: I): DirectBackupState {
    const message = createBaseDirectBackupState();
    message.revision = (object.revision !== undefined && object.revision !== null) ? BigInt(object.revision) : 0n;
    message.updatedAt = (object.updatedAt !== undefined && object.updatedAt !== null) ? BigInt(object.updatedAt) : 0n;
    message.manifest = (object.manifest !== undefined && object.manifest !== null)
      ? DirectBackupManifest.fromPartial(object.manifest)
      : undefined;
    return message;
  },
};

function createBaseDirectBackupRequest(): DirectBackupRequest {
  return {
    action: 0,
    revision: 0n,
    leaseId: "",
    id: "",
    ciphertext: Buffer.alloc(0),
    manifest: undefined,
    object: undefined,
  };
}

export const DirectBackupRequest: MessageFns<DirectBackupRequest> = {
  encode(message: DirectBackupRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.action !== 0) {
      writer.uint32(8).int32(message.action);
    }
    if (message.revision !== 0n) {
      if (BigInt.asIntN(64, message.revision) !== message.revision) {
        throw new globalThis.Error("value provided for field message.revision of type int64 too large");
      }
      writer.uint32(16).int64(message.revision);
    }
    if (message.leaseId !== "") {
      writer.uint32(26).string(message.leaseId);
    }
    if (message.id !== "") {
      writer.uint32(34).string(message.id);
    }
    if (message.ciphertext.length !== 0) {
      writer.uint32(42).bytes(message.ciphertext);
    }
    if (message.manifest !== undefined) {
      DirectBackupManifest.encode(message.manifest, writer.uint32(50).fork()).join();
    }
    if (message.object !== undefined) {
      DirectBackupChunk.encode(message.object, writer.uint32(58).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectBackupRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectBackupRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.action = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.revision = reader.int64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.leaseId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.id = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.ciphertext = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.manifest = DirectBackupManifest.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.object = DirectBackupChunk.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectBackupRequest {
    return {
      action: isSet(object.action) ? directBackupRequest_ActionFromJSON(object.action) : 0,
      revision: isSet(object.revision) ? BigInt(object.revision) : 0n,
      leaseId: isSet(object.leaseId)
        ? globalThis.String(object.leaseId)
        : isSet(object.lease_id)
        ? globalThis.String(object.lease_id)
        : "",
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      ciphertext: isSet(object.ciphertext) ? Buffer.from(bytesFromBase64(object.ciphertext)) : Buffer.alloc(0),
      manifest: isSet(object.manifest) ? DirectBackupManifest.fromJSON(object.manifest) : undefined,
      object: isSet(object.object) ? DirectBackupChunk.fromJSON(object.object) : undefined,
    };
  },

  toJSON(message: DirectBackupRequest): unknown {
    const obj: any = {};
    if (message.action !== 0) {
      obj.action = directBackupRequest_ActionToJSON(message.action);
    }
    if (message.revision !== 0n) {
      obj.revision = message.revision.toString();
    }
    if (message.leaseId !== "") {
      obj.leaseId = message.leaseId;
    }
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.ciphertext.length !== 0) {
      obj.ciphertext = base64FromBytes(message.ciphertext);
    }
    if (message.manifest !== undefined) {
      obj.manifest = DirectBackupManifest.toJSON(message.manifest);
    }
    if (message.object !== undefined) {
      obj.object = DirectBackupChunk.toJSON(message.object);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectBackupRequest>, I>>(base?: I): DirectBackupRequest {
    return DirectBackupRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectBackupRequest>, I>>(object: I): DirectBackupRequest {
    const message = createBaseDirectBackupRequest();
    message.action = object.action ?? 0;
    message.revision = (object.revision !== undefined && object.revision !== null) ? BigInt(object.revision) : 0n;
    message.leaseId = object.leaseId ?? "";
    message.id = object.id ?? "";
    message.ciphertext = object.ciphertext ?? Buffer.alloc(0);
    message.manifest = (object.manifest !== undefined && object.manifest !== null)
      ? DirectBackupManifest.fromPartial(object.manifest)
      : undefined;
    message.object = (object.object !== undefined && object.object !== null)
      ? DirectBackupChunk.fromPartial(object.object)
      : undefined;
    return message;
  },
};

function createBaseDirectBackupRecord(): DirectBackupRecord {
  return { sourceId: 0n, other: "", sentByOther: false, message: Buffer.alloc(0), messageType: 0 };
}

export const DirectBackupRecord: MessageFns<DirectBackupRecord> = {
  encode(message: DirectBackupRecord, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.sourceId !== 0n) {
      if (BigInt.asUintN(64, message.sourceId) !== message.sourceId) {
        throw new globalThis.Error("value provided for field message.sourceId of type uint64 too large");
      }
      writer.uint32(8).uint64(message.sourceId);
    }
    if (message.other !== "") {
      writer.uint32(18).string(message.other);
    }
    if (message.sentByOther !== false) {
      writer.uint32(24).bool(message.sentByOther);
    }
    if (message.message.length !== 0) {
      writer.uint32(34).bytes(message.message);
    }
    if (message.messageType !== 0) {
      writer.uint32(40).uint32(message.messageType);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectBackupRecord {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectBackupRecord();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.sourceId = reader.uint64() as bigint;
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.other = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.sentByOther = reader.bool();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.message = Buffer.from(reader.bytes());
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.messageType = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectBackupRecord {
    return {
      sourceId: isSet(object.sourceId)
        ? BigInt(object.sourceId)
        : isSet(object.source_id)
        ? BigInt(object.source_id)
        : 0n,
      other: isSet(object.other) ? globalThis.String(object.other) : "",
      sentByOther: isSet(object.sentByOther)
        ? globalThis.Boolean(object.sentByOther)
        : isSet(object.sent_by_other)
        ? globalThis.Boolean(object.sent_by_other)
        : false,
      message: isSet(object.message) ? Buffer.from(bytesFromBase64(object.message)) : Buffer.alloc(0),
      messageType: isSet(object.messageType)
        ? globalThis.Number(object.messageType)
        : isSet(object.message_type)
        ? globalThis.Number(object.message_type)
        : 0,
    };
  },

  toJSON(message: DirectBackupRecord): unknown {
    const obj: any = {};
    if (message.sourceId !== 0n) {
      obj.sourceId = message.sourceId.toString();
    }
    if (message.other !== "") {
      obj.other = message.other;
    }
    if (message.sentByOther !== false) {
      obj.sentByOther = message.sentByOther;
    }
    if (message.message.length !== 0) {
      obj.message = base64FromBytes(message.message);
    }
    if (message.messageType !== 0) {
      obj.messageType = Math.round(message.messageType);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectBackupRecord>, I>>(base?: I): DirectBackupRecord {
    return DirectBackupRecord.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectBackupRecord>, I>>(object: I): DirectBackupRecord {
    const message = createBaseDirectBackupRecord();
    message.sourceId = (object.sourceId !== undefined && object.sourceId !== null) ? BigInt(object.sourceId) : 0n;
    message.other = object.other ?? "";
    message.sentByOther = object.sentByOther ?? false;
    message.message = object.message ?? Buffer.alloc(0);
    message.messageType = object.messageType ?? 0;
    return message;
  },
};

function createBaseDirectBackupRecords(): DirectBackupRecords {
  return { version: 0, records: [] };
}

export const DirectBackupRecords: MessageFns<DirectBackupRecords> = {
  encode(message: DirectBackupRecords, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    for (const v of message.records) {
      DirectBackupRecord.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectBackupRecords {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectBackupRecords();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.version = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.records.push(DirectBackupRecord.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectBackupRecords {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      records: globalThis.Array.isArray(object?.records)
        ? object.records.map((e: any) => DirectBackupRecord.fromJSON(e))
        : [],
    };
  },

  toJSON(message: DirectBackupRecords): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.records?.length) {
      obj.records = message.records.map((e) => DirectBackupRecord.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectBackupRecords>, I>>(base?: I): DirectBackupRecords {
    return DirectBackupRecords.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectBackupRecords>, I>>(object: I): DirectBackupRecords {
    const message = createBaseDirectBackupRecords();
    message.version = object.version ?? 0;
    message.records = object.records?.map((e) => DirectBackupRecord.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDirectBackupBinding(): DirectBackupBinding {
  return { version: 0, username: "", epoch: "", chunkId: "", start: 0n, count: 0 };
}

export const DirectBackupBinding: MessageFns<DirectBackupBinding> = {
  encode(message: DirectBackupBinding, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.username !== "") {
      writer.uint32(18).string(message.username);
    }
    if (message.epoch !== "") {
      writer.uint32(26).string(message.epoch);
    }
    if (message.chunkId !== "") {
      writer.uint32(34).string(message.chunkId);
    }
    if (message.start !== 0n) {
      if (BigInt.asUintN(64, message.start) !== message.start) {
        throw new globalThis.Error("value provided for field message.start of type uint64 too large");
      }
      writer.uint32(40).uint64(message.start);
    }
    if (message.count !== 0) {
      writer.uint32(48).uint32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectBackupBinding {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectBackupBinding();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.version = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.username = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.epoch = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.chunkId = reader.string();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.start = reader.uint64() as bigint;
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.count = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectBackupBinding {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      username: isSet(object.username) ? globalThis.String(object.username) : "",
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "",
      chunkId: isSet(object.chunkId)
        ? globalThis.String(object.chunkId)
        : isSet(object.chunk_id)
        ? globalThis.String(object.chunk_id)
        : "",
      start: isSet(object.start) ? BigInt(object.start) : 0n,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: DirectBackupBinding): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.username !== "") {
      obj.username = message.username;
    }
    if (message.epoch !== "") {
      obj.epoch = message.epoch;
    }
    if (message.chunkId !== "") {
      obj.chunkId = message.chunkId;
    }
    if (message.start !== 0n) {
      obj.start = message.start.toString();
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectBackupBinding>, I>>(base?: I): DirectBackupBinding {
    return DirectBackupBinding.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectBackupBinding>, I>>(object: I): DirectBackupBinding {
    const message = createBaseDirectBackupBinding();
    message.version = object.version ?? 0;
    message.username = object.username ?? "";
    message.epoch = object.epoch ?? "";
    message.chunkId = object.chunkId ?? "";
    message.start = (object.start !== undefined && object.start !== null) ? BigInt(object.start) : 0n;
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseDirectBackupKeySync(): DirectBackupKeySync {
  return { version: 0, epoch: "", key: Buffer.alloc(0), requestKey: false, requestId: "" };
}

export const DirectBackupKeySync: MessageFns<DirectBackupKeySync> = {
  encode(message: DirectBackupKeySync, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.epoch !== "") {
      writer.uint32(18).string(message.epoch);
    }
    if (message.key.length !== 0) {
      writer.uint32(26).bytes(message.key);
    }
    if (message.requestKey !== false) {
      writer.uint32(32).bool(message.requestKey);
    }
    if (message.requestId !== "") {
      writer.uint32(42).string(message.requestId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectBackupKeySync {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectBackupKeySync();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.version = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.epoch = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.key = Buffer.from(reader.bytes());
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.requestKey = reader.bool();
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.requestId = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectBackupKeySync {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      epoch: isSet(object.epoch) ? globalThis.String(object.epoch) : "",
      key: isSet(object.key) ? Buffer.from(bytesFromBase64(object.key)) : Buffer.alloc(0),
      requestKey: isSet(object.requestKey)
        ? globalThis.Boolean(object.requestKey)
        : isSet(object.request_key)
        ? globalThis.Boolean(object.request_key)
        : false,
      requestId: isSet(object.requestId)
        ? globalThis.String(object.requestId)
        : isSet(object.request_id)
        ? globalThis.String(object.request_id)
        : "",
    };
  },

  toJSON(message: DirectBackupKeySync): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.epoch !== "") {
      obj.epoch = message.epoch;
    }
    if (message.key.length !== 0) {
      obj.key = base64FromBytes(message.key);
    }
    if (message.requestKey !== false) {
      obj.requestKey = message.requestKey;
    }
    if (message.requestId !== "") {
      obj.requestId = message.requestId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectBackupKeySync>, I>>(base?: I): DirectBackupKeySync {
    return DirectBackupKeySync.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectBackupKeySync>, I>>(object: I): DirectBackupKeySync {
    const message = createBaseDirectBackupKeySync();
    message.version = object.version ?? 0;
    message.epoch = object.epoch ?? "";
    message.key = object.key ?? Buffer.alloc(0);
    message.requestKey = object.requestKey ?? false;
    message.requestId = object.requestId ?? "";
    return message;
  },
};

function createBaseDirectPinSnapshotSecret(): DirectPinSnapshotSecret {
  return {
    version: 0,
    other: "",
    snapshotId: "",
    key: Buffer.alloc(0),
    ciphertextHash: Buffer.alloc(0),
    messageCount: 0,
  };
}

export const DirectPinSnapshotSecret: MessageFns<DirectPinSnapshotSecret> = {
  encode(message: DirectPinSnapshotSecret, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.version !== 0) {
      writer.uint32(8).uint32(message.version);
    }
    if (message.other !== "") {
      writer.uint32(18).string(message.other);
    }
    if (message.snapshotId !== "") {
      writer.uint32(26).string(message.snapshotId);
    }
    if (message.key.length !== 0) {
      writer.uint32(34).bytes(message.key);
    }
    if (message.ciphertextHash.length !== 0) {
      writer.uint32(42).bytes(message.ciphertextHash);
    }
    if (message.messageCount !== 0) {
      writer.uint32(48).uint32(message.messageCount);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DirectPinSnapshotSecret {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseDirectPinSnapshotSecret();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.version = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.other = reader.string();
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.snapshotId = reader.string();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.key = Buffer.from(reader.bytes());
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.ciphertextHash = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.messageCount = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): DirectPinSnapshotSecret {
    return {
      version: isSet(object.version) ? globalThis.Number(object.version) : 0,
      other: isSet(object.other) ? globalThis.String(object.other) : "",
      snapshotId: isSet(object.snapshotId)
        ? globalThis.String(object.snapshotId)
        : isSet(object.snapshot_id)
        ? globalThis.String(object.snapshot_id)
        : "",
      key: isSet(object.key) ? Buffer.from(bytesFromBase64(object.key)) : Buffer.alloc(0),
      ciphertextHash: isSet(object.ciphertextHash)
        ? Buffer.from(bytesFromBase64(object.ciphertextHash))
        : isSet(object.ciphertext_hash)
        ? Buffer.from(bytesFromBase64(object.ciphertext_hash))
        : Buffer.alloc(0),
      messageCount: isSet(object.messageCount)
        ? globalThis.Number(object.messageCount)
        : isSet(object.message_count)
        ? globalThis.Number(object.message_count)
        : 0,
    };
  },

  toJSON(message: DirectPinSnapshotSecret): unknown {
    const obj: any = {};
    if (message.version !== 0) {
      obj.version = Math.round(message.version);
    }
    if (message.other !== "") {
      obj.other = message.other;
    }
    if (message.snapshotId !== "") {
      obj.snapshotId = message.snapshotId;
    }
    if (message.key.length !== 0) {
      obj.key = base64FromBytes(message.key);
    }
    if (message.ciphertextHash.length !== 0) {
      obj.ciphertextHash = base64FromBytes(message.ciphertextHash);
    }
    if (message.messageCount !== 0) {
      obj.messageCount = Math.round(message.messageCount);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DirectPinSnapshotSecret>, I>>(base?: I): DirectPinSnapshotSecret {
    return DirectPinSnapshotSecret.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DirectPinSnapshotSecret>, I>>(object: I): DirectPinSnapshotSecret {
    const message = createBaseDirectPinSnapshotSecret();
    message.version = object.version ?? 0;
    message.other = object.other ?? "";
    message.snapshotId = object.snapshotId ?? "";
    message.key = object.key ?? Buffer.alloc(0);
    message.ciphertextHash = object.ciphertextHash ?? Buffer.alloc(0);
    message.messageCount = object.messageCount ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
